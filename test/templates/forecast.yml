Resources:

  RetainBucket:
    Type: AWS::S3::Bucket
    DeletionPolicy: Retain
    UpdateReplacePolicy: Retain

  NoRetainBucket:
    Type: AWS::S3::Bucket

Outputs:

  RetainBucketName:
    Value: !Ref RetainBucket   
    
  NoRetainBucketName:
    Value: !Ref NoRetainBucket    