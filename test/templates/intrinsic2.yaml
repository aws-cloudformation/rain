Parameters:
  AppName:
    Type: String
Resources:
  App:
    Type: AWS::AppConfig::Application
    Properties:
      Name: !Ref AppName
      Description: "Testing"
  Beta:
    Type: AWS::AppConfig::Environment
    Properties:
      ApplicationId: !Ref App
      Name: !Sub "${AppName}-Beta"
  Gamma:
    Type: AWS::AppConfig::Environment
    Properties:
      ApplicationId: !Ref App
      Name: !Sub "${AppName}-Gamma1"
  Prod:
    Type: AWS::AppConfig::Environment
    Properties:
      ApplicationId: !Ref App
      Name: !Sub "${AppName}-Prod"
