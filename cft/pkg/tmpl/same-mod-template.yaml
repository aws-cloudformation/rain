Parameters:
  XParam:
    Type: String
    Default: default-x
Resources:
  A:
    Type: !Rain::Module same-mod-module.yaml
    Properties:
      X: !Ref XParam
  B:
    Type: !Rain::Module same-mod-module.yaml
    Properties:
      X: x 

