/// AWS::Route53RecoveryControl::SafetyRule
///
/// Generated by rain build --pkl-class AWS::Route53RecoveryControl::SafetyRule
module aws.route53recoverycontrol.safetyrule

import "../../cloudformation.pkl"

/// An assertion rule enforces that, when a routing control state is changed, that the criteria set by the rule configuration is met. Otherwise, the change to the routing control is not accepted.
open class AssertionRule {
    WaitPeriodMs: Int|Mapping
    AssertedControls: Listing<String|Mapping>
}

/// 
open class Tag {
    Key: String|Mapping
    Value: String|Mapping
}

/// The name for the safety rule.
open class SafetyRuleName {
}

/// A rule can be one of the following: ATLEAST, AND, or OR.
open class RuleType {
}

/// The rule configuration for an assertion rule or gating rule. This is the criteria that you set for specific assertion controls (routing controls) or gating controls. This configuration specifies how many controls must be enabled after a transaction completes.
open class RuleConfig {
    Inverted: Boolean|Mapping
    Type: RuleType
    Threshold: Int|Mapping
}

/// A gating rule verifies that a set of gating controls evaluates as true, based on a rule configuration that you specify. If the gating rule evaluates to true, Amazon Route 53 Application Recovery Controller allows a set of routing control state changes to run and complete against the set of target controls.
open class GatingRule {
    GatingControls: Listing<String|Mapping>
    TargetControls: Listing<String|Mapping>
    WaitPeriodMs: Int|Mapping
}

/// Resource schema for AWS Route53 Recovery Control basic constructs and validation rules.
open class SafetyRule extends cloudformation.Resource {

    Type = "AWS::Route53RecoveryControl::SafetyRule"


    /// 
    hidden GatingRule: (GatingRule)?

    /// 
    hidden Name: (SafetyRuleName)?

    /// The Amazon Resource Name (ARN) of the control panel.
    hidden ControlPanelArn: (String|Mapping)?

    /// 
    hidden RuleConfig: (RuleConfig)?

    /// A collection of tags associated with a resource
    hidden Tags: (Listing<Tag>)?

    /// 
    hidden AssertionRule: (AssertionRule)?

    Properties {
        ["GatingRule"] = if (GatingRule == null) null else GatingRule
        ["Name"] = if (Name == null) null else Name
        ["ControlPanelArn"] = if (ControlPanelArn == null) null else ControlPanelArn
        ["RuleConfig"] = if (RuleConfig == null) null else RuleConfig
        ["Tags"] = if (Tags == null) null else Tags
        ["AssertionRule"] = if (AssertionRule == null) null else AssertionRule
    }

}
