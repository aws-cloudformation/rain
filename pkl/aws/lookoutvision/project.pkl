/// AWS::LookoutVision::Project
///
/// Generated by rain build --pkl-class AWS::LookoutVision::Project
module aws.lookoutvision.project

import "../../cloudformation.pkl"

/// 
open class Arn {
}

/// The name of the Amazon Lookout for Vision project
open class ProjectName {
}

/// The AWS::LookoutVision::Project type creates an Amazon Lookout for Vision project. A project is a grouping of the resources needed to create and manage a Lookout for Vision model.
open class Project extends cloudformation.Resource {

    Type = "AWS::LookoutVision::Project"


    /// 
    hidden ProjectName: ProjectName

    Properties {
        ["ProjectName"] = if (ProjectName == null) null else ProjectName
    }

}
