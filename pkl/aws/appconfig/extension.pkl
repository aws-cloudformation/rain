/// AWS::AppConfig::Extension
///
/// Generated by rain build --pkl-class AWS::AppConfig::Extension
module aws.appconfig.extension

import "../../cloudformation.pkl"

/// A key-value pair to associate with a resource.
open class ExtensionTag {
    Key: String|Mapping
    Value: String|Mapping
}

/// A list of actions for an extension to take at a specific action point.
open class ExtensionActions {
}

/// An action for an extension to take at a specific action point.
open class ExtensionAction {
    Name: String|Mapping
    Description: (String|Mapping)?
    Uri: String|Mapping
    RoleArn: (String|Mapping)?
}

/// A parameter for the extension to send to a specific action.
open class ExtensionParameter {
    Dynamic: (Boolean|Mapping)?
    Required: Boolean|Mapping
    Description: (String|Mapping)?
}

/// Resource Type definition for AWS::AppConfig::Extension
open class Extension extends cloudformation.Resource {

    Type = "AWS::AppConfig::Extension"


    /// 
    hidden Actions: Dynamic

    /// 
    hidden Parameters: (Dynamic)?

    /// 
    hidden LatestVersionNumber: (Int|Mapping)?

    /// An array of key-value tags to apply to this resource.
    hidden Tags: (Listing<ExtensionTag>)?

    /// Name of the extension.
    hidden Name: String|Mapping

    /// Description of the extension.
    hidden Description: (String|Mapping)?

    Properties {
        ["Actions"] = if (Actions == null) null else Actions
        ["Parameters"] = if (Parameters == null) null else Parameters
        ["LatestVersionNumber"] = if (LatestVersionNumber == null) null else LatestVersionNumber
        ["Tags"] = if (Tags == null) null else Tags
        ["Name"] = if (Name == null) null else Name
        ["Description"] = if (Description == null) null else Description
    }

}
