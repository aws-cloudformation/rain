/// AWS::DataSync::Agent
///
/// Generated by rain build --pkl-class AWS::DataSync::Agent
module aws.datasync.agent

import "../../cloudformation.pkl"

/// A key-value pair to associate with a resource.
open class AgentTag {
    Key: String(matches(Regex(#"^[a-zA-Z0-9\s+=._:/-]+$"#)))|Mapping
    Value: String(matches(Regex(#"^[a-zA-Z0-9\s+=._:@/-]+$"#)))|Mapping
}

/// Resource schema for AWS::DataSync::Agent.
open class Agent extends cloudformation.Resource {

    Type = "AWS::DataSync::Agent"


    /// Activation key of the Agent.
    hidden ActivationKey: (String(matches(Regex(#"[A-Z0-9]{5}(-[A-Z0-9]{5}){4}"#)))|Mapping)?

    /// The name configured for the agent. Text reference used to identify the agent in the console.
    hidden AgentName: (String(matches(Regex(#"^[a-zA-Z0-9\s+=._:@/-]+$"#)))|Mapping)?

    /// The ARNs of the security group used to protect your data transfer task subnets.
    hidden SecurityGroupArns: (Listing<String|Mapping>)?

    /// The ARNs of the subnets in which DataSync will create elastic network interfaces for each data transfer task.
    hidden SubnetArns: (Listing<String|Mapping>)?

    /// An array of key-value pairs to apply to this resource.
    hidden Tags: (Listing<AgentTag>)?

    /// The ID of the VPC endpoint that the agent has access to.
    hidden VpcEndpointId: (String(matches(Regex(#"^vpce-[0-9a-f]{17}$"#)))|Mapping)?

    Properties {
        ["ActivationKey"] = if (ActivationKey == null) null else ActivationKey
        ["AgentName"] = if (AgentName == null) null else AgentName
        ["SecurityGroupArns"] = if (SecurityGroupArns == null) null else SecurityGroupArns
        ["SubnetArns"] = if (SubnetArns == null) null else SubnetArns
        ["Tags"] = if (Tags == null) null else Tags
        ["VpcEndpointId"] = if (VpcEndpointId == null) null else VpcEndpointId
    }

}
