/// AWS::Config::StoredQuery
///
/// Generated by rain build --pkl-class AWS::Config::StoredQuery
module aws.config.storedquery

import "../../cloudformation.pkl"

/// A key-value pair to associate with a resource.
open class StoredQueryTag {
    Key: String|Mapping
    Value: String|Mapping
}

/// Resource Type definition for AWS::Config::StoredQuery
open class StoredQuery extends cloudformation.Resource {

    Type = "AWS::Config::StoredQuery"


    /// 
    hidden QueryDescription: (String(matches(Regex(#"[\s\S]*"#)))|Mapping)?

    /// 
    hidden QueryExpression: String(matches(Regex(#"[\s\S]*"#)))|Mapping

    /// 
    hidden QueryName: String(matches(Regex(#"^[a-zA-Z0-9-_]+$"#)))|Mapping

    /// The tags for the stored query.
    hidden Tags: (Listing<StoredQueryTag>)?

    Properties {
        ["QueryDescription"] = if (QueryDescription == null) null else QueryDescription
        ["QueryExpression"] = if (QueryExpression == null) null else QueryExpression
        ["QueryName"] = if (QueryName == null) null else QueryName
        ["Tags"] = if (Tags == null) null else Tags
    }

}
