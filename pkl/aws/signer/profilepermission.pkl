/// AWS::Signer::ProfilePermission
///
/// Generated by rain build --pkl-class AWS::Signer::ProfilePermission
module aws.signer.profilepermission

import "../../cloudformation.pkl"

/// An example resource schema demonstrating some basic constructs and validation rules.
open class ProfilePermission extends cloudformation.Resource {

    Type = "AWS::Signer::ProfilePermission"


    /// 
    hidden Action: String|Mapping

    /// 
    hidden Principal: String|Mapping

    /// 
    hidden ProfileName: String|Mapping

    /// 
    hidden ProfileVersion: (String(matches(Regex(#"^[0-9a-zA-Z]{10}$"#)))|Mapping)?

    /// 
    hidden StatementId: String|Mapping

    Properties {
        ["Action"] = if (Action == null) null else Action
        ["Principal"] = if (Principal == null) null else Principal
        ["ProfileName"] = if (ProfileName == null) null else ProfileName
        ["ProfileVersion"] = if (ProfileVersion == null) null else ProfileVersion
        ["StatementId"] = if (StatementId == null) null else StatementId
    }

}
