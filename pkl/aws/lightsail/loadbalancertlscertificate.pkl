/// AWS::Lightsail::LoadBalancerTlsCertificate
///
/// Generated by rain build --pkl-class AWS::Lightsail::LoadBalancerTlsCertificate
module aws.lightsail.loadbalancertlscertificate

import "../../cloudformation.pkl"

/// Resource Type definition for AWS::Lightsail::LoadBalancerTlsCertificate
open class LoadBalancerTlsCertificate extends cloudformation.Resource {

    Type = "AWS::Lightsail::LoadBalancerTlsCertificate"


    /// When true, the SSL/TLS certificate is attached to the Lightsail load balancer.
    hidden IsAttached: (Boolean|Mapping)?

    /// A Boolean value that indicates whether HTTPS redirection is enabled for the load balancer.
    hidden HttpsRedirectionEnabled: (Boolean|Mapping)?

    /// The name of your load balancer.
    hidden LoadBalancerName: String(matches(Regex(#"\w[\w\-]*\w"#)))|Mapping

    /// The SSL/TLS certificate name.
    hidden CertificateName: String|Mapping

    /// The domain name (e.g., example.com ) for your SSL/TLS certificate.
    hidden CertificateDomainName: String|Mapping

    /// An array of strings listing alternative domains and subdomains for your SSL/TLS certificate.
    hidden CertificateAlternativeNames: (Listing<String|Mapping>)?

    Properties {
        ["IsAttached"] = if (IsAttached == null) null else IsAttached
        ["HttpsRedirectionEnabled"] = if (HttpsRedirectionEnabled == null) null else HttpsRedirectionEnabled
        ["LoadBalancerName"] = if (LoadBalancerName == null) null else LoadBalancerName
        ["CertificateName"] = if (CertificateName == null) null else CertificateName
        ["CertificateDomainName"] = if (CertificateDomainName == null) null else CertificateDomainName
        ["CertificateAlternativeNames"] = if (CertificateAlternativeNames == null) null else CertificateAlternativeNames
    }

}
