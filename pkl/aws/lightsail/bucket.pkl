/// AWS::Lightsail::Bucket
///
/// Generated by rain build --pkl-class AWS::Lightsail::Bucket
module aws.lightsail.bucket

import "../../cloudformation.pkl"

/// A key-value pair to associate with a resource.
open class BucketTag {
    Value: (String|Mapping)?
    Key: String|Mapping
}

/// An object that sets the public accessibility of objects in the specified bucket.
open class BucketAccessRules {
    AllowPublicOverrides: (Boolean|Mapping)?
    GetObject: (String|Mapping)?
}

/// Resource Type definition for AWS::Lightsail::Bucket
open class Bucket extends cloudformation.Resource {

    Type = "AWS::Lightsail::Bucket"


    /// The ID of the bundle to use for the bucket.
    hidden BundleId: BucketBundleId|Mapping

    /// Specifies whether to enable or disable versioning of objects in the bucket.
    hidden ObjectVersioning: (Boolean|Mapping)?

    /// The names of the Lightsail resources for which to set bucket access.
    hidden ResourcesReceivingAccess: (Listing<String|Mapping>)?

    /// The name for the bucket.
    hidden BucketName: String(matches(Regex(#"^[a-z0-9][a-z0-9-]{1,52}[a-z0-9]$"#)))|Mapping

    /// 
    hidden AccessRules: (BucketAccessRules)?

    /// An array of strings to specify the AWS account IDs that can access the bucket.
    hidden ReadOnlyAccessAccounts: (Listing<String|Mapping>)?

    /// An array of key-value pairs to apply to this resource.
    hidden Tags: (Listing<BucketTag>)?

    Properties {
        ["BundleId"] = if (BundleId == null) null else BundleId
        ["ObjectVersioning"] = if (ObjectVersioning == null) null else ObjectVersioning
        ["ResourcesReceivingAccess"] = if (ResourcesReceivingAccess == null) null else ResourcesReceivingAccess
        ["BucketName"] = if (BucketName == null) null else BucketName
        ["AccessRules"] = if (AccessRules == null) null else AccessRules
        ["ReadOnlyAccessAccounts"] = if (ReadOnlyAccessAccounts == null) null else ReadOnlyAccessAccounts
        ["Tags"] = if (Tags == null) null else Tags
    }

}


typealias BucketBundleId = "small_1_0"|"medium_1_0"|"large_1_0"
