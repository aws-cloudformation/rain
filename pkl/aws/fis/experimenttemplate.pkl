/// AWS::FIS::ExperimentTemplate
///
/// Generated by rain build --pkl-class AWS::FIS::ExperimentTemplate
module aws.fis.experimenttemplate

import "../../cloudformation.pkl"

/// 
open class StopConditionValue {
}

/// The targets for the experiment.
open class ExperimentTemplateTargetMap {
}

/// The ID of the action.
open class ActionId {
}

/// 
open class ExperimentTemplateLogConfiguration {
    CloudWatchLogsConfiguration: (Dynamic)?
    S3Configuration: (Dynamic)?
    LogSchemaVersion: Int|Mapping
}

/// A description for the experiment template.
open class ExperimentTemplateDescription {
}

/// 
open class ResourceArn {
}

/// The names of the actions that must be completed before the current action starts.
open class ExperimentTemplateActionItemStartAfterList {
}

/// Describes a filter used for the target resource input in an experiment template.
open class ExperimentTemplateTargetFilter {
    Path: ExperimentTemplateTargetFilterPath
    Values: ExperimentTemplateTargetFilterValues
}

/// 
open class ExperimentTemplateActionItemParameter {
}

/// The attribute path for the filter.
open class ExperimentTemplateTargetFilterPath {
}

/// 
open class ExperimentTemplateActionItemTarget {
}

/// 
open class ExperimentTemplateActionItemStartAfter {
}

/// The actions for the experiment.
open class ExperimentTemplateActionMap {
}

/// Specifies a target for an experiment.
open class ExperimentTemplateTarget {
    SelectionMode: ExperimentTemplateTargetSelectionMode
    ResourceType: ResourceType
    ResourceArns: (ResourceArnList)?
    ResourceTags: (Dynamic)?
    Parameters: (Dynamic)?
    Filters: (ExperimentTemplateTargetFilterList)?
}

/// Specifies an action for the experiment template.
open class ExperimentTemplateAction {
    ActionId: ActionId
    Description: (ExperimentTemplateActionItemDescription)?
    Parameters: (Dynamic)?
    Targets: (Dynamic)?
    StartAfter: (ExperimentTemplateActionItemStartAfterList)?
}

typealias ExperimentTemplateExperimentOptionsAccountTargeting = "multi-account"|"single-account"

typealias ExperimentTemplateExperimentOptionsEmptyTargetResolutionMode = "fail"|"skip"

/// 
open class ExperimentTemplateExperimentOptions {
    AccountTargeting: (ExperimentTemplateExperimentOptionsAccountTargeting|Mapping)?
    EmptyTargetResolutionMode: (ExperimentTemplateExperimentOptionsEmptyTargetResolutionMode|Mapping)?
}

/// The Amazon Resource Name (ARN) of an IAM role that grants the AWS FIS service permission to perform service actions on your behalf.
open class RoleArn {
}

/// The attribute values for the filter.
open class ExperimentTemplateTargetFilterValues {
}

/// 
open class ExperimentTemplateId {
}

/// 
open class StopConditionSource {
}

/// The Amazon Resource Names (ARNs) of the target resources.
open class ResourceArnList {
}

/// Scopes the identified resources to a specific number of the resources at random, or a percentage of the resources.
open class ExperimentTemplateTargetSelectionMode {
}

/// A description for the action.
open class ExperimentTemplateActionItemDescription {
}

/// 
open class ExperimentTemplateStopCondition {
    Source: StopConditionSource
    Value: (StopConditionValue)?
}

/// One or more stop conditions.
open class ExperimentTemplateStopConditionList {
}

/// The AWS resource type. The resource type must be supported for the specified action.
open class ResourceType {
}

/// 
open class ExperimentTemplateTargetFilterValue {
}

/// 
open class ExperimentTemplateTargetFilterList {
}

/// Resource schema for AWS::FIS::ExperimentTemplate
open class ExperimentTemplate extends cloudformation.Resource {

    Type = "AWS::FIS::ExperimentTemplate"


    /// 
    hidden Description: ExperimentTemplateDescription

    /// 
    hidden ExperimentOptions: (ExperimentTemplateExperimentOptions)?

    /// 
    hidden RoleArn: RoleArn

    /// 
    hidden Tags: Dynamic

    /// 
    hidden Targets: ExperimentTemplateTargetMap

    /// 
    hidden Actions: (ExperimentTemplateActionMap)?

    /// 
    hidden StopConditions: ExperimentTemplateStopConditionList

    /// 
    hidden LogConfiguration: (ExperimentTemplateLogConfiguration)?

    Properties {
        ["Description"] = if (Description == null) null else Description
        ["ExperimentOptions"] = if (ExperimentOptions == null) null else ExperimentOptions
        ["RoleArn"] = if (RoleArn == null) null else RoleArn
        ["Tags"] = if (Tags == null) null else Tags
        ["Targets"] = if (Targets == null) null else Targets
        ["Actions"] = if (Actions == null) null else Actions
        ["StopConditions"] = if (StopConditions == null) null else StopConditions
        ["LogConfiguration"] = if (LogConfiguration == null) null else LogConfiguration
    }

}
