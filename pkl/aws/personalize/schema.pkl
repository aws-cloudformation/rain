/// AWS::Personalize::Schema
///
/// Generated by rain build --pkl-class AWS::Personalize::Schema
module aws.personalize.schema

import "../../cloudformation.pkl"

/// Resource schema for AWS::Personalize::Schema.
open class Schema extends cloudformation.Resource {

    Type = "AWS::Personalize::Schema"


    /// The domain of a Domain dataset group.
    hidden Domain: (SchemaDomain|Mapping)?

    /// Name for the schema.
    hidden Name: String(matches(Regex(#"^[a-zA-Z0-9][a-zA-Z0-9\-_]*"#)))|Mapping

    /// A schema in Avro JSON format.
    hidden Schema: String|Mapping

    Properties {
        ["Domain"] = if (Domain == null) null else Domain
        ["Name"] = if (Name == null) null else Name
        ["Schema"] = if (Schema == null) null else Schema
    }

}


typealias SchemaDomain = "ECOMMERCE"|"VIDEO_ON_DEMAND"
