/// AWS::Personalize::Solution
///
/// Generated by rain build --pkl-class AWS::Personalize::Solution
module aws.personalize.solution

import "../../cloudformation.pkl"

/// The ARN of the solution
open class SolutionArn {
}

/// Provides the name and values of a Categorical hyperparameter.
open class CategoricalHyperParameterRange {
    Name: (String|Mapping)?
    Values: (Listing<String|Mapping>)?
}

/// Provides the name and range of a continuous hyperparameter.
open class ContinuousHyperParameterRange {
    MinValue: (Number|Mapping)?
    MaxValue: (Number|Mapping)?
    Name: (String|Mapping)?
}

/// Provides the name and range of an integer-valued hyperparameter.
open class IntegerHyperParameterRange {
    MaxValue: (Int|Mapping)?
    Name: (String|Mapping)?
    MinValue: (Int|Mapping)?
}

/// The configuration to use with the solution. When performAutoML is set to true, Amazon Personalize only evaluates the autoMLConfig section of the solution configuration.
open class SolutionConfig {
    AutoMLConfig: (Dynamic)?
    EventValueThreshold: (String|Mapping)?
    FeatureTransformationParameters: (Dynamic)?
    HpoConfig: (Dynamic)?
    AlgorithmHyperParameters: (Dynamic)?
}

/// Resource schema for AWS::Personalize::Solution.
open class Solution extends cloudformation.Resource {

    Type = "AWS::Personalize::Solution"


    /// When your have multiple event types (using an EVENT_TYPE schema field), this parameter specifies which event type (for example, 'click' or 'like') is used for training the model. If you do not provide an eventType, Amazon Personalize will use all interactions for training with equal weight regardless of type.
    hidden EventType: (String|Mapping)?

    /// The ARN of the dataset group that provides the training data.
    hidden DatasetGroupArn: String(matches(Regex(#"arn:([a-z\d-]+):personalize:.*:.*:.+"#)))|Mapping

    /// Whether to perform automated machine learning (AutoML). The default is false. For this case, you must specify recipeArn.
    hidden PerformAutoML: (Boolean|Mapping)?

    /// Whether to perform hyperparameter optimization (HPO) on the specified or selected recipe. The default is false. When performing AutoML, this parameter is always true and you should not set it to false.
    hidden PerformHPO: (Boolean|Mapping)?

    /// The ARN of the recipe to use for model training. Only specified when performAutoML is false.
    hidden RecipeArn: (String(matches(Regex(#"arn:([a-z\d-]+):personalize:.*:.*:.+"#)))|Mapping)?

    /// 
    hidden SolutionConfig: (SolutionConfig)?

    /// The name for the solution
    hidden Name: String(matches(Regex(#"^[a-zA-Z0-9][a-zA-Z0-9\-_]*"#)))|Mapping

    Properties {
        ["EventType"] = if (EventType == null) null else EventType
        ["DatasetGroupArn"] = if (DatasetGroupArn == null) null else DatasetGroupArn
        ["PerformAutoML"] = if (PerformAutoML == null) null else PerformAutoML
        ["PerformHPO"] = if (PerformHPO == null) null else PerformHPO
        ["RecipeArn"] = if (RecipeArn == null) null else RecipeArn
        ["SolutionConfig"] = if (SolutionConfig == null) null else SolutionConfig
        ["Name"] = if (Name == null) null else Name
    }

}
