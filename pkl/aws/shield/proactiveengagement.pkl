/// AWS::Shield::ProactiveEngagement
///
/// Generated by rain build --pkl-class AWS::Shield::ProactiveEngagement
module aws.shield.proactiveengagement

import "../../cloudformation.pkl"

/// An emergency contact is used by Shield Response Team (SRT) to contact you for escalations to the SRT and to initiate proactive customer support. An emergency contact requires an email address.
open class ProactiveEngagementEmergencyContact {
    ContactNotes: (String(matches(Regex(#"^[\w\s\.\-,:/()+@]*$"#)))|Mapping)?
    EmailAddress: String(matches(Regex(#"^\S+@\S+\.\S+$"#)))|Mapping
    PhoneNumber: (String(matches(Regex(#"^\+[1-9]\d{1,14}$"#)))|Mapping)?
}

/// Authorizes the Shield Response Team (SRT) to use email and phone to notify contacts about escalations to the SRT and to initiate proactive customer support.
open class ProactiveEngagement extends cloudformation.Resource {

    Type = "AWS::Shield::ProactiveEngagement"


    /// A list of email addresses and phone numbers that the Shield Response Team (SRT) can use to contact you for escalations to the SRT and to initiate proactive customer support.
    ///
    /// To enable proactive engagement, the contact list must include at least one phone number.
    hidden EmergencyContactList: Listing<ProactiveEngagementEmergencyContact>

    /// If `ENABLED`, the Shield Response Team (SRT) will use email and phone to notify contacts about escalations to the SRT and to initiate proactive customer support.
    ///
    /// If `DISABLED`, the SRT will not proactively notify contacts about escalations or to initiate proactive customer support.
    hidden ProactiveEngagementStatus: ProactiveEngagementProactiveEngagementStatus|Mapping

    Properties {
        ["EmergencyContactList"] = if (EmergencyContactList == null) null else EmergencyContactList
        ["ProactiveEngagementStatus"] = if (ProactiveEngagementStatus == null) null else ProactiveEngagementStatus
    }

}


typealias ProactiveEngagementProactiveEngagementStatus = "ENABLED"|"DISABLED"
