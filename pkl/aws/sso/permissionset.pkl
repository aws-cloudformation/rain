/// AWS::SSO::PermissionSet
///
/// Generated by rain build --pkl-class AWS::SSO::PermissionSet
module aws.sso.permissionset

import "../../cloudformation.pkl"

/// The metadata that you apply to the permission set to help you categorize and organize them.
open class PermissionSetTag {
    Key: String(matches(Regex(#"[\w+=,.@-]+"#)))|Mapping
    Value: String(matches(Regex(#"[\w+=,.@-]+"#)))|Mapping
}

typealias PermissionSetManagedPolicyArn = String|Mapping


/// 
open class PermissionSetCustomerManagedPolicyReference {
    Name: String(matches(Regex(#"[\w+=,.@-]+"#)))|Mapping
    Path: (String(matches(Regex(#"((/[A-Za-z0-9\.,\+@=_-]+)*)/"#)))|Mapping)?
}

/// 
open class PermissionSetPermissionsBoundary {
    ManagedPolicyArn: (PermissionSetManagedPolicyArn)?
    CustomerManagedPolicyReference: (PermissionSetCustomerManagedPolicyReference)?
}

/// Resource Type definition for SSO PermissionSet
open class PermissionSet extends cloudformation.Resource {

    Type = "AWS::SSO::PermissionSet"


    /// The relay state URL that redirect links to any service in the AWS Management Console.
    hidden RelayStateType: (String(matches(Regex(#"[a-zA-Z0-9&amp;$@#\/%?=~\-_'&quot;|!:,.;*+\[\]\ \(\)\{\}]+"#)))|Mapping)?

    /// 
    hidden ManagedPolicies: (Listing<PermissionSetManagedPolicyArn>)?

    /// 
    hidden CustomerManagedPolicyReferences: (Listing<PermissionSetCustomerManagedPolicyReference>)?

    /// The name you want to assign to this permission set.
    hidden Name: String(matches(Regex(#"[\w+=,.@-]+"#)))|Mapping

    /// The sso instance arn that the permission set is owned.
    hidden InstanceArn: String(matches(Regex(#"arn:(aws|aws-us-gov|aws-cn|aws-iso|aws-iso-b):sso:::instance/(sso)?ins-[a-zA-Z0-9-.]{16}"#)))|Mapping

    /// 
    hidden Tags: (Listing<PermissionSetTag>)?

    /// 
    hidden PermissionsBoundary: (PermissionSetPermissionsBoundary)?

    /// The permission set description.
    hidden Description: (String(matches(Regex(#"[\u0009\u000A\u000D\u0020-\u007E\u00A1-\u00FF]*"#)))|Mapping)?

    /// The length of time that a user can be signed in to an AWS account.
    hidden SessionDuration: (String(matches(Regex(#"^(-?)P(?=\d|T\d)(?:(\d+)Y)?(?:(\d+)M)?(?:(\d+)([DW]))?(?:T(?:(\d+)H)?(?:(\d+)M)?(?:(\d+(?:\.\d+)?)S)?)?$"#)))|Mapping)?

    /// The inline policy to put in permission set.
    hidden InlinePolicy: (Dynamic)?

    Properties {
        ["RelayStateType"] = if (RelayStateType == null) null else RelayStateType
        ["ManagedPolicies"] = if (ManagedPolicies == null) null else ManagedPolicies
        ["CustomerManagedPolicyReferences"] = if (CustomerManagedPolicyReferences == null) null else CustomerManagedPolicyReferences
        ["Name"] = if (Name == null) null else Name
        ["InstanceArn"] = if (InstanceArn == null) null else InstanceArn
        ["Tags"] = if (Tags == null) null else Tags
        ["PermissionsBoundary"] = if (PermissionsBoundary == null) null else PermissionsBoundary
        ["Description"] = if (Description == null) null else Description
        ["SessionDuration"] = if (SessionDuration == null) null else SessionDuration
        ["InlinePolicy"] = if (InlinePolicy == null) null else InlinePolicy
    }

}
