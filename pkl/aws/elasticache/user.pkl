/// AWS::ElastiCache::User
///
/// Generated by rain build --pkl-class AWS::ElastiCache::User
module aws.elasticache.user

import "../../cloudformation.pkl"

/// A key-value pair to associate with a resource.
open class Tag {
    Value: (String(matches(Regex(#"^[a-zA-Z0-9 _\.\/=+:\-@]*$"#)))|Mapping)?
    Key: String(matches(Regex(#"^(?!aws:)[a-zA-Z0-9 _\.\/=+:\-@]*$"#)))|Mapping
}

/// Resource Type definition for AWS::ElastiCache::User
open class User extends cloudformation.Resource {

    Type = "AWS::ElastiCache::User"


    /// The ID of the user.
    hidden UserId: String(matches(Regex(#"[a-z][a-z0-9\\-]*"#)))|Mapping

    /// Passwords used for this user account. You can create up to two passwords for each user.
    hidden Passwords: (Listing<String|Mapping>)?

    /// 
    hidden AuthenticationMode: (Dynamic)?

    /// The username of the user.
    hidden UserName: String|Mapping

    /// Must be redis.
    hidden Engine: UserEngine|Mapping

    /// Access permissions string used for this user account.
    hidden AccessString: (String|Mapping)?

    /// Indicates a password is not required for this user account.
    hidden NoPasswordRequired: (Boolean|Mapping)?

    /// An array of key-value pairs to apply to this user.
    hidden Tags: (Listing<Tag>)?

    Properties {
        ["UserId"] = if (UserId == null) null else UserId
        ["Passwords"] = if (Passwords == null) null else Passwords
        ["AuthenticationMode"] = if (AuthenticationMode == null) null else AuthenticationMode
        ["UserName"] = if (UserName == null) null else UserName
        ["Engine"] = if (Engine == null) null else Engine
        ["AccessString"] = if (AccessString == null) null else AccessString
        ["NoPasswordRequired"] = if (NoPasswordRequired == null) null else NoPasswordRequired
        ["Tags"] = if (Tags == null) null else Tags
    }

}


typealias UserEngine = "redis"
