/// AWS::ElastiCache::UserGroup
///
/// Generated by rain build --pkl-class AWS::ElastiCache::UserGroup
module aws.elasticache.usergroup

import "../../cloudformation.pkl"

/// A key-value pair to associate with a resource.
open class UserGroupTag {
    Key: String(matches(Regex(#"^(?!aws:)[a-zA-Z0-9 _\.\/=+:\-@]*$"#)))|Mapping
    Value: (String(matches(Regex(#"^[a-zA-Z0-9 _\.\/=+:\-@]*$"#)))|Mapping)?
}

/// Resource Type definition for AWS::ElastiCache::UserGroup
open class UserGroup extends cloudformation.Resource {

    Type = "AWS::ElastiCache::UserGroup"


    /// Must be redis.
    hidden Engine: UserGroupEngine|Mapping

    /// An array of key-value pairs to apply to this user.
    hidden Tags: (Listing<UserGroupTag>)?

    /// The ID of the user group.
    hidden UserGroupId: String(matches(Regex(#"[a-z][a-z0-9\\-]*"#)))|Mapping

    /// List of users associated to this user group.
    hidden UserIds: Listing<String|Mapping>

    Properties {
        ["Engine"] = if (Engine == null) null else Engine
        ["Tags"] = if (Tags == null) null else Tags
        ["UserGroupId"] = if (UserGroupId == null) null else UserGroupId
        ["UserIds"] = if (UserIds == null) null else UserIds
    }

}


typealias UserGroupEngine = "redis"
