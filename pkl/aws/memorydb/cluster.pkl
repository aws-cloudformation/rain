/// AWS::MemoryDB::Cluster
///
/// Generated by rain build --pkl-class AWS::MemoryDB::Cluster
module aws.memorydb.cluster

import "../../cloudformation.pkl"

/// 
open class Endpoint {
    Address: (String|Mapping)?
    Port: (Int|Mapping)?
}

/// A key-value pair to associate with a resource.
open class Tag {
    Key: String(matches(Regex(#"^(?!aws:)(?!memorydb:)[a-zA-Z0-9 _\.\/=+:\-@]{1,128}$"#)))|Mapping
    Value: String(matches(Regex(#"^(?!aws:)(?!memorydb:)[a-zA-Z0-9 _\.\/=+:\-@]{1,256}$"#)))|Mapping
}

/// 
open class DataTieringStatus {
}

/// The AWS::MemoryDB::Cluster resource creates an Amazon MemoryDB Cluster.
open class Cluster extends cloudformation.Resource {

    Type = "AWS::MemoryDB::Cluster"


    /// The name of the cluster. This value must be unique as it also serves as the cluster identifier.
    hidden ClusterName: String(matches(Regex(#"[a-z][a-z0-9\-]*"#)))|Mapping

    /// An optional description of the cluster.
    hidden Description: (String|Mapping)?

    /// A flag that enables in-transit encryption when set to true.
    ///    /// 
    /// You cannot modify the value of TransitEncryptionEnabled after the cluster is created. To enable in-transit encryption on a cluster you must set TransitEncryptionEnabled to true when you create a cluster.
    hidden TLSEnabled: (Boolean|Mapping)?

    /// The cluster endpoint.
    hidden ClusterEndpoint: (Endpoint)?

    /// The name of the parameter group associated with the cluster.
    hidden ParameterGroupName: (String|Mapping)?

    /// The number of days for which MemoryDB retains automatic snapshots before deleting them. For example, if you set SnapshotRetentionLimit to 5, a snapshot that was taken today is retained for 5 days before being deleted.
    hidden SnapshotRetentionLimit: (Int|Mapping)?

    /// The daily time range (in UTC) during which MemoryDB begins taking a daily snapshot of your cluster.
    hidden SnapshotWindow: (String|Mapping)?

    /// The status of the Amazon SNS notification topic. Notifications are sent only if the status is enabled.
    hidden SnsTopicStatus: (String|Mapping)?

    /// A flag that enables automatic minor version upgrade when set to true.
    ///    /// 
    /// You cannot modify the value of AutoMinorVersionUpgrade after the cluster is created. To enable AutoMinorVersionUpgrade on a cluster you must set AutoMinorVersionUpgrade to true when you create a cluster.
    hidden AutoMinorVersionUpgrade: (Boolean|Mapping)?

    /// The name of the subnet group to be used for the cluster.
    hidden SubnetGroupName: (String|Mapping)?

    /// The number of shards the cluster will contain.
    hidden NumShards: (Int|Mapping)?

    /// The port number on which each member of the cluster accepts connections.
    hidden Port: (Int|Mapping)?

    /// Enables data tiering. Data tiering is only supported for clusters using the r6gd node type. This parameter must be set when using r6gd nodes.
    hidden DataTiering: (Dynamic)?

    /// The Amazon Resource Name (ARN) of the Amazon Simple Notification Service (SNS) topic to which notifications are sent.
    hidden SnsTopicArn: (String|Mapping)?

    /// The Redis engine version used by the cluster.
    hidden EngineVersion: (String|Mapping)?

    /// The name of a snapshot from which to restore data into the new cluster. The snapshot status changes to restoring while the new cluster is being created.
    hidden SnapshotName: (String|Mapping)?

    /// The number of replicas to apply to each shard. The limit is 5.
    hidden NumReplicasPerShard: (Int|Mapping)?

    /// The ID of the KMS key used to encrypt the cluster.
    hidden KmsKeyId: (String|Mapping)?

    /// An array of key-value pairs to apply to this cluster.
    hidden Tags: (Listing<Tag>)?

    /// The compute and memory capacity of the nodes in the cluster.
    hidden NodeType: String|Mapping

    /// One or more Amazon VPC security groups associated with this cluster.
    hidden SecurityGroupIds: (Listing<String|Mapping>)?

    /// Specifies the weekly time range during which maintenance on the cluster is performed. It is specified as a range in the format ddd:hh24:mi-ddd:hh24:mi (24H Clock UTC). The minimum maintenance window is a 60 minute period.
    hidden MaintenanceWindow: (String|Mapping)?

    /// The name of the Access Control List to associate with the cluster.
    hidden ACLName: String(matches(Regex(#"[a-zA-Z][a-zA-Z0-9\-]*"#)))|Mapping

    /// A list of Amazon Resource Names (ARN) that uniquely identify the RDB snapshot files stored in Amazon S3. The snapshot files are used to populate the new cluster. The Amazon S3 object name in the ARN cannot contain any commas.
    hidden SnapshotArns: (Listing<String|Mapping>)?

    /// The user-supplied name of a final cluster snapshot. This is the unique name that identifies the snapshot. MemoryDB creates the snapshot, and then deletes the cluster immediately afterward.
    hidden FinalSnapshotName: (String|Mapping)?

    Properties {
        ["ClusterName"] = if (ClusterName == null) null else ClusterName
        ["Description"] = if (Description == null) null else Description
        ["TLSEnabled"] = if (TLSEnabled == null) null else TLSEnabled
        ["ClusterEndpoint"] = if (ClusterEndpoint == null) null else ClusterEndpoint
        ["ParameterGroupName"] = if (ParameterGroupName == null) null else ParameterGroupName
        ["SnapshotRetentionLimit"] = if (SnapshotRetentionLimit == null) null else SnapshotRetentionLimit
        ["SnapshotWindow"] = if (SnapshotWindow == null) null else SnapshotWindow
        ["SnsTopicStatus"] = if (SnsTopicStatus == null) null else SnsTopicStatus
        ["AutoMinorVersionUpgrade"] = if (AutoMinorVersionUpgrade == null) null else AutoMinorVersionUpgrade
        ["SubnetGroupName"] = if (SubnetGroupName == null) null else SubnetGroupName
        ["NumShards"] = if (NumShards == null) null else NumShards
        ["Port"] = if (Port == null) null else Port
        ["DataTiering"] = if (DataTiering == null) null else DataTiering
        ["SnsTopicArn"] = if (SnsTopicArn == null) null else SnsTopicArn
        ["EngineVersion"] = if (EngineVersion == null) null else EngineVersion
        ["SnapshotName"] = if (SnapshotName == null) null else SnapshotName
        ["NumReplicasPerShard"] = if (NumReplicasPerShard == null) null else NumReplicasPerShard
        ["KmsKeyId"] = if (KmsKeyId == null) null else KmsKeyId
        ["Tags"] = if (Tags == null) null else Tags
        ["NodeType"] = if (NodeType == null) null else NodeType
        ["SecurityGroupIds"] = if (SecurityGroupIds == null) null else SecurityGroupIds
        ["MaintenanceWindow"] = if (MaintenanceWindow == null) null else MaintenanceWindow
        ["ACLName"] = if (ACLName == null) null else ACLName
        ["SnapshotArns"] = if (SnapshotArns == null) null else SnapshotArns
        ["FinalSnapshotName"] = if (FinalSnapshotName == null) null else FinalSnapshotName
    }

}
