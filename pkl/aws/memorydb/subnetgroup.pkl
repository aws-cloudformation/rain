/// AWS::MemoryDB::SubnetGroup
///
/// Generated by rain build --pkl-class AWS::MemoryDB::SubnetGroup
module aws.memorydb.subnetgroup

import "../../cloudformation.pkl"

/// A key-value pair to associate with a resource.
open class Tag {
    Value: String(matches(Regex(#"^(?!aws:)(?!memorydb:)[a-zA-Z0-9 _\.\/=+:\-@]{1,256}$"#)))|Mapping
    Key: String(matches(Regex(#"^(?!aws:)(?!memorydb:)[a-zA-Z0-9 _\.\/=+:\-@]{1,128}$"#)))|Mapping
}

/// The AWS::MemoryDB::SubnetGroup resource creates an Amazon MemoryDB Subnet Group.
open class SubnetGroup extends cloudformation.Resource {

    Type = "AWS::MemoryDB::SubnetGroup"


    /// A list of VPC subnet IDs for the subnet group.
    hidden SubnetIds: Listing<String|Mapping>

    /// An array of key-value pairs to apply to this subnet group.
    hidden Tags: (Listing<Tag>)?

    /// The name of the subnet group. This value must be unique as it also serves as the subnet group identifier.
    hidden SubnetGroupName: String(matches(Regex(#"[a-z][a-z0-9\-]*"#)))|Mapping

    /// An optional description of the subnet group.
    hidden Description: (String|Mapping)?

    Properties {
        ["SubnetIds"] = if (SubnetIds == null) null else SubnetIds
        ["Tags"] = if (Tags == null) null else Tags
        ["SubnetGroupName"] = if (SubnetGroupName == null) null else SubnetGroupName
        ["Description"] = if (Description == null) null else Description
    }

}
