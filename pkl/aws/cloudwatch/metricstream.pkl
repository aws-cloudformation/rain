/// AWS::CloudWatch::MetricStream
///
/// Generated by rain build --pkl-class AWS::CloudWatch::MetricStream
module aws.cloudwatch.metricstream

import "../../cloudformation.pkl"

/// This structure defines the metrics that will be streamed.
open class MetricStreamMetricStreamFilter {
    MetricNames: (Listing<String|Mapping>)?
    Namespace: String|Mapping
}

/// This structure specifies a list of additional statistics to stream, and the metrics to stream those additional statistics for. All metrics that match the combination of metric name and namespace will be streamed with the extended statistics, no matter their dimensions.
open class MetricStreamMetricStreamStatisticsConfiguration {
    AdditionalStatistics: Listing<String|Mapping>
    IncludeMetrics: Listing<MetricStreamMetricStreamStatisticsMetric>
}

/// A structure that specifies the metric name and namespace for one metric that is going to have additional statistics included in the stream.
open class MetricStreamMetricStreamStatisticsMetric {
    MetricName: String|Mapping
    Namespace: String|Mapping
}

/// Metadata that you can assign to a Metric Stream, consisting of a key-value pair.
open class MetricStreamTag {
    Key: String|Mapping
    Value: String|Mapping
}

/// Resource Type definition for Metric Stream
open class MetricStream extends cloudformation.Resource {

    Type = "AWS::CloudWatch::MetricStream"


    /// Define which metrics will be not streamed. Metrics matched by multiple instances of MetricStreamFilter are joined with an OR operation by default. If both IncludeFilters and ExcludeFilters are omitted, all metrics in the account will be streamed. IncludeFilters and ExcludeFilters are mutually exclusive. Default to null.
    hidden ExcludeFilters: (Listing<MetricStreamMetricStreamFilter>)?

    /// The ARN of the Kinesis Firehose where to stream the data.
    hidden FirehoseArn: String|Mapping

    /// Define which metrics will be streamed. Metrics matched by multiple instances of MetricStreamFilter are joined with an OR operation by default. If both IncludeFilters and ExcludeFilters are omitted, all metrics in the account will be streamed. IncludeFilters and ExcludeFilters are mutually exclusive. Default to null.
    hidden IncludeFilters: (Listing<MetricStreamMetricStreamFilter>)?

    /// If you are creating a metric stream in a monitoring account, specify true to include metrics from source accounts that are linked to this monitoring account, in the metric stream. The default is false.
    hidden IncludeLinkedAccountsMetrics: (Boolean|Mapping)?

    /// Name of the metric stream.
    hidden Name: (String|Mapping)?

    /// The output format of the data streamed to the Kinesis Firehose.
    hidden OutputFormat: String|Mapping

    /// The ARN of the role that provides access to the Kinesis Firehose.
    hidden RoleArn: String|Mapping

    /// By default, a metric stream always sends the MAX, MIN, SUM, and SAMPLECOUNT statistics for each metric that is streamed. You can use this parameter to have the metric stream also send additional statistics in the stream. This array can have up to 100 members.
    hidden StatisticsConfigurations: (Listing<MetricStreamMetricStreamStatisticsConfiguration>)?

    /// A set of tags to assign to the delivery stream.
    hidden Tags: (Listing<MetricStreamTag>)?

    Properties {
        ["ExcludeFilters"] = if (ExcludeFilters == null) null else ExcludeFilters
        ["FirehoseArn"] = if (FirehoseArn == null) null else FirehoseArn
        ["IncludeFilters"] = if (IncludeFilters == null) null else IncludeFilters
        ["IncludeLinkedAccountsMetrics"] = if (IncludeLinkedAccountsMetrics == null) null else IncludeLinkedAccountsMetrics
        ["Name"] = if (Name == null) null else Name
        ["OutputFormat"] = if (OutputFormat == null) null else OutputFormat
        ["RoleArn"] = if (RoleArn == null) null else RoleArn
        ["StatisticsConfigurations"] = if (StatisticsConfigurations == null) null else StatisticsConfigurations
        ["Tags"] = if (Tags == null) null else Tags
    }

}
