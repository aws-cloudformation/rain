/// AWS::IoTSiteWise::Dashboard
///
/// Generated by rain build --pkl-class AWS::IoTSiteWise::Dashboard
module aws.iotsitewise.dashboard

import "../../cloudformation.pkl"

/// To add or update tag, provide both key and value. To delete tag, provide only tag key to be deleted
open class DashboardTag {
    Key: String|Mapping
    Value: String|Mapping
}

/// Resource schema for AWS::IoTSiteWise::Dashboard
open class Dashboard extends cloudformation.Resource {

    Type = "AWS::IoTSiteWise::Dashboard"


    /// The dashboard definition specified in a JSON literal.
    hidden DashboardDefinition: String|Mapping

    /// A description for the dashboard.
    hidden DashboardDescription: String|Mapping

    /// A friendly name for the dashboard.
    hidden DashboardName: String|Mapping

    /// The ID of the project in which to create the dashboard.
    hidden ProjectId: (String|Mapping)?

    /// A list of key-value pairs that contain metadata for the dashboard.
    hidden Tags: (Listing<DashboardTag>)?

    Properties {
        ["DashboardDefinition"] = if (DashboardDefinition == null) null else DashboardDefinition
        ["DashboardDescription"] = if (DashboardDescription == null) null else DashboardDescription
        ["DashboardName"] = if (DashboardName == null) null else DashboardName
        ["ProjectId"] = if (ProjectId == null) null else ProjectId
        ["Tags"] = if (Tags == null) null else Tags
    }

}
