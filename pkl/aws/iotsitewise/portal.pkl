/// AWS::IoTSiteWise::Portal
///
/// Generated by rain build --pkl-class AWS::IoTSiteWise::Portal
module aws.iotsitewise.portal

import "../../cloudformation.pkl"

/// To add or update tag, provide both key and value. To delete tag, provide only tag key to be deleted.
open class PortalTag {
    Key: String|Mapping
    Value: String|Mapping
}

/// Resource schema for AWS::IoTSiteWise::Portal
open class Portal extends cloudformation.Resource {

    Type = "AWS::IoTSiteWise::Portal"


    /// The AWS administrator's contact email address.
    hidden PortalContactEmail: String|Mapping

    /// A description for the portal.
    hidden PortalDescription: (String|Mapping)?

    /// The email address that sends alarm notifications.
    hidden NotificationSenderEmail: (String|Mapping)?

    /// Contains the configuration information of an alarm created in an AWS IoT SiteWise Monitor portal. You can use the alarm to monitor an asset property and get notified when the asset property value is outside a specified range.
    hidden Alarms: (Dynamic)?

    /// The service to use to authenticate users to the portal. Choose from SSO or IAM. You can't change this value after you create a portal.
    hidden PortalAuthMode: (String|Mapping)?

    /// A friendly name for the portal.
    hidden PortalName: String|Mapping

    /// The ARN of a service role that allows the portal's users to access your AWS IoT SiteWise resources on your behalf.
    hidden RoleArn: String|Mapping

    /// A list of key-value pairs that contain metadata for the portal.
    hidden Tags: (Listing<PortalTag>)?

    Properties {
        ["PortalContactEmail"] = if (PortalContactEmail == null) null else PortalContactEmail
        ["PortalDescription"] = if (PortalDescription == null) null else PortalDescription
        ["NotificationSenderEmail"] = if (NotificationSenderEmail == null) null else NotificationSenderEmail
        ["Alarms"] = if (Alarms == null) null else Alarms
        ["PortalAuthMode"] = if (PortalAuthMode == null) null else PortalAuthMode
        ["PortalName"] = if (PortalName == null) null else PortalName
        ["RoleArn"] = if (RoleArn == null) null else RoleArn
        ["Tags"] = if (Tags == null) null else Tags
    }

}
