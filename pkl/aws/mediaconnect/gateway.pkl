/// AWS::MediaConnect::Gateway
///
/// Generated by rain build --pkl-class AWS::MediaConnect::Gateway
module aws.mediaconnect.gateway

import "../../cloudformation.pkl"

/// The network settings for a gateway.
open class GatewayNetwork {
    Name: String|Mapping
    CidrBlock: String|Mapping
}

/// Resource schema for AWS::MediaConnect::Gateway
open class Gateway extends cloudformation.Resource {

    Type = "AWS::MediaConnect::Gateway"


    /// The name of the gateway. This name can not be modified after the gateway is created.
    hidden Name: String|Mapping

    /// The range of IP addresses that contribute content or initiate output requests for flows communicating with this gateway. These IP addresses should be in the form of a Classless Inter-Domain Routing (CIDR) block; for example, 10.0.0.0/16.
    hidden EgressCidrBlocks: Listing<String|Mapping>

    /// The list of networks in the gateway.
    hidden Networks: Listing<GatewayNetwork>

    Properties {
        ["Name"] = if (Name == null) null else Name
        ["EgressCidrBlocks"] = if (EgressCidrBlocks == null) null else EgressCidrBlocks
        ["Networks"] = if (Networks == null) null else Networks
    }

}
