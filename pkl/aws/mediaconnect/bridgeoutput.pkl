/// AWS::MediaConnect::BridgeOutput
///
/// Generated by rain build --pkl-class AWS::MediaConnect::BridgeOutput
module aws.mediaconnect.bridgeoutput

import "../../cloudformation.pkl"

typealias BridgeNetworkOutputProtocol = "rtp-fec"|"rtp"|"udp"

/// The output of the bridge. A network output is delivered to your premises.
open class BridgeNetworkOutput {
    Protocol: BridgeNetworkOutputProtocol|Mapping
    IpAddress: String|Mapping
    Port: Int|Mapping
    NetworkName: String|Mapping
    Ttl: Int|Mapping
}

/// Resource schema for AWS::MediaConnect::BridgeOutput
open class BridgeOutput extends cloudformation.Resource {

    Type = "AWS::MediaConnect::BridgeOutput"


    /// The Amazon Resource Number (ARN) of the bridge.
    hidden BridgeArn: String|Mapping

    /// The output of the bridge.
    hidden NetworkOutput: BridgeNetworkOutput

    /// The network output name.
    hidden Name: String|Mapping

    Properties {
        ["BridgeArn"] = if (BridgeArn == null) null else BridgeArn
        ["NetworkOutput"] = if (NetworkOutput == null) null else NetworkOutput
        ["Name"] = if (Name == null) null else Name
    }

}
