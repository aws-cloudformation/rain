/// AWS::FraudDetector::Outcome
///
/// Generated by rain build --pkl-class AWS::FraudDetector::Outcome
module aws.frauddetector.outcome

import "../../cloudformation.pkl"

/// 
open class OutcomeTag {
    Key: String|Mapping
    Value: String|Mapping
}

/// An outcome for rule evaluation.
open class Outcome extends cloudformation.Resource {

    Type = "AWS::FraudDetector::Outcome"


    /// The outcome description.
    hidden Description: (String|Mapping)?

    /// The name of the outcome.
    hidden Name: String(matches(Regex(#"^[0-9a-z_-]+$"#)))|Mapping

    /// Tags associated with this outcome.
    hidden Tags: (Listing<OutcomeTag>)?

    Properties {
        ["Description"] = if (Description == null) null else Description
        ["Name"] = if (Name == null) null else Name
        ["Tags"] = if (Tags == null) null else Tags
    }

}
