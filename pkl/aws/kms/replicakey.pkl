/// AWS::KMS::ReplicaKey
///
/// Generated by rain build --pkl-class AWS::KMS::ReplicaKey
module aws.kms.replicakey

import "../../cloudformation.pkl"

/// A key-value pair to associate with a resource.
open class ReplicaKeyTag {
    Key: String|Mapping
    Value: String|Mapping
}

/// The AWS::KMS::ReplicaKey resource specifies a multi-region replica AWS KMS key in AWS Key Management Service (AWS KMS).
open class ReplicaKey extends cloudformation.Resource {

    Type = "AWS::KMS::ReplicaKey"


    /// A description of the AWS KMS key. Use a description that helps you to distinguish this AWS KMS key from others in the account, such as its intended use.
    hidden Description: (String|Mapping)?

    /// Specifies whether the AWS KMS key is enabled. Disabled AWS KMS keys cannot be used in cryptographic operations.
    hidden Enabled: (Boolean|Mapping)?

    /// The key policy that authorizes use of the AWS KMS key. The key policy must observe the following rules.
    hidden KeyPolicy: Dynamic

    /// Specifies the number of days in the waiting period before AWS KMS deletes an AWS KMS key that has been removed from a CloudFormation stack. Enter a value between 7 and 30 days. The default value is 30 days.
    hidden PendingWindowInDays: (Int|Mapping)?

    /// Identifies the primary AWS KMS key to create a replica of. Specify the Amazon Resource Name (ARN) of the AWS KMS key. You cannot specify an alias or key ID. For help finding the ARN, see Finding the Key ID and ARN in the AWS Key Management Service Developer Guide.
    hidden PrimaryKeyArn: String|Mapping

    /// An array of key-value pairs to apply to this resource.
    hidden Tags: (Listing<ReplicaKeyTag>)?

    Properties {
        ["Description"] = if (Description == null) null else Description
        ["Enabled"] = if (Enabled == null) null else Enabled
        ["KeyPolicy"] = if (KeyPolicy == null) null else KeyPolicy
        ["PendingWindowInDays"] = if (PendingWindowInDays == null) null else PendingWindowInDays
        ["PrimaryKeyArn"] = if (PrimaryKeyArn == null) null else PrimaryKeyArn
        ["Tags"] = if (Tags == null) null else Tags
    }

}
