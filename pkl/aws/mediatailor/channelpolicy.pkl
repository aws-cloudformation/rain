/// AWS::MediaTailor::ChannelPolicy
///
/// Generated by rain build --pkl-class AWS::MediaTailor::ChannelPolicy
module aws.mediatailor.channelpolicy

import "../../cloudformation.pkl"

/// Definition of AWS::MediaTailor::ChannelPolicy Resource Type
open class ChannelPolicy extends cloudformation.Resource {

    Type = "AWS::MediaTailor::ChannelPolicy"


    /// 
    hidden ChannelName: String|Mapping

    /// <p>The IAM policy for the channel. IAM policies are used to control access to your channel.</p>
    hidden Policy: Dynamic

    Properties {
        ["ChannelName"] = if (ChannelName == null) null else ChannelName
        ["Policy"] = if (Policy == null) null else Policy
    }

}
