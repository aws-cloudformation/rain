/// AWS::EMRContainers::VirtualCluster
///
/// Generated by rain build --pkl-class AWS::EMRContainers::VirtualCluster
module aws.emrcontainers.virtualcluster

import "../../cloudformation.pkl"

/// 
open class VirtualClusterContainerInfo {
    EksInfo: VirtualClusterEksInfo
}

/// 
open class VirtualClusterContainerProvider {
    Id: String(matches(Regex(#"^[0-9A-Za-z][A-Za-z0-9\-_]*"#)))|Mapping
    Info: VirtualClusterContainerInfo
    Type: String|Mapping
}

/// 
open class VirtualClusterEksInfo {
    Namespace: String(matches(Regex(#"[a-z0-9]([-a-z0-9]*[a-z0-9])?"#)))|Mapping
}

/// An arbitrary set of tags (key-value pairs) for this virtual cluster.
open class VirtualClusterTag {
    Key: String|Mapping
    Value: String|Mapping
}

/// Resource Schema of AWS::EMRContainers::VirtualCluster Type
open class VirtualCluster extends cloudformation.Resource {

    Type = "AWS::EMRContainers::VirtualCluster"


    /// Container provider of the virtual cluster.
    hidden ContainerProvider: VirtualClusterContainerProvider

    /// Name of the virtual cluster.
    hidden Name: String(matches(Regex(#"[\.\-_/#A-Za-z0-9]+"#)))|Mapping

    /// An array of key-value pairs to apply to this virtual cluster.
    hidden Tags: (Listing<VirtualClusterTag>)?

    Properties {
        ["ContainerProvider"] = if (ContainerProvider == null) null else ContainerProvider
        ["Name"] = if (Name == null) null else Name
        ["Tags"] = if (Tags == null) null else Tags
    }

}
