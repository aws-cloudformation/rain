/// AWS::IoTTwinMaker::SyncJob
///
/// Generated by rain build --pkl-class AWS::IoTTwinMaker::SyncJob
module aws.iottwinmaker.syncjob

import "../../cloudformation.pkl"

typealias SyncJobDateTimeFormat = String|Mapping


/// Resource schema for AWS::IoTTwinMaker::SyncJob
open class SyncJob extends cloudformation.Resource {

    Type = "AWS::IoTTwinMaker::SyncJob"


    /// The IAM Role that execute SyncJob.
    hidden SyncRole: String(matches(Regex(#"arn:((aws)|(aws-cn)|(aws-us-gov)):iam::[0-9]{12}:role/.*"#)))|Mapping

    /// The source of the SyncJob.
    hidden SyncSource: String|Mapping

    /// A key-value pair to associate with a resource.
    hidden Tags: (SyncJobSyncJobTags)?

    /// The ID of the workspace.
    hidden WorkspaceId: String(matches(Regex(#"[a-zA-Z_0-9][a-zA-Z_\-0-9]*[a-zA-Z0-9]+"#)))|Mapping

    Properties {
        ["SyncRole"] = if (SyncRole == null) null else SyncRole
        ["SyncSource"] = if (SyncSource == null) null else SyncSource
        ["Tags"] = if (Tags == null) null else Tags
        ["WorkspaceId"] = if (WorkspaceId == null) null else WorkspaceId
    }

}


typealias SyncJobSyncJobTags = Mapping<String, Any>
