/// AWS::CodeConnections::Connection
///
/// Generated by rain build --pkl-class AWS::CodeConnections::Connection
module aws.codeconnections.connection

import "../../cloudformation.pkl"

/// A key-value pair to associate with a resource.
open class ConnectionTag {
    Key: String|Mapping
    Value: String|Mapping
}

/// Schema for AWS::CodeConnections::Connection resource which can be used to connect external source providers with other AWS services (i.e. AWS CodePipeline)
open class Connection extends cloudformation.Resource {

    Type = "AWS::CodeConnections::Connection"


    /// The name of the connection. Connection names must be unique in an AWS user account.
    hidden ConnectionName: String|Mapping

    /// The host arn configured to represent the infrastructure where your third-party provider is installed. You must specify either a ProviderType or a HostArn.
    hidden HostArn: (String(matches(Regex(#"arn:aws(-[\w]+)*:.+:.+:[0-9]{12}:.+"#)))|Mapping)?

    /// The name of the external provider where your third-party code repository is configured. You must specify either a ProviderType or a HostArn.
    hidden ProviderType: (String|Mapping)?

    /// Specifies the tags applied to a connection.
    hidden Tags: (Listing<ConnectionTag>)?

    Properties {
        ["ConnectionName"] = if (ConnectionName == null) null else ConnectionName
        ["HostArn"] = if (HostArn == null) null else HostArn
        ["ProviderType"] = if (ProviderType == null) null else ProviderType
        ["Tags"] = if (Tags == null) null else Tags
    }

}
