/// AWS::Kinesis::Stream
///
/// Generated by rain build --pkl-class AWS::Kinesis::Stream
module aws.kinesis.stream

import "../../cloudformation.pkl"

typealias StreamEncryptionEncryptionType = "KMS"

/// When specified, enables or updates server-side encryption using an AWS KMS key for a specified stream. Removing this property from your stack template and updating your stack disables encryption.
open class StreamStreamEncryption {
    EncryptionType: StreamEncryptionEncryptionType|Mapping
    KeyId: String|Mapping
}

typealias StreamModeDetailsStreamMode = "ON_DEMAND"|"PROVISIONED"

/// When specified, enables or updates the mode of stream. Default is PROVISIONED.
open class StreamStreamModeDetails {
    StreamMode: StreamModeDetailsStreamMode|Mapping
}

/// An arbitrary set of tags (key-value pairs) to associate with the Kinesis stream.
open class StreamTag {
    Key: String|Mapping
    Value: String|Mapping
}

/// Resource Type definition for AWS::Kinesis::Stream
open class Stream extends cloudformation.Resource {

    Type = "AWS::Kinesis::Stream"


    /// The name of the Kinesis stream.
    hidden Name: (String(matches(Regex(#"^[a-zA-Z0-9_.-]+$"#)))|Mapping)?

    /// The number of hours for the data records that are stored in shards to remain accessible.
    hidden RetentionPeriodHours: (Int|Mapping)?

    /// The number of shards that the stream uses. Required when StreamMode = PROVISIONED is passed.
    hidden ShardCount: (Int|Mapping)?

    /// When specified, enables or updates server-side encryption using an AWS KMS key for a specified stream.
    hidden StreamEncryption: (StreamStreamEncryption)?

    /// The mode in which the stream is running.
    hidden StreamModeDetails: (StreamStreamModeDetails)?

    /// An arbitrary set of tags (key–value pairs) to associate with the Kinesis stream.
    hidden Tags: (Listing<StreamTag>)?

    Properties {
        ["Name"] = if (Name == null) null else Name
        ["RetentionPeriodHours"] = if (RetentionPeriodHours == null) null else RetentionPeriodHours
        ["ShardCount"] = if (ShardCount == null) null else ShardCount
        ["StreamEncryption"] = if (StreamEncryption == null) null else StreamEncryption
        ["StreamModeDetails"] = if (StreamModeDetails == null) null else StreamModeDetails
        ["Tags"] = if (Tags == null) null else Tags
    }

}
