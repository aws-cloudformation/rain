/// AWS::SageMaker::Project
///
/// Generated by rain build --pkl-class AWS::SageMaker::Project
module aws.sagemaker.project

import "../../cloudformation.pkl"

/// The path identifier of the product.
open class PathId {
}

/// Project Id.
open class ProjectId {
}

/// The Amazon Resource Name (ARN) of the Project.
open class ProjectArn {
}

/// Provisioned Product Status Message
open class ProvisionedProductStatusMessage {
}

/// The name of the project.
open class ProjectName {
}

/// Service Catalog product identifier.
open class ProductId {
}

/// The identifier of the provisioning artifact (also known as a version).
open class ProvisioningArtifactId {
}

/// Information about a parameter used to provision a product.
open class ProvisioningParameter {
    Key: String(matches(Regex(#".*"#)))|Mapping
    Value: String(matches(Regex(#".*"#)))|Mapping
}

/// A key-value pair to associate with a resource.
open class Tag {
    Key: String(matches(Regex(#"^([\p{L}\p{Z}\p{N}_.:/=+\-@]*)$"#)))|Mapping
    Value: String(matches(Regex(#"^([\p{L}\p{Z}\p{N}_.:/=+\-@]*)$"#)))|Mapping
}

/// The description of the project.
open class ProjectDescription {
}

/// Resource Type definition for AWS::SageMaker::Project
open class Project extends cloudformation.Resource {

    Type = "AWS::SageMaker::Project"


    /// Input ServiceCatalog Provisioning Details
    hidden ServiceCatalogProvisioningDetails: Dynamic

    /// Provisioned ServiceCatalog  Details
    hidden ServiceCatalogProvisionedProductDetails: (Dynamic)?

    /// An array of key-value pairs to apply to this resource.
    hidden Tags: (Listing<Tag>)?

    /// 
    hidden ProjectName: ProjectName

    /// 
    hidden ProjectDescription: (ProjectDescription)?

    Properties {
        ["ServiceCatalogProvisioningDetails"] = if (ServiceCatalogProvisioningDetails == null) null else ServiceCatalogProvisioningDetails
        ["ServiceCatalogProvisionedProductDetails"] = if (ServiceCatalogProvisionedProductDetails == null) null else ServiceCatalogProvisionedProductDetails
        ["Tags"] = if (Tags == null) null else Tags
        ["ProjectName"] = if (ProjectName == null) null else ProjectName
        ["ProjectDescription"] = if (ProjectDescription == null) null else ProjectDescription
    }

}
