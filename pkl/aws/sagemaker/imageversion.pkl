/// AWS::SageMaker::ImageVersion
///
/// Generated by rain build --pkl-class AWS::SageMaker::ImageVersion
module aws.sagemaker.imageversion

import "../../cloudformation.pkl"

/// The alias of the image version.
open class Alias {
}

/// The machine learning framework vended in the image version.
open class MLFramework {
}

/// Indicates Horovod compatibility.
open class Horovod {
}

/// The registry path of the container image on which this image version is based.
open class BaseImage {
}

/// The registry path of the container image that contains this image version.
open class ContainerImage {
}

/// Indicates SageMaker job type compatibility.
open class JobType {
}

/// The supported programming language and its version.
open class ProgrammingLang {
}

/// The Amazon Resource Name (ARN) of the image version.
open class ImageVersionArn {
}

/// The version number of the image version.
open class Version {
}

/// The availability of the image version specified by the maintainer.
open class VendorGuidance {
}

/// Indicates CPU or GPU compatibility.
open class Processor {
}

/// List of aliases for the image version.
open class Aliases {
}

/// The Amazon Resource Name (ARN) of the parent image.
open class ImageArn {
}

/// The maintainer description of the image version.
open class ReleaseNotes {
}

/// The name of the image this version belongs to.
open class ImageName {
}

/// Resource Type definition for AWS::SageMaker::ImageVersion
open class ImageVersion extends cloudformation.Resource {

    Type = "AWS::SageMaker::ImageVersion"


    /// 
    hidden MLFramework: (MLFramework)?

    /// 
    hidden ProgrammingLang: (ProgrammingLang)?

    /// 
    hidden Horovod: (Horovod)?

    /// 
    hidden BaseImage: BaseImage

    /// 
    hidden Aliases: (Aliases)?

    /// 
    hidden VendorGuidance: (VendorGuidance)?

    /// 
    hidden ImageName: ImageName

    /// 
    hidden Alias: (Alias)?

    /// 
    hidden JobType: (JobType)?

    /// 
    hidden Processor: (Processor)?

    /// 
    hidden ReleaseNotes: (ReleaseNotes)?

    Properties {
        ["MLFramework"] = if (MLFramework == null) null else MLFramework
        ["ProgrammingLang"] = if (ProgrammingLang == null) null else ProgrammingLang
        ["Horovod"] = if (Horovod == null) null else Horovod
        ["BaseImage"] = if (BaseImage == null) null else BaseImage
        ["Aliases"] = if (Aliases == null) null else Aliases
        ["VendorGuidance"] = if (VendorGuidance == null) null else VendorGuidance
        ["ImageName"] = if (ImageName == null) null else ImageName
        ["Alias"] = if (Alias == null) null else Alias
        ["JobType"] = if (JobType == null) null else JobType
        ["Processor"] = if (Processor == null) null else Processor
        ["ReleaseNotes"] = if (ReleaseNotes == null) null else ReleaseNotes
    }

}
