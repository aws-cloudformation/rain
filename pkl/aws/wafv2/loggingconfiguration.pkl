/// AWS::WAFv2::LoggingConfiguration
///
/// Generated by rain build --pkl-class AWS::WAFv2::LoggingConfiguration
module aws.wafv2.loggingconfiguration

import "../../cloudformation.pkl"

/// 
open class LoggingConfigurationCondition {
    ActionCondition: (Dynamic)?
    LabelNameCondition: (Dynamic)?
}

/// A key-value pair to associate with a resource.
open class LoggingConfigurationFieldToMatch {
    Method: (Dynamic)?
    QueryString: (Dynamic)?
    SingleHeader: (Dynamic)?
    UriPath: (Dynamic)?
}

typealias FilterBehavior = "KEEP"|"DROP"

typealias FilterRequirement = "MEETS_ALL"|"MEETS_ANY"

/// 
open class LoggingConfigurationFilter {
    Behavior: FilterBehavior|Mapping
    Conditions: Listing<LoggingConfigurationCondition>
    Requirement: FilterRequirement|Mapping
}

/// A WAFv2 Logging Configuration Resource Provider
open class LoggingConfiguration extends cloudformation.Resource {

    Type = "AWS::WAFv2::LoggingConfiguration"


    /// The Amazon Resource Names (ARNs) of the logging destinations that you want to associate with the web ACL.
    hidden LogDestinationConfigs: Listing<String|Mapping>

    /// Filtering that specifies which web requests are kept in the logs and which are dropped. You can filter on the rule action and on the web request labels that were applied by matching rules during web ACL evaluation.
    hidden LoggingFilter: (Dynamic)?

    /// The parts of the request that you want to keep out of the logs. For example, if you redact the HEADER field, the HEADER field in the firehose will be xxx.
    hidden RedactedFields: (Listing<LoggingConfigurationFieldToMatch>)?

    /// The Amazon Resource Name (ARN) of the web ACL that you want to associate with LogDestinationConfigs.
    hidden ResourceArn: String|Mapping

    Properties {
        ["LogDestinationConfigs"] = if (LogDestinationConfigs == null) null else LogDestinationConfigs
        ["LoggingFilter"] = if (LoggingFilter == null) null else LoggingFilter
        ["RedactedFields"] = if (RedactedFields == null) null else RedactedFields
        ["ResourceArn"] = if (ResourceArn == null) null else ResourceArn
    }

}
