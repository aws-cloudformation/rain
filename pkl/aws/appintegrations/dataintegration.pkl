/// AWS::AppIntegrations::DataIntegration
///
/// Generated by rain build --pkl-class AWS::AppIntegrations::DataIntegration
module aws.appintegrations.dataintegration

import "../../cloudformation.pkl"

/// 
open class DataIntegrationScheduleConfig {
    FirstExecutionFrom: (String(matches(Regex(#".*\S.*"#)))|Mapping)?
    Object: (String(matches(Regex(#"^[a-zA-Z0-9/\._\-]+$"#)))|Mapping)?
    ScheduleExpression: String(matches(Regex(#".*\S.*"#)))|Mapping
}

/// A label for tagging DataIntegration resources
open class DataIntegrationTag {
    Key: String(matches(Regex(#"^(?!aws:)[a-zA-Z+-=._:/]+$"#)))|Mapping
    Value: String|Mapping
}

typealias DataIntegrationFileConfigurationFilters = Mapping<String, Any>

/// The configuration for what files should be pulled from the source.
open class DataIntegrationFileConfiguration {
    Folders: Listing<String|Mapping>
    Filters: (DataIntegrationFileConfigurationFilters)?
}

typealias DataIntegrationObjectConfiguration = Mapping<String, Any>


/// Resource Type definition for AWS::AppIntegrations::DataIntegration
open class DataIntegration extends cloudformation.Resource {

    Type = "AWS::AppIntegrations::DataIntegration"


    /// The URI of the data source.
    hidden SourceURI: String(matches(Regex(#"^(\w+\:\/\/[\w.-]+[\w/!@#+=.-]+$)|(\w+\:\/\/[\w.-]+[\w/!@#+=.-]+[\w/!@#+=.-]+[\w/!@#+=.,-]+$)"#)))|Mapping

    /// The tags (keys and values) associated with the data integration.
    hidden Tags: (Listing<DataIntegrationTag>)?

    /// The configuration for what files should be pulled from the source.
    hidden FileConfiguration: (DataIntegrationFileConfiguration)?

    /// The data integration description.
    hidden Description: (String|Mapping)?

    /// The name of the data integration.
    hidden Name: String(matches(Regex(#"^[a-zA-Z0-9/\._\-]+$"#)))|Mapping

    /// The name of the data and how often it should be pulled from the source.
    hidden ScheduleConfig: (DataIntegrationScheduleConfig)?

    /// The configuration for what data should be pulled from the source.
    hidden ObjectConfiguration: (DataIntegrationObjectConfiguration)?

    /// The KMS key of the data integration.
    hidden KmsKey: String(matches(Regex(#".*\S.*"#)))|Mapping

    Properties {
        ["SourceURI"] = if (SourceURI == null) null else SourceURI
        ["Tags"] = if (Tags == null) null else Tags
        ["FileConfiguration"] = if (FileConfiguration == null) null else FileConfiguration
        ["Description"] = if (Description == null) null else Description
        ["Name"] = if (Name == null) null else Name
        ["ScheduleConfig"] = if (ScheduleConfig == null) null else ScheduleConfig
        ["ObjectConfiguration"] = if (ObjectConfiguration == null) null else ObjectConfiguration
        ["KmsKey"] = if (KmsKey == null) null else KmsKey
    }

}
