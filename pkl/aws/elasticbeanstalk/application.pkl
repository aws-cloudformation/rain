/// AWS::ElasticBeanstalk::Application
///
/// Generated by rain build --pkl-class AWS::ElasticBeanstalk::Application
module aws.elasticbeanstalk.application

import "../../cloudformation.pkl"

/// 
open class ApplicationApplicationResourceLifecycleConfig {
    ServiceRole: (String|Mapping)?
    VersionLifecycleConfig: (ApplicationApplicationVersionLifecycleConfig)?
}

/// 
open class ApplicationApplicationVersionLifecycleConfig {
    MaxAgeRule: (ApplicationMaxAgeRule)?
    MaxCountRule: (ApplicationMaxCountRule)?
}

/// 
open class ApplicationMaxAgeRule {
    DeleteSourceFromS3: (Boolean|Mapping)?
    Enabled: (Boolean|Mapping)?
    MaxAgeInDays: (Int|Mapping)?
}

/// 
open class ApplicationMaxCountRule {
    DeleteSourceFromS3: (Boolean|Mapping)?
    Enabled: (Boolean|Mapping)?
    MaxCount: (Int|Mapping)?
}

/// The AWS::ElasticBeanstalk::Application resource specifies an Elastic Beanstalk application.
open class Application extends cloudformation.Resource {

    Type = "AWS::ElasticBeanstalk::Application"


    /// A name for the Elastic Beanstalk application. If you don't specify a name, AWS CloudFormation generates a unique physical ID and uses that ID for the application name.
    hidden ApplicationName: (String|Mapping)?

    /// Your description of the application.
    hidden Description: (String|Mapping)?

    /// Specifies an application resource lifecycle configuration to prevent your application from accumulating too many versions.
    hidden ResourceLifecycleConfig: (ApplicationApplicationResourceLifecycleConfig)?

    Properties {
        ["ApplicationName"] = if (ApplicationName == null) null else ApplicationName
        ["Description"] = if (Description == null) null else Description
        ["ResourceLifecycleConfig"] = if (ResourceLifecycleConfig == null) null else ResourceLifecycleConfig
    }

}
