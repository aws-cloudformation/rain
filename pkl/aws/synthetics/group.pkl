/// AWS::Synthetics::Group
///
/// Generated by rain build --pkl-class AWS::Synthetics::Group
module aws.synthetics.group

import "../../cloudformation.pkl"

/// A key-value pair to associate with a resource.
open class Tag {
    Key: String(matches(Regex(#"^(?!aws:)([a-zA-Z\d\s_.:/=+\-@]+)$"#)))|Mapping
    Value: String(matches(Regex(#"^([a-zA-Z\d\s_.:/=+\-@]*)$"#)))|Mapping
}

/// Provide Canary Arn associated with the group.
open class ResourceArn {
}

/// Resource Type definition for AWS::Synthetics::Group
open class Group extends cloudformation.Resource {

    Type = "AWS::Synthetics::Group"


    /// 
    hidden ResourceArns: (Listing<ResourceArn>)?

    /// Name of the group.
    hidden Name: String(matches(Regex(#"^[0-9a-z_\-]{1,64}$"#)))|Mapping

    /// 
    hidden Tags: (Listing<Tag>)?

    Properties {
        ["ResourceArns"] = if (ResourceArns == null) null else ResourceArns
        ["Name"] = if (Name == null) null else Name
        ["Tags"] = if (Tags == null) null else Tags
    }

}
