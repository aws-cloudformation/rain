/// AWS::SSMContacts::Contact
///
/// Generated by rain build --pkl-class AWS::SSMContacts::Contact
module aws.ssmcontacts.contact

import "../../cloudformation.pkl"

/// The contact that SSM Incident Manager is engaging during an incident.
open class ContactTargetInfo {
    ContactId: String|Mapping
    IsEssential: Boolean|Mapping
}

/// Information about the contact channel that SSM Incident Manager uses to engage the contact.
open class ChannelTargetInfo {
    ChannelId: String|Mapping
    RetryIntervalInMinutes: Int|Mapping
}

/// A set amount of time that an escalation plan or engagement plan engages the specified contacts or contact methods.
open class Stage {
    DurationInMinutes: (Int|Mapping)?
    Targets: (Listing<Targets>)?
    RotationIds: (Listing<String|Mapping>)?
}

/// The contacts or contact methods that the escalation plan or engagement plan is engaging.
open class Targets {
    ContactTargetInfo: (ContactTargetInfo)?
    ChannelTargetInfo: (ChannelTargetInfo)?
}

/// Resource Type definition for AWS::SSMContacts::Contact
open class Contact extends cloudformation.Resource {

    Type = "AWS::SSMContacts::Contact"


    /// Contact type, which specify type of contact. Currently supported values: ?PERSONAL?, ?SHARED?, ?OTHER?.
    hidden TypeProperty: ContactType|Mapping

    /// The stages that an escalation plan or engagement plan engages contacts and contact methods in.
    hidden Plan: (Listing<Stage>)?

    /// Alias of the contact. String value with 20 to 256 characters. Only alphabetical, numeric characters, dash, or underscore allowed.
    hidden Alias: String(matches(Regex(#"^[a-z0-9_\-\.]*$"#)))|Mapping

    /// Name of the contact. String value with 3 to 256 characters. Only alphabetical, space, numeric characters, dash, or underscore allowed.
    hidden DisplayName: String(matches(Regex(#"^[a-zA-Z0-9_\-\s]*$"#)))|Mapping

    Properties {
        ["Type"] = if (TypeProperty == null) null else TypeProperty
        ["Plan"] = if (Plan == null) null else Plan
        ["Alias"] = if (Alias == null) null else Alias
        ["DisplayName"] = if (DisplayName == null) null else DisplayName
    }

}


typealias ContactType = "PERSONAL"|"ESCALATION"|"ONCALL_SCHEDULE"
