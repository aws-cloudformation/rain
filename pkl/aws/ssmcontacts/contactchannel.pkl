/// AWS::SSMContacts::ContactChannel
///
/// Generated by rain build --pkl-class AWS::SSMContacts::ContactChannel
module aws.ssmcontacts.contactchannel

import "../../cloudformation.pkl"

/// Resource Type definition for AWS::SSMContacts::ContactChannel
open class ContactChannel extends cloudformation.Resource {

    Type = "AWS::SSMContacts::ContactChannel"


    /// The details that SSM Incident Manager uses when trying to engage the contact channel.
    hidden ChannelAddress: (String|Mapping)?

    /// The device name. String of 6 to 50 alphabetical, numeric, dash, and underscore characters.
    hidden ChannelName: (String(matches(Regex(#"[a-zA-Z 0-9_\-+'&amp;\uD83C-\uDBFF\uDC00-\uDFFF\u2000-\u3300]+"#)))|Mapping)?

    /// Device type, which specify notification channel. Currently supported values: ?SMS?, ?VOICE?, ?EMAIL?, ?CHATBOT.
    hidden ChannelType: (ContactChannelChannelType|Mapping)?

    /// ARN of the contact resource
    hidden ContactId: (String(matches(Regex(#"arn:[-\w+=\/,.@]+:[-\w+=\/,.@]+:[-\w+=\/,.@]*:[0-9]+:([\w+=\/,.@:-]+)*"#)))|Mapping)?

    /// If you want to activate the channel at a later time, you can choose to defer activation. SSM Incident Manager can't engage your contact channel until it has been activated.
    hidden DeferActivation: (Boolean|Mapping)?

    Properties {
        ["ChannelAddress"] = if (ChannelAddress == null) null else ChannelAddress
        ["ChannelName"] = if (ChannelName == null) null else ChannelName
        ["ChannelType"] = if (ChannelType == null) null else ChannelType
        ["ContactId"] = if (ContactId == null) null else ContactId
        ["DeferActivation"] = if (DeferActivation == null) null else DeferActivation
    }

}


typealias ContactChannelChannelType = "SMS"|"VOICE"|"EMAIL"
