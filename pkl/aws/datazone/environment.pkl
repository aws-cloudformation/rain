/// AWS::DataZone::Environment
///
/// Generated by rain build --pkl-class AWS::DataZone::Environment
module aws.datazone.environment

import "../../cloudformation.pkl"

/// The parameter details of an environment.
open class EnvironmentEnvironmentParameter {
    Name: (String|Mapping)?
    Value: (String|Mapping)?
}

typealias EnvironmentEnvironmentStatus = String|Mapping


/// Definition of AWS::DataZone::Environment Resource Type
open class Environment extends cloudformation.Resource {

    Type = "AWS::DataZone::Environment"


    /// The description of the Amazon DataZone environment.
    hidden Description: (String|Mapping)?

    /// The identifier of the Amazon DataZone domain in which the environment would be created.
    hidden DomainIdentifier: String(matches(Regex(#"^dzd[-_][a-zA-Z0-9_-]{1,36}$"#)))|Mapping

    /// The ID of the environment profile with which the Amazon DataZone environment would be created.
    hidden EnvironmentProfileIdentifier: String(matches(Regex(#"^[a-zA-Z0-9_-]{1,36}$"#)))|Mapping

    /// The glossary terms that can be used in the Amazon DataZone environment.
    hidden GlossaryTerms: (Listing<String|Mapping>)?

    /// The name of the environment.
    hidden Name: String(matches(Regex(#"^[\w -]+$"#)))|Mapping

    /// The ID of the Amazon DataZone project in which the environment would be created.
    hidden ProjectIdentifier: String(matches(Regex(#"^[a-zA-Z0-9_-]{1,36}$"#)))|Mapping

    /// The user parameters of the Amazon DataZone environment.
    hidden UserParameters: (Listing<EnvironmentEnvironmentParameter>)?

    Properties {
        ["Description"] = if (Description == null) null else Description
        ["DomainIdentifier"] = if (DomainIdentifier == null) null else DomainIdentifier
        ["EnvironmentProfileIdentifier"] = if (EnvironmentProfileIdentifier == null) null else EnvironmentProfileIdentifier
        ["GlossaryTerms"] = if (GlossaryTerms == null) null else GlossaryTerms
        ["Name"] = if (Name == null) null else Name
        ["ProjectIdentifier"] = if (ProjectIdentifier == null) null else ProjectIdentifier
        ["UserParameters"] = if (UserParameters == null) null else UserParameters
    }

}
