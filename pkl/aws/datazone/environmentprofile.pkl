/// AWS::DataZone::EnvironmentProfile
///
/// Generated by rain build --pkl-class AWS::DataZone::EnvironmentProfile
module aws.datazone.environmentprofile

import "../../cloudformation.pkl"

/// The parameter details of an environment profile.
open class EnvironmentProfileEnvironmentParameter {
    Name: (String|Mapping)?
    Value: (String|Mapping)?
}

/// AWS Datazone Environment Profile is pre-configured set of resources and blueprints that provide reusable templates for creating environments.
open class EnvironmentProfile extends cloudformation.Resource {

    Type = "AWS::DataZone::EnvironmentProfile"


    /// The AWS account in which the Amazon DataZone environment is created.
    hidden AwsAccountId: String(matches(Regex(#"^\d{12}$"#)))|Mapping

    /// The AWS region in which this environment profile is created.
    hidden AwsAccountRegion: String(matches(Regex(#"^[a-z]{2}-[a-z]{4,10}-\d$"#)))|Mapping

    /// The description of this Amazon DataZone environment profile.
    hidden Description: (String|Mapping)?

    /// The ID of the Amazon DataZone domain in which this environment profile is created.
    hidden DomainIdentifier: String(matches(Regex(#"^dzd[-_][a-zA-Z0-9_-]{1,36}$"#)))|Mapping

    /// The ID of the blueprint with which this environment profile is created.
    hidden EnvironmentBlueprintIdentifier: String(matches(Regex(#"^[a-zA-Z0-9_-]{1,36}$"#)))|Mapping

    /// The name of this Amazon DataZone environment profile.
    hidden Name: String(matches(Regex(#"^[\w -]+$"#)))|Mapping

    /// The identifier of the project in which to create the environment profile.
    hidden ProjectIdentifier: String(matches(Regex(#"^[a-zA-Z0-9_-]{1,36}$"#)))|Mapping

    /// The user parameters of this Amazon DataZone environment profile.
    hidden UserParameters: (Listing<EnvironmentProfileEnvironmentParameter>)?

    Properties {
        ["AwsAccountId"] = if (AwsAccountId == null) null else AwsAccountId
        ["AwsAccountRegion"] = if (AwsAccountRegion == null) null else AwsAccountRegion
        ["Description"] = if (Description == null) null else Description
        ["DomainIdentifier"] = if (DomainIdentifier == null) null else DomainIdentifier
        ["EnvironmentBlueprintIdentifier"] = if (EnvironmentBlueprintIdentifier == null) null else EnvironmentBlueprintIdentifier
        ["Name"] = if (Name == null) null else Name
        ["ProjectIdentifier"] = if (ProjectIdentifier == null) null else ProjectIdentifier
        ["UserParameters"] = if (UserParameters == null) null else UserParameters
    }

}
