/// AWS::DataZone::Project
///
/// Generated by rain build --pkl-class AWS::DataZone::Project
module aws.datazone.project

import "../../cloudformation.pkl"

/// Amazon DataZone projects are business use case?based groupings of people, assets (data), and tools used to simplify access to the AWS analytics.
open class Project extends cloudformation.Resource {

    Type = "AWS::DataZone::Project"


    /// The description of the Amazon DataZone project.
    hidden Description: (String|Mapping)?

    /// The ID of the Amazon DataZone domain in which this project is created.
    hidden DomainIdentifier: String(matches(Regex(#"^dzd[-_][a-zA-Z0-9_-]{1,36}$"#)))|Mapping

    /// The glossary terms that can be used in this Amazon DataZone project.
    hidden GlossaryTerms: (Listing<String|Mapping>)?

    /// The name of the Amazon DataZone project.
    hidden Name: String(matches(Regex(#"^[\w -]+$"#)))|Mapping

    Properties {
        ["Description"] = if (Description == null) null else Description
        ["DomainIdentifier"] = if (DomainIdentifier == null) null else DomainIdentifier
        ["GlossaryTerms"] = if (GlossaryTerms == null) null else GlossaryTerms
        ["Name"] = if (Name == null) null else Name
    }

}
