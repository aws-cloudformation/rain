/// AWS::DataZone::Domain
///
/// Generated by rain build --pkl-class AWS::DataZone::Domain
module aws.datazone.domain

import "../../cloudformation.pkl"

typealias DomainAuthType = String|Mapping


typealias DomainDomainStatus = String|Mapping


/// The single-sign on configuration of the Amazon DataZone domain.
open class DomainSingleSignOn {
    Type: (DomainAuthType)?
    UserAssignment: (DomainUserAssignment)?
}

/// A key-value pair to associate with the domain.
open class DomainTag {
    Key: String|Mapping
    Value: String|Mapping
}

typealias DomainUserAssignment = String|Mapping


/// A domain is an organizing entity for connecting together assets, users, and their projects
open class Domain extends cloudformation.Resource {

    Type = "AWS::DataZone::Domain"


    /// The description of the Amazon DataZone domain.
    hidden Description: (String|Mapping)?

    /// The domain execution role that is created when an Amazon DataZone domain is created. The domain execution role is created in the AWS account that houses the Amazon DataZone domain.
    hidden DomainExecutionRole: String(matches(Regex(#"^arn:aws[^:]*:iam::\d{12}:(role|role/service-role)/[\w+=,.@-]*$"#)))|Mapping

    /// The identifier of the AWS Key Management Service (KMS) key that is used to encrypt the Amazon DataZone domain, metadata, and reporting data.
    hidden KmsKeyIdentifier: (String(matches(Regex(#"^arn:aws(|-cn|-us-gov):kms:[a-zA-Z0-9-]*:[0-9]{12}:key/[a-zA-Z0-9-]{36}$"#)))|Mapping)?

    /// The name of the Amazon DataZone domain.
    hidden Name: String|Mapping

    /// The single-sign on configuration of the Amazon DataZone domain.
    hidden SingleSignOn: (DomainSingleSignOn)?

    /// The tags specified for the Amazon DataZone domain.
    hidden Tags: (Listing<DomainTag>)?

    Properties {
        ["Description"] = if (Description == null) null else Description
        ["DomainExecutionRole"] = if (DomainExecutionRole == null) null else DomainExecutionRole
        ["KmsKeyIdentifier"] = if (KmsKeyIdentifier == null) null else KmsKeyIdentifier
        ["Name"] = if (Name == null) null else Name
        ["SingleSignOn"] = if (SingleSignOn == null) null else SingleSignOn
        ["Tags"] = if (Tags == null) null else Tags
    }

}
