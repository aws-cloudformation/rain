/// AWS::DataZone::SubscriptionTarget
///
/// Generated by rain build --pkl-class AWS::DataZone::SubscriptionTarget
module aws.datazone.subscriptiontarget

import "../../cloudformation.pkl"

/// The details of the subscription target configuration.
open class SubscriptionTargetSubscriptionTargetForm {
    Content: String|Mapping
    FormName: String(matches(Regex(#"^(?![0-9_])\w+$|^_\w*[a-zA-Z0-9]\w*$"#)))|Mapping
}

/// Subscription targets enables one to access the data to which you have subscribed in your projects.
open class SubscriptionTarget extends cloudformation.Resource {

    Type = "AWS::DataZone::SubscriptionTarget"


    /// The asset types that can be included in the subscription target.
    hidden ApplicableAssetTypes: Listing<String|Mapping>

    /// The authorized principals of the subscription target.
    hidden AuthorizedPrincipals: Listing<String|Mapping>

    /// The ID of the Amazon DataZone domain in which subscription target would be created.
    hidden DomainIdentifier: String(matches(Regex(#"^dzd[-_][a-zA-Z0-9_-]{1,36}$"#)))|Mapping

    /// The ID of the environment in which subscription target would be created.
    hidden EnvironmentIdentifier: String(matches(Regex(#"^[a-zA-Z0-9_-]{1,36}$"#)))|Mapping

    /// The manage access role that is used to create the subscription target.
    hidden ManageAccessRole: String|Mapping

    /// The name of the subscription target.
    hidden Name: String|Mapping

    /// The provider of the subscription target.
    hidden Provider: (String|Mapping)?

    /// The configuration of the subscription target.
    hidden SubscriptionTargetConfig: Listing<SubscriptionTargetSubscriptionTargetForm>

    /// The type of the subscription target.
    hidden TypeProperty: String|Mapping

    Properties {
        ["ApplicableAssetTypes"] = if (ApplicableAssetTypes == null) null else ApplicableAssetTypes
        ["AuthorizedPrincipals"] = if (AuthorizedPrincipals == null) null else AuthorizedPrincipals
        ["DomainIdentifier"] = if (DomainIdentifier == null) null else DomainIdentifier
        ["EnvironmentIdentifier"] = if (EnvironmentIdentifier == null) null else EnvironmentIdentifier
        ["ManageAccessRole"] = if (ManageAccessRole == null) null else ManageAccessRole
        ["Name"] = if (Name == null) null else Name
        ["Provider"] = if (Provider == null) null else Provider
        ["SubscriptionTargetConfig"] = if (SubscriptionTargetConfig == null) null else SubscriptionTargetConfig
        ["Type"] = if (TypeProperty == null) null else TypeProperty
    }

}
