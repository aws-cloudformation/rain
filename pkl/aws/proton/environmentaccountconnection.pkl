/// AWS::Proton::EnvironmentAccountConnection
///
/// Generated by rain build --pkl-class AWS::Proton::EnvironmentAccountConnection
module aws.proton.environmentaccountconnection

import "../../cloudformation.pkl"

typealias EnvironmentAccountConnectionStatus = String|Mapping


/// <p>A description of a resource tag.</p>
open class EnvironmentAccountConnectionTag {
    Key: String|Mapping
    Value: String|Mapping
}

/// Resource Schema describing various properties for AWS Proton Environment Account Connections resources.
open class EnvironmentAccountConnection extends cloudformation.Resource {

    Type = "AWS::Proton::EnvironmentAccountConnection"


    /// The Amazon Resource Name (ARN) of an IAM service role in the environment account. AWS Proton uses this role to provision infrastructure resources using CodeBuild-based provisioning in the associated environment account.
    hidden CodebuildRoleArn: (String(matches(Regex(#"^arn:(aws|aws-cn|aws-us-gov):iam::\d{12}:role/([\w+=,.@-]{1,512}[/:])*([\w+=,.@-]{1,64})$"#)))|Mapping)?

    /// The Amazon Resource Name (ARN) of the IAM service role that AWS Proton uses when provisioning directly defined components in the associated environment account. It determines the scope of infrastructure that a component can provision in the account.
    hidden ComponentRoleArn: (String(matches(Regex(#"^arn:(aws|aws-cn|aws-us-gov):iam::\d{12}:role/([\w+=,.@-]{1,512}[/:])*([\w+=,.@-]{1,64})$"#)))|Mapping)?

    /// The environment account that's connected to the environment account connection.
    hidden EnvironmentAccountId: (String(matches(Regex(#"^\d{12}$"#)))|Mapping)?

    /// The name of the AWS Proton environment that's created in the associated management account.
    hidden EnvironmentName: (String(matches(Regex(#"^[0-9A-Za-z]+[0-9A-Za-z_\-]*$"#)))|Mapping)?

    /// The ID of the management account that accepts or rejects the environment account connection. You create an manage the AWS Proton environment in this account. If the management account accepts the environment account connection, AWS Proton can use the associated IAM role to provision environment infrastructure resources in the associated environment account.
    hidden ManagementAccountId: (String(matches(Regex(#"^\d{12}$"#)))|Mapping)?

    /// The Amazon Resource Name (ARN) of the IAM service role that's created in the environment account. AWS Proton uses this role to provision infrastructure resources in the associated environment account.
    hidden RoleArn: (String(matches(Regex(#"^arn:(aws|aws-cn|aws-us-gov):iam::\d{12}:role/([\w+=,.@-]{1,512}[/:])*([\w+=,.@-]{1,64})$"#)))|Mapping)?

    /// <p>An optional list of metadata items that you can associate with the Proton environment account connection. A tag is a key-value pair.</p>
    ///
    ///          <p>For more information, see <a href="https://docs.aws.amazon.com/proton/latest/userguide/resources.html">Proton resources and tagging</a> in the
    ///         <i>Proton User Guide</i>.</p>
    hidden Tags: (Listing<EnvironmentAccountConnectionTag>)?

    Properties {
        ["CodebuildRoleArn"] = if (CodebuildRoleArn == null) null else CodebuildRoleArn
        ["ComponentRoleArn"] = if (ComponentRoleArn == null) null else ComponentRoleArn
        ["EnvironmentAccountId"] = if (EnvironmentAccountId == null) null else EnvironmentAccountId
        ["EnvironmentName"] = if (EnvironmentName == null) null else EnvironmentName
        ["ManagementAccountId"] = if (ManagementAccountId == null) null else ManagementAccountId
        ["RoleArn"] = if (RoleArn == null) null else RoleArn
        ["Tags"] = if (Tags == null) null else Tags
    }

}
