/// AWS::SES::DedicatedIpPool
///
/// Generated by rain build --pkl-class AWS::SES::DedicatedIpPool
module aws.ses.dedicatedippool

import "../../cloudformation.pkl"

/// Resource Type definition for AWS::SES::DedicatedIpPool
open class DedicatedIpPool extends cloudformation.Resource {

    Type = "AWS::SES::DedicatedIpPool"


    /// The name of the dedicated IP pool.
    hidden PoolName: (String(matches(Regex(#"^[a-z0-9_-]{0,64}$"#)))|Mapping)?

    /// Specifies whether the dedicated IP pool is managed or not. The default value is STANDARD.
    hidden ScalingMode: (String(matches(Regex(#"^(STANDARD|MANAGED)$"#)))|Mapping)?

    Properties {
        ["PoolName"] = if (PoolName == null) null else PoolName
        ["ScalingMode"] = if (ScalingMode == null) null else ScalingMode
    }

}
