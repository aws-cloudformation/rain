/// AWS::SES::ConfigurationSet
///
/// Generated by rain build --pkl-class AWS::SES::ConfigurationSet
module aws.ses.configurationset

import "../../cloudformation.pkl"

/// An object that contains Virtual Deliverability Manager (VDM) settings for this configuration set.
open class VdmOptions {
    DashboardOptions: (DashboardOptions)?
    GuardianOptions: (GuardianOptions)?
}

/// An object that defines the open and click tracking options for emails that you send using the configuration set.
open class TrackingOptions {
    CustomRedirectDomain: (String|Mapping)?
}

/// An object that defines the dedicated IP pool that is used to send emails that you send using the configuration set.
open class DeliveryOptions {
    TlsPolicy: (String(matches(Regex(#"REQUIRE|OPTIONAL"#)))|Mapping)?
    SendingPoolName: (String|Mapping)?
}

/// An object that defines whether or not Amazon SES collects reputation metrics for the emails that you send that use the configuration set.
open class ReputationOptions {
    ReputationMetricsEnabled: (Boolean|Mapping)?
}

/// An object that defines whether or not Amazon SES can send email that you send using the configuration set.
open class SendingOptions {
    SendingEnabled: (Boolean|Mapping)?
}

/// An object that contains information about the suppression list preferences for your account.
open class SuppressionOptions {
    SuppressedReasons: (Listing<String|Mapping>)?
}

/// Preferences regarding the Dashboard feature.
open class DashboardOptions {
    EngagementMetrics: String(matches(Regex(#"ENABLED|DISABLED"#)))|Mapping
}

/// Preferences regarding the Guardian feature.
open class GuardianOptions {
    OptimizedSharedDelivery: String(matches(Regex(#"ENABLED|DISABLED"#)))|Mapping
}

/// Resource schema for AWS::SES::ConfigurationSet.
open class ConfigurationSet extends cloudformation.Resource {

    Type = "AWS::SES::ConfigurationSet"


    /// 
    hidden DeliveryOptions: (DeliveryOptions)?

    /// 
    hidden ReputationOptions: (ReputationOptions)?

    /// 
    hidden SendingOptions: (SendingOptions)?

    /// 
    hidden SuppressionOptions: (SuppressionOptions)?

    /// 
    hidden VdmOptions: (VdmOptions)?

    /// The name of the configuration set.
    hidden Name: (String(matches(Regex(#"^[a-zA-Z0-9_-]{1,64}$"#)))|Mapping)?

    /// 
    hidden TrackingOptions: (TrackingOptions)?

    Properties {
        ["DeliveryOptions"] = if (DeliveryOptions == null) null else DeliveryOptions
        ["ReputationOptions"] = if (ReputationOptions == null) null else ReputationOptions
        ["SendingOptions"] = if (SendingOptions == null) null else SendingOptions
        ["SuppressionOptions"] = if (SuppressionOptions == null) null else SuppressionOptions
        ["VdmOptions"] = if (VdmOptions == null) null else VdmOptions
        ["Name"] = if (Name == null) null else Name
        ["TrackingOptions"] = if (TrackingOptions == null) null else TrackingOptions
    }

}
