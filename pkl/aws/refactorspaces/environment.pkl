/// AWS::RefactorSpaces::Environment
///
/// Generated by rain build --pkl-class AWS::RefactorSpaces::Environment
module aws.refactorspaces.environment

import "../../cloudformation.pkl"

typealias EnvironmentNetworkFabricType = String|Mapping


/// A label for tagging Environment resource
open class EnvironmentTag {
    Key: String(matches(Regex(#"^(?!aws:).+"#)))|Mapping
    Value: String|Mapping
}

/// Definition of AWS::RefactorSpaces::Environment Resource Type
open class Environment extends cloudformation.Resource {

    Type = "AWS::RefactorSpaces::Environment"


    /// 
    hidden Description: (String(matches(Regex(#"^[a-zA-Z0-9-_\s\.\!\*\#\@\']+$"#)))|Mapping)?

    /// 
    hidden Name: String(matches(Regex(#"^(?!env-)[a-zA-Z0-9]+[a-zA-Z0-9-_ ]+$"#)))|Mapping

    /// 
    hidden NetworkFabricType: EnvironmentNetworkFabricType

    /// Metadata that you can assign to help organize the frameworks that you create. Each tag is a key-value pair.
    hidden Tags: (Listing<EnvironmentTag>)?

    Properties {
        ["Description"] = if (Description == null) null else Description
        ["Name"] = if (Name == null) null else Name
        ["NetworkFabricType"] = if (NetworkFabricType == null) null else NetworkFabricType
        ["Tags"] = if (Tags == null) null else Tags
    }

}
