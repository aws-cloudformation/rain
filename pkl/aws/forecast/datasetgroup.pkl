/// AWS::Forecast::DatasetGroup
///
/// Generated by rain build --pkl-class AWS::Forecast::DatasetGroup
module aws.forecast.datasetgroup

import "../../cloudformation.pkl"

typealias DatasetGroupArn = String|Mapping


typealias DatasetGroupMaxResults = Int|Mapping


typealias DatasetGroupNextToken = String|Mapping


/// A key-value pair to associate with a resource.
open class DatasetGroupTag {
    Key: String|Mapping
    Value: String|Mapping
}

/// Represents a dataset group that holds a collection of related datasets
open class DatasetGroup extends cloudformation.Resource {

    Type = "AWS::Forecast::DatasetGroup"


    /// An array of Amazon Resource Names (ARNs) of the datasets that you want to include in the dataset group.
    hidden DatasetArns: (Listing<DatasetGroupArn>)?

    /// A name for the dataset group.
    hidden DatasetGroupName: String(matches(Regex(#"^[a-zA-Z][a-zA-Z0-9_]*"#)))|Mapping

    /// The domain associated with the dataset group. When you add a dataset to a dataset group, this value and the value specified for the Domain parameter of the CreateDataset operation must match.
    hidden Domain: DatasetGroupDomain|Mapping

    /// The tags of Application Insights application.
    hidden Tags: (Listing<DatasetGroupTag>)?

    Properties {
        ["DatasetArns"] = if (DatasetArns == null) null else DatasetArns
        ["DatasetGroupName"] = if (DatasetGroupName == null) null else DatasetGroupName
        ["Domain"] = if (Domain == null) null else Domain
        ["Tags"] = if (Tags == null) null else Tags
    }

}


typealias DatasetGroupDomain = "RETAIL"|"CUSTOM"|"INVENTORY_PLANNING"|"EC2_CAPACITY"|"WORK_FORCE"|"WEB_TRAFFIC"|"METRICS"
