/// AWS::KafkaConnect::CustomPlugin
///
/// Generated by rain build --pkl-class AWS::KafkaConnect::CustomPlugin
module aws.kafkaconnect.customplugin

import "../../cloudformation.pkl"

/// Details about the custom plugin file.
open class CustomPluginCustomPluginFileDescription {
    FileMd5: (String|Mapping)?
    FileSize: (Int|Mapping)?
}

/// Information about the location of a custom plugin.
open class CustomPluginCustomPluginLocation {
    S3Location: CustomPluginS3Location
}

/// The S3 bucket Amazon Resource Name (ARN), file key, and object version of the plugin file stored in Amazon S3.
open class CustomPluginS3Location {
    BucketArn: String|Mapping
    FileKey: String|Mapping
    ObjectVersion: (String|Mapping)?
}

/// A key-value pair to associate with a resource.
open class CustomPluginTag {
    Key: String|Mapping
    Value: String|Mapping
}

/// An example resource schema demonstrating some basic constructs and validation rules.
open class CustomPlugin extends cloudformation.Resource {

    Type = "AWS::KafkaConnect::CustomPlugin"


    /// The type of the plugin file.
    hidden ContentType: CustomPluginContentType|Mapping

    /// A summary description of the custom plugin.
    hidden Description: (String|Mapping)?

    /// 
    hidden Location: CustomPluginCustomPluginLocation

    /// The name of the custom plugin.
    hidden Name: String|Mapping

    /// An array of key-value pairs to apply to this resource.
    hidden Tags: (Listing<CustomPluginTag>)?

    Properties {
        ["ContentType"] = if (ContentType == null) null else ContentType
        ["Description"] = if (Description == null) null else Description
        ["Location"] = if (Location == null) null else Location
        ["Name"] = if (Name == null) null else Name
        ["Tags"] = if (Tags == null) null else Tags
    }

}


typealias CustomPluginContentType = "JAR"|"ZIP"
