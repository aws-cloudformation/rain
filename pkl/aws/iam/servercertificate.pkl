/// AWS::IAM::ServerCertificate
///
/// Generated by rain build --pkl-class AWS::IAM::ServerCertificate
module aws.iam.servercertificate

import "../../cloudformation.pkl"

/// A key-value pair to associate with a resource.
open class ServerCertificateTag {
    Value: String|Mapping
    Key: String|Mapping
}

/// Resource Type definition for AWS::IAM::ServerCertificate
open class ServerCertificate extends cloudformation.Resource {

    Type = "AWS::IAM::ServerCertificate"


    /// 
    hidden PrivateKey: (String(matches(Regex(#"[\u0009\u000A\u000D\u0020-\u00FF]+"#)))|Mapping)?

    /// 
    hidden Tags: (Listing<ServerCertificateTag>)?

    /// 
    hidden CertificateBody: (String(matches(Regex(#"[\u0009\u000A\u000D\u0020-\u00FF]+"#)))|Mapping)?

    /// 
    hidden CertificateChain: (String(matches(Regex(#"[\u0009\u000A\u000D\u0020-\u00FF]+"#)))|Mapping)?

    /// 
    hidden ServerCertificateName: (String(matches(Regex(#"[\w+=,.@-]+"#)))|Mapping)?

    /// 
    hidden Path: (String(matches(Regex(#"(\u002F)|(\u002F[\u0021-\u007F]+\u002F)"#)))|Mapping)?

    Properties {
        ["PrivateKey"] = if (PrivateKey == null) null else PrivateKey
        ["Tags"] = if (Tags == null) null else Tags
        ["CertificateBody"] = if (CertificateBody == null) null else CertificateBody
        ["CertificateChain"] = if (CertificateChain == null) null else CertificateChain
        ["ServerCertificateName"] = if (ServerCertificateName == null) null else ServerCertificateName
        ["Path"] = if (Path == null) null else Path
    }

}
