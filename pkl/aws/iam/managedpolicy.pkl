/// AWS::IAM::ManagedPolicy
///
/// Generated by rain build --pkl-class AWS::IAM::ManagedPolicy
module aws.iam.managedpolicy

import "../../cloudformation.pkl"

/// Resource Type definition for AWS::IAM::ManagedPolicy
open class ManagedPolicy extends cloudformation.Resource {

    Type = "AWS::IAM::ManagedPolicy"


    /// A friendly description of the policy.
    hidden Description: (String|Mapping)?

    /// The name (friendly name, not ARN) of the group to attach the policy to.
    hidden Groups: (Listing<String|Mapping>)?

    /// The friendly name of the policy.
    hidden ManagedPolicyName: (String|Mapping)?

    /// The path for the policy.
    hidden Path: (String|Mapping)?

    /// The JSON policy document that you want to use as the content for the new policy.
    hidden PolicyDocument: Dynamic

    /// The name (friendly name, not ARN) of the role to attach the policy to.
    hidden Roles: (Listing<String|Mapping>)?

    /// The name (friendly name, not ARN) of the IAM user to attach the policy to.
    hidden Users: (Listing<String|Mapping>)?

    Properties {
        ["Description"] = if (Description == null) null else Description
        ["Groups"] = if (Groups == null) null else Groups
        ["ManagedPolicyName"] = if (ManagedPolicyName == null) null else ManagedPolicyName
        ["Path"] = if (Path == null) null else Path
        ["PolicyDocument"] = if (PolicyDocument == null) null else PolicyDocument
        ["Roles"] = if (Roles == null) null else Roles
        ["Users"] = if (Users == null) null else Users
    }

}
