/// AWS::IAM::AccessKey
///
/// Generated by rain build --pkl-class AWS::IAM::AccessKey
module aws.iam.accesskey

import "../../cloudformation.pkl"

/// Resource Type definition for AWS::IAM::AccessKey
open class AccessKey extends cloudformation.Resource {

    Type = "AWS::IAM::AccessKey"


    /// 
    hidden Serial: (Int|Mapping)?

    /// 
    hidden Status: (String|Mapping)?

    /// 
    hidden UserName: String|Mapping

    Properties {
        ["Serial"] = if (Serial == null) null else Serial
        ["Status"] = if (Status == null) null else Status
        ["UserName"] = if (UserName == null) null else UserName
    }

}
