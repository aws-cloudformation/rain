/// AWS::IAM::SAMLProvider
///
/// Generated by rain build --pkl-class AWS::IAM::SAMLProvider
module aws.iam.samlprovider

import "../../cloudformation.pkl"

/// A key-value pair to associate with a resource.
open class SAMLProviderTag {
    Key: String|Mapping
    Value: String|Mapping
}

/// Resource Type definition for AWS::IAM::SAMLProvider
open class SAMLProvider extends cloudformation.Resource {

    Type = "AWS::IAM::SAMLProvider"


    /// 
    hidden Name: (String(matches(Regex(#"[\w._-]+"#)))|Mapping)?

    /// 
    hidden SamlMetadataDocument: String|Mapping

    /// 
    hidden Tags: (Listing<SAMLProviderTag>)?

    Properties {
        ["Name"] = if (Name == null) null else Name
        ["SamlMetadataDocument"] = if (SamlMetadataDocument == null) null else SamlMetadataDocument
        ["Tags"] = if (Tags == null) null else Tags
    }

}
