/// AWS::Transfer::Agreement
///
/// Generated by rain build --pkl-class AWS::Transfer::Agreement
module aws.transfer.agreement

import "../../cloudformation.pkl"

/// Creates a key-value pair for a specific resource.
open class Tag {
    Key: String|Mapping
    Value: String|Mapping
}

/// Resource Type definition for AWS::Transfer::Agreement
open class Agreement extends cloudformation.Resource {

    Type = "AWS::Transfer::Agreement"


    /// Specifies the base directory for the agreement.
    hidden BaseDirectory: String(matches(Regex(#"^$|/.*"#)))|Mapping

    /// A textual description for the agreement.
    hidden Description: (String(matches(Regex(#"^[\w\- ]*$"#)))|Mapping)?

    /// A unique identifier for the server.
    hidden ServerId: String(matches(Regex(#"^s-([0-9a-f]{17})$"#)))|Mapping

    /// A unique identifier for the partner profile.
    hidden PartnerProfileId: String(matches(Regex(#"^p-([0-9a-f]{17})$"#)))|Mapping

    /// Key-value pairs that can be used to group and search for agreements. Tags are metadata attached to agreements for any purpose.
    hidden Tags: (Listing<Tag>)?

    /// A unique identifier for the local profile.
    hidden LocalProfileId: String(matches(Regex(#"^p-([0-9a-f]{17})$"#)))|Mapping

    /// Specifies the access role for the agreement.
    hidden AccessRole: String(matches(Regex(#"arn:.*role/.*"#)))|Mapping

    /// Specifies the status of the agreement.
    hidden Status: (AgreementStatus|Mapping)?

    Properties {
        ["BaseDirectory"] = if (BaseDirectory == null) null else BaseDirectory
        ["Description"] = if (Description == null) null else Description
        ["ServerId"] = if (ServerId == null) null else ServerId
        ["PartnerProfileId"] = if (PartnerProfileId == null) null else PartnerProfileId
        ["Tags"] = if (Tags == null) null else Tags
        ["LocalProfileId"] = if (LocalProfileId == null) null else LocalProfileId
        ["AccessRole"] = if (AccessRole == null) null else AccessRole
        ["Status"] = if (Status == null) null else Status
    }

}


typealias AgreementStatus = "ACTIVE"|"INACTIVE"
