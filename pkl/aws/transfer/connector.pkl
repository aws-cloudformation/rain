/// AWS::Transfer::Connector
///
/// Generated by rain build --pkl-class AWS::Transfer::Connector
module aws.transfer.connector

import "../../cloudformation.pkl"

typealias ConnectorSftpConnectorTrustedHostKey = String|Mapping


/// Creates a key-value pair for a specific resource.
open class ConnectorTag {
    Key: String|Mapping
    Value: String|Mapping
}

/// Resource Type definition for AWS::Transfer::Connector
open class Connector extends cloudformation.Resource {

    Type = "AWS::Transfer::Connector"


    /// Specifies the access role for the connector.
    hidden AccessRole: String(matches(Regex(#"arn:.*role/.*"#)))|Mapping

    /// Configuration for an AS2 connector.
    hidden As2Config: (Dynamic)?

    /// Specifies the logging role for the connector.
    hidden LoggingRole: (String(matches(Regex(#"arn:.*role/.*"#)))|Mapping)?

    /// Configuration for an SFTP connector.
    hidden SftpConfig: (Dynamic)?

    /// Key-value pairs that can be used to group and search for connectors. Tags are metadata attached to connectors for any purpose.
    hidden Tags: (Listing<ConnectorTag>)?

    /// URL for Connector
    hidden Url: String|Mapping

    Properties {
        ["AccessRole"] = if (AccessRole == null) null else AccessRole
        ["As2Config"] = if (As2Config == null) null else As2Config
        ["LoggingRole"] = if (LoggingRole == null) null else LoggingRole
        ["SftpConfig"] = if (SftpConfig == null) null else SftpConfig
        ["Tags"] = if (Tags == null) null else Tags
        ["Url"] = if (Url == null) null else Url
    }

}
