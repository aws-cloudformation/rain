/// AWS::SSMIncidents::ReplicationSet
///
/// Generated by rain build --pkl-class AWS::SSMIncidents::ReplicationSet
module aws.ssmincidents.replicationset

import "../../cloudformation.pkl"

typealias ReplicationSetDeletionProtected = Boolean|Mapping


typealias ReplicationSetRegionList = Listing<(ReplicationSetReplicationRegion)?>


/// A key-value pair to tag a resource.
open class ReplicationSetTag {
    Key: String(matches(Regex(#"^(?!aws:)[a-zA-Z+-=._:/]+$"#)))|Mapping
    Value: String|Mapping
}

typealias ReplicationSetArn = String|Mapping


typealias ReplicationSetRegionName = String|Mapping


/// The ReplicationSet regional configuration.
open class ReplicationSetReplicationRegion {
    RegionName: (ReplicationSetRegionName)?
    RegionConfiguration: (ReplicationSetRegionConfiguration)?
}

/// The ReplicationSet regional configuration.
open class ReplicationSetRegionConfiguration {
    SseKmsKeyId: ReplicationSetArn
}

/// Resource type definition for AWS::SSMIncidents::ReplicationSet
open class ReplicationSet extends cloudformation.Resource {

    Type = "AWS::SSMIncidents::ReplicationSet"


    /// The tags to apply to the replication set.
    hidden Tags: (Listing<ReplicationSetTag>)?

    /// The ReplicationSet configuration.
    hidden Regions: ReplicationSetRegionList

    /// 
    hidden DeletionProtected: (ReplicationSetDeletionProtected)?

    Properties {
        ["Tags"] = if (Tags == null) null else Tags
        ["Regions"] = if (Regions == null) null else Regions
        ["DeletionProtected"] = if (DeletionProtected == null) null else DeletionProtected
    }

}
