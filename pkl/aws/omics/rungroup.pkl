/// AWS::Omics::RunGroup
///
/// Generated by rain build --pkl-class AWS::Omics::RunGroup
module aws.omics.rungroup

import "../../cloudformation.pkl"

/// A map of resource tags
open class TagMap {
}

/// Definition of AWS::Omics::RunGroup Resource Type
open class RunGroup extends cloudformation.Resource {

    Type = "AWS::Omics::RunGroup"


    /// 
    hidden Tags: (TagMap)?

    /// 
    hidden MaxCpus: (Number|Mapping)?

    /// 
    hidden MaxDuration: (Number|Mapping)?

    /// 
    hidden Name: (String(matches(Regex(#"^[\p{L}||\p{M}||\p{Z}||\p{S}||\p{N}||\p{P}]+$"#)))|Mapping)?

    /// 
    hidden MaxGpus: (Number|Mapping)?

    /// 
    hidden MaxRuns: (Number|Mapping)?

    Properties {
        ["Tags"] = if (Tags == null) null else Tags
        ["MaxCpus"] = if (MaxCpus == null) null else MaxCpus
        ["MaxDuration"] = if (MaxDuration == null) null else MaxDuration
        ["Name"] = if (Name == null) null else Name
        ["MaxGpus"] = if (MaxGpus == null) null else MaxGpus
        ["MaxRuns"] = if (MaxRuns == null) null else MaxRuns
    }

}
