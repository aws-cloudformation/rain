/// AWS::Detective::MemberInvitation
///
/// Generated by rain build --pkl-class AWS::Detective::MemberInvitation
module aws.detective.memberinvitation

import "../../cloudformation.pkl"

/// Resource schema for AWS::Detective::MemberInvitation
open class MemberInvitation extends cloudformation.Resource {

    Type = "AWS::Detective::MemberInvitation"


    /// When set to true, invitation emails are not sent to the member accounts. Member accounts must still accept the invitation before they are added to the behavior graph. Updating this field has no effect.
    hidden DisableEmailNotification: (Boolean|Mapping)?

    /// The ARN of the graph to which the member account will be invited
    hidden GraphArn: String(matches(Regex(#"arn:aws(-[\w]+)*:detective:(([a-z]+-)+[0-9]+):[0-9]{12}:graph:[0-9a-f]{32}"#)))|Mapping

    /// The root email address for the account to be invited, for validation. Updating this field has no effect.
    hidden MemberEmailAddress: String(matches(Regex(#".*@.*"#)))|Mapping

    /// The AWS account ID to be invited to join the graph as a member
    hidden MemberId: String(matches(Regex(#"[0-9]{12}"#)))|Mapping

    /// A message to be included in the email invitation sent to the invited account. Updating this field has no effect.
    hidden Message: (String|Mapping)?

    Properties {
        ["DisableEmailNotification"] = if (DisableEmailNotification == null) null else DisableEmailNotification
        ["GraphArn"] = if (GraphArn == null) null else GraphArn
        ["MemberEmailAddress"] = if (MemberEmailAddress == null) null else MemberEmailAddress
        ["MemberId"] = if (MemberId == null) null else MemberId
        ["Message"] = if (Message == null) null else Message
    }

}
