/// AWS::Glue::Workflow
///
/// Generated by rain build --pkl-class AWS::Glue::Workflow
module aws.glue.workflow

import "../../cloudformation.pkl"

/// Resource Type definition for AWS::Glue::Workflow
open class Workflow extends cloudformation.Resource {

    Type = "AWS::Glue::Workflow"


    /// 
    hidden DefaultRunProperties: (Dynamic)?

    /// 
    hidden Description: (String|Mapping)?

    /// 
    hidden MaxConcurrentRuns: (Int|Mapping)?

    /// 
    hidden Name: (String|Mapping)?

    /// 
    hidden Tags: (Dynamic)?

    Properties {
        ["DefaultRunProperties"] = if (DefaultRunProperties == null) null else DefaultRunProperties
        ["Description"] = if (Description == null) null else Description
        ["MaxConcurrentRuns"] = if (MaxConcurrentRuns == null) null else MaxConcurrentRuns
        ["Name"] = if (Name == null) null else Name
        ["Tags"] = if (Tags == null) null else Tags
    }

}
