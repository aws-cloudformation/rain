/// AWS::Glue::SchemaVersion
///
/// Generated by rain build --pkl-class AWS::Glue::SchemaVersion
module aws.glue.schemaversion

import "../../cloudformation.pkl"

/// Identifier for the schema where the schema version will be created.
open class SchemaVersionSchema {
    RegistryName: (String|Mapping)?
    SchemaArn: (String(matches(Regex(#"arn:(aws|aws-us-gov|aws-cn):glue:.*"#)))|Mapping)?
    SchemaName: (String|Mapping)?
}

/// This resource represents an individual schema version of a schema defined in Glue Schema Registry.
open class SchemaVersion extends cloudformation.Resource {

    Type = "AWS::Glue::SchemaVersion"


    /// 
    hidden Schema: SchemaVersionSchema

    /// Complete definition of the schema in plain-text.
    hidden SchemaDefinition: String|Mapping

    Properties {
        ["Schema"] = if (Schema == null) null else Schema
        ["SchemaDefinition"] = if (SchemaDefinition == null) null else SchemaDefinition
    }

}
