/// AWS::ACMPCA::Permission
///
/// Generated by rain build --pkl-class AWS::ACMPCA::Permission
module aws.acmpca.permission

import "../../cloudformation.pkl"

/// Permission set on private certificate authority
open class Permission extends cloudformation.Resource {

    Type = "AWS::ACMPCA::Permission"


    /// The actions that the specified AWS service principal can use. Actions IssueCertificate, GetCertificate and ListPermissions must be provided.
    hidden Actions: Listing<String|Mapping>

    /// The Amazon Resource Name (ARN) of the Private Certificate Authority that grants the permission.
    hidden CertificateAuthorityArn: String|Mapping

    /// The AWS service or identity that receives the permission. At this time, the only valid principal is acm.amazonaws.com.
    hidden Principal: String|Mapping

    /// The ID of the calling account.
    hidden SourceAccount: (String|Mapping)?

    Properties {
        ["Actions"] = if (Actions == null) null else Actions
        ["CertificateAuthorityArn"] = if (CertificateAuthorityArn == null) null else CertificateAuthorityArn
        ["Principal"] = if (Principal == null) null else Principal
        ["SourceAccount"] = if (SourceAccount == null) null else SourceAccount
    }

}
