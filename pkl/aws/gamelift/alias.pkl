/// AWS::GameLift::Alias
///
/// Generated by rain build --pkl-class AWS::GameLift::Alias
module aws.gamelift.alias

import "../../cloudformation.pkl"

typealias RoutingStrategyType = "SIMPLE"|"TERMINAL"

/// 
open class AliasRoutingStrategy {
    FleetId: (String(matches(Regex(#"^fleet-\S+"#)))|Mapping)?
    Message: (String|Mapping)?
    Type: RoutingStrategyType|Mapping
}

/// The AWS::GameLift::Alias resource creates an alias for an Amazon GameLift (GameLift) fleet destination.
open class Alias extends cloudformation.Resource {

    Type = "AWS::GameLift::Alias"


    /// A human-readable description of the alias.
    hidden Description: (String|Mapping)?

    /// A descriptive label that is associated with an alias. Alias names do not need to be unique.
    hidden Name: String(matches(Regex(#".*\S.*"#)))|Mapping

    /// A routing configuration that specifies where traffic is directed for this alias, such as to a fleet or to a message.
    hidden RoutingStrategy: AliasRoutingStrategy

    Properties {
        ["Description"] = if (Description == null) null else Description
        ["Name"] = if (Name == null) null else Name
        ["RoutingStrategy"] = if (RoutingStrategy == null) null else RoutingStrategy
    }

}
