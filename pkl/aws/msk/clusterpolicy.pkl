/// AWS::MSK::ClusterPolicy
///
/// Generated by rain build --pkl-class AWS::MSK::ClusterPolicy
module aws.msk.clusterpolicy

import "../../cloudformation.pkl"

/// Resource Type definition for AWS::MSK::ClusterPolicy
open class ClusterPolicy extends cloudformation.Resource {

    Type = "AWS::MSK::ClusterPolicy"


    /// The arn of the cluster for the resource policy.
    hidden ClusterArn: String(matches(Regex(#"^arn:[\w-]+:kafka:[\w-]+:\d+:cluster.*\Z"#)))|Mapping

    /// A policy document containing permissions to add to the specified cluster.
    hidden Policy: Dynamic

    Properties {
        ["ClusterArn"] = if (ClusterArn == null) null else ClusterArn
        ["Policy"] = if (Policy == null) null else Policy
    }

}
