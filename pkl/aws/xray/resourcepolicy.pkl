/// AWS::XRay::ResourcePolicy
///
/// Generated by rain build --pkl-class AWS::XRay::ResourcePolicy
module aws.xray.resourcepolicy

import "../../cloudformation.pkl"

/// This schema provides construct and validation rules for AWS-XRay Resource Policy resource parameters.
open class ResourcePolicy extends cloudformation.Resource {

    Type = "AWS::XRay::ResourcePolicy"


    /// A flag to indicate whether to bypass the resource policy lockout safety check
    hidden BypassPolicyLockoutCheck: (Boolean|Mapping)?

    /// The resource policy document, which can be up to 5kb in size.
    hidden PolicyDocument: String|Mapping

    /// The name of the resource policy. Must be unique within a specific AWS account.
    hidden PolicyName: String(matches(Regex(#"[\w+=,.@-]+"#)))|Mapping

    Properties {
        ["BypassPolicyLockoutCheck"] = if (BypassPolicyLockoutCheck == null) null else BypassPolicyLockoutCheck
        ["PolicyDocument"] = if (PolicyDocument == null) null else PolicyDocument
        ["PolicyName"] = if (PolicyName == null) null else PolicyName
    }

}
