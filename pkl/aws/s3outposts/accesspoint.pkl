/// AWS::S3Outposts::AccessPoint
///
/// Generated by rain build --pkl-class AWS::S3Outposts::AccessPoint
module aws.s3outposts.accesspoint

import "../../cloudformation.pkl"

/// 
open class AccessPointVpcConfiguration {
    VpcId: (String|Mapping)?
}

/// Resource Type Definition for AWS::S3Outposts::AccessPoint
open class AccessPoint extends cloudformation.Resource {

    Type = "AWS::S3Outposts::AccessPoint"


    /// The Amazon Resource Name (ARN) of the bucket you want to associate this AccessPoint with.
    hidden Bucket: String(matches(Regex(#"^arn:[^:]+:s3-outposts:[a-zA-Z0-9\-]+:\d{12}:outpost\/[^:]+\/bucket\/[^:]+$"#)))|Mapping

    /// A name for the AccessPoint.
    hidden Name: String(matches(Regex(#"^[a-z0-9]([a-z0-9\\-]*[a-z0-9])?$"#)))|Mapping

    /// The access point policy associated with this access point.
    hidden Policy: (Dynamic)?

    /// Virtual Private Cloud (VPC) from which requests can be made to the AccessPoint.
    hidden VpcConfiguration: AccessPointVpcConfiguration

    Properties {
        ["Bucket"] = if (Bucket == null) null else Bucket
        ["Name"] = if (Name == null) null else Name
        ["Policy"] = if (Policy == null) null else Policy
        ["VpcConfiguration"] = if (VpcConfiguration == null) null else VpcConfiguration
    }

}
