/// AWS::RAM::Permission
///
/// Generated by rain build --pkl-class AWS::RAM::Permission
module aws.ram.permission

import "../../cloudformation.pkl"

/// 
open class PermissionTag {
    Key: String|Mapping
    Value: String|Mapping
}

/// Resource type definition for AWS::RAM::Permission
open class Permission extends cloudformation.Resource {

    Type = "AWS::RAM::Permission"


    /// The name of the permission.
    hidden Name: String|Mapping

    /// Policy template for the permission.
    hidden PolicyTemplate: Dynamic

    /// The resource type this permission can be used with.
    hidden ResourceType: String|Mapping

    /// 
    hidden Tags: (Listing<PermissionTag>)?

    Properties {
        ["Name"] = if (Name == null) null else Name
        ["PolicyTemplate"] = if (PolicyTemplate == null) null else PolicyTemplate
        ["ResourceType"] = if (ResourceType == null) null else ResourceType
        ["Tags"] = if (Tags == null) null else Tags
    }

}
