/// AWS::Connect::ContactFlow
///
/// Generated by rain build --pkl-class AWS::Connect::ContactFlow
module aws.connect.contactflow

import "../../cloudformation.pkl"

/// A key-value pair to associate with a resource.
open class ContactFlowTag {
    Key: String(matches(Regex(#"^(?!aws:)[a-zA-Z+-=._:/]+$"#)))|Mapping
    Value: String|Mapping
}

/// Resource Type definition for AWS::Connect::ContactFlow
open class ContactFlow extends cloudformation.Resource {

    Type = "AWS::Connect::ContactFlow"


    /// The content of the contact flow in JSON format.
    hidden Content: String|Mapping

    /// The description of the contact flow.
    hidden Description: (String|Mapping)?

    /// The identifier of the Amazon Connect instance (ARN).
    hidden InstanceArn: String(matches(Regex(#"^arn:aws[-a-z0-9]*:connect:[-a-z0-9]*:[0-9]{12}:instance/[-a-zA-Z0-9]*$"#)))|Mapping

    /// The name of the contact flow.
    hidden Name: String|Mapping

    /// The state of the contact flow.
    hidden State: (ContactFlowState|Mapping)?

    /// One or more tags.
    hidden Tags: (Listing<ContactFlowTag>)?

    /// The type of the contact flow.
    hidden TypeProperty: ContactFlowType|Mapping

    Properties {
        ["Content"] = if (Content == null) null else Content
        ["Description"] = if (Description == null) null else Description
        ["InstanceArn"] = if (InstanceArn == null) null else InstanceArn
        ["Name"] = if (Name == null) null else Name
        ["State"] = if (State == null) null else State
        ["Tags"] = if (Tags == null) null else Tags
        ["Type"] = if (TypeProperty == null) null else TypeProperty
    }

}


typealias ContactFlowState = "ACTIVE"|"ARCHIVED"

typealias ContactFlowType = "CONTACT_FLOW"|"CUSTOMER_QUEUE"|"CUSTOMER_HOLD"|"CUSTOMER_WHISPER"|"AGENT_HOLD"|"AGENT_WHISPER"|"OUTBOUND_WHISPER"|"AGENT_TRANSFER"|"QUEUE_TRANSFER"
