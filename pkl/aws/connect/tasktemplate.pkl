/// AWS::Connect::TaskTemplate
///
/// Generated by rain build --pkl-class AWS::Connect::TaskTemplate
module aws.connect.tasktemplate

import "../../cloudformation.pkl"

typealias TaskTemplateInvisibleTaskTemplateFields = Listing<(TaskTemplateInvisibleFieldInfo)?>


typealias TaskTemplateRequiredTaskTemplateFields = Listing<(TaskTemplateRequiredFieldInfo)?>


typealias TaskTemplateClientToken = String|Mapping


typealias TaskTemplateFieldOption = String|Mapping


typealias TaskTemplateReadOnlyTaskTemplateFields = Listing<(TaskTemplateReadOnlyFieldInfo)?>


/// A key-value pair to associate with a resource.
open class TaskTemplateTag {
    Key: String(matches(Regex(#"^(?!aws:)[a-zA-Z+-=._:/]+$"#)))|Mapping
    Value: String|Mapping
}

/// the default value for the task template's field
open class TaskTemplateDefaultFieldValue {
    Id: TaskTemplateFieldIdentifier
    DefaultValue: TaskTemplateFieldValue
}

/// A task template field object.
open class TaskTemplateField {
    Id: TaskTemplateFieldIdentifier
    Description: (String|Mapping)?
    Type: TaskTemplateFieldType
    SingleSelectOptions: (Listing<TaskTemplateFieldOption>)?
}

/// the identifier (name) for the task template field
open class TaskTemplateFieldIdentifier {
    Name: String|Mapping
}

typealias TaskTemplateFieldValue = String|Mapping


typealias TaskTemplateStatus = String|Mapping


typealias TaskTemplateFieldType = String|Mapping


/// Invisible field info
open class TaskTemplateInvisibleFieldInfo {
    Id: TaskTemplateFieldIdentifier
}

/// ReadOnly field info
open class TaskTemplateReadOnlyFieldInfo {
    Id: TaskTemplateFieldIdentifier
}

/// Required field info
open class TaskTemplateRequiredFieldInfo {
    Id: TaskTemplateFieldIdentifier
}

/// Resource Type definition for AWS::Connect::TaskTemplate.
open class TaskTemplate extends cloudformation.Resource {

    Type = "AWS::Connect::TaskTemplate"


    /// 
    hidden ClientToken: (TaskTemplateClientToken)?

    /// The constraints for the task template
    hidden Constraints: (Dynamic)?

    /// The identifier of the contact flow.
    hidden ContactFlowArn: (String(matches(Regex(#"^$|arn:aws[-a-z0-9]*:connect:[-a-z0-9]*:[0-9]{12}:instance/[-a-zA-Z0-9]*/contact-flow/[-a-zA-Z0-9]*$"#)))|Mapping)?

    /// 
    hidden Defaults: (Listing<TaskTemplateDefaultFieldValue>)?

    /// The description of the task template.
    hidden Description: (String|Mapping)?

    /// The list of task template's fields
    hidden Fields: (Listing<TaskTemplateField>)?

    /// The identifier (arn) of the instance.
    hidden InstanceArn: String(matches(Regex(#"^arn:aws[-a-z0-9]*:connect:[-a-z0-9]*:[0-9]{12}:instance/[-a-zA-Z0-9]*$"#)))|Mapping

    /// The name of the task template.
    hidden Name: (String|Mapping)?

    /// 
    hidden Status: (TaskTemplateStatus)?

    /// One or more tags.
    hidden Tags: (Listing<TaskTemplateTag>)?

    Properties {
        ["ClientToken"] = if (ClientToken == null) null else ClientToken
        ["Constraints"] = if (Constraints == null) null else Constraints
        ["ContactFlowArn"] = if (ContactFlowArn == null) null else ContactFlowArn
        ["Defaults"] = if (Defaults == null) null else Defaults
        ["Description"] = if (Description == null) null else Description
        ["Fields"] = if (Fields == null) null else Fields
        ["InstanceArn"] = if (InstanceArn == null) null else InstanceArn
        ["Name"] = if (Name == null) null else Name
        ["Status"] = if (Status == null) null else Status
        ["Tags"] = if (Tags == null) null else Tags
    }

}
