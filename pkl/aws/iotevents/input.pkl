/// AWS::IoTEvents::Input
///
/// Generated by rain build --pkl-class AWS::IoTEvents::Input
module aws.iotevents.input

import "../../cloudformation.pkl"

/// The attributes from the JSON payload that are made available by the input. Inputs are derived from messages sent to the AWS IoT Events system using `BatchPutMessage`. Each such message contains a JSON payload, and those attributes (and their paired values) specified here are available for use in the `condition` expressions used by detectors that monitor this input.
open class InputAttribute {
    JsonPath: String(matches(Regex(#"^((`[a-zA-Z0-9_\- ]+`)|([a-zA-Z0-9_\-]+))(\.((`[a-zA-Z0-9_\- ]+`)|([a-zA-Z0-9_\-]+)))*$"#)))|Mapping
}

/// The definition of the input.
open class InputInputDefinition {
    Attributes: Listing<InputAttribute>
}

/// Tags to be applied to Input.
open class InputTag {
    Key: String|Mapping
    Value: String|Mapping
}

/// The AWS::IoTEvents::Input resource creates an input. To monitor your devices and processes, they must have a way to get telemetry data into AWS IoT Events. This is done by sending messages as *inputs* to AWS IoT Events. For more information, see [How to Use AWS IoT Events](https://docs.aws.amazon.com/iotevents/latest/developerguide/how-to-use-iotevents.html) in the *AWS IoT Events Developer Guide*.
open class Input extends cloudformation.Resource {

    Type = "AWS::IoTEvents::Input"


    /// 
    hidden InputDefinition: InputInputDefinition

    /// A brief description of the input.
    hidden InputDescription: (String|Mapping)?

    /// The name of the input.
    hidden InputName: (String(matches(Regex(#"^[a-zA-Z][a-zA-Z0-9_]*$"#)))|Mapping)?

    /// An array of key-value pairs to apply to this resource.
    ///
    /// 
    /// For more information, see [Tag](https://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/aws-properties-resource-tags.html).
    hidden Tags: (Listing<InputTag>)?

    Properties {
        ["InputDefinition"] = if (InputDefinition == null) null else InputDefinition
        ["InputDescription"] = if (InputDescription == null) null else InputDescription
        ["InputName"] = if (InputName == null) null else InputName
        ["Tags"] = if (Tags == null) null else Tags
    }

}
