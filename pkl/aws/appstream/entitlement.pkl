/// AWS::AppStream::Entitlement
///
/// Generated by rain build --pkl-class AWS::AppStream::Entitlement
module aws.appstream.entitlement

import "../../cloudformation.pkl"

/// 
open class EntitlementAttribute {
    Name: String|Mapping
    Value: String|Mapping
}

/// Resource Type definition for AWS::AppStream::Entitlement
open class Entitlement extends cloudformation.Resource {

    Type = "AWS::AppStream::Entitlement"


    /// 
    hidden StackName: String|Mapping

    /// 
    hidden Description: (String|Mapping)?

    /// 
    hidden AppVisibility: String|Mapping

    /// 
    hidden Attributes: Listing<EntitlementAttribute>

    /// 
    hidden Name: String|Mapping

    Properties {
        ["StackName"] = if (StackName == null) null else StackName
        ["Description"] = if (Description == null) null else Description
        ["AppVisibility"] = if (AppVisibility == null) null else AppVisibility
        ["Attributes"] = if (Attributes == null) null else Attributes
        ["Name"] = if (Name == null) null else Name
    }

}
