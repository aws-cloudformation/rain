/// AWS::AppStream::User
///
/// Generated by rain build --pkl-class AWS::AppStream::User
module aws.appstream.user

import "../../cloudformation.pkl"

/// Resource Type definition for AWS::AppStream::User
open class User extends cloudformation.Resource {

    Type = "AWS::AppStream::User"


    /// 
    hidden AuthenticationType: String|Mapping

    /// 
    hidden FirstName: (String|Mapping)?

    /// 
    hidden LastName: (String|Mapping)?

    /// 
    hidden MessageAction: (String|Mapping)?

    /// 
    hidden UserName: String|Mapping

    Properties {
        ["AuthenticationType"] = if (AuthenticationType == null) null else AuthenticationType
        ["FirstName"] = if (FirstName == null) null else FirstName
        ["LastName"] = if (LastName == null) null else LastName
        ["MessageAction"] = if (MessageAction == null) null else MessageAction
        ["UserName"] = if (UserName == null) null else UserName
    }

}
