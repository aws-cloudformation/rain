/// AWS::Scheduler::ScheduleGroup
///
/// Generated by rain build --pkl-class AWS::Scheduler::ScheduleGroup
module aws.scheduler.schedulegroup

import "../../cloudformation.pkl"

typealias ScheduleGroupScheduleGroupState = String|Mapping


/// Tag to associate with the resource.
open class ScheduleGroupTag {
    Key: String|Mapping
    Value: String|Mapping
}

/// Definition of AWS::Scheduler::ScheduleGroup Resource Type
open class ScheduleGroup extends cloudformation.Resource {

    Type = "AWS::Scheduler::ScheduleGroup"


    /// 
    hidden Name: (String(matches(Regex(#"^[0-9a-zA-Z-_.]+$"#)))|Mapping)?

    /// The list of tags to associate with the schedule group.
    hidden Tags: (Listing<ScheduleGroupTag>)?

    Properties {
        ["Name"] = if (Name == null) null else Name
        ["Tags"] = if (Tags == null) null else Tags
    }

}
