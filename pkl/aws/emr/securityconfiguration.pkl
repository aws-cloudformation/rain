/// AWS::EMR::SecurityConfiguration
///
/// Generated by rain build --pkl-class AWS::EMR::SecurityConfiguration
module aws.emr.securityconfiguration

import "../../cloudformation.pkl"

/// Use a SecurityConfiguration resource to configure data encryption, Kerberos authentication, and Amazon S3 authorization for EMRFS.
open class SecurityConfiguration extends cloudformation.Resource {

    Type = "AWS::EMR::SecurityConfiguration"


    /// The name of the security configuration.
    hidden Name: (String|Mapping)?

    /// The security configuration details in JSON format.
    hidden SecurityConfiguration: Dynamic

    Properties {
        ["Name"] = if (Name == null) null else Name
        ["SecurityConfiguration"] = if (SecurityConfiguration == null) null else SecurityConfiguration
    }

}
