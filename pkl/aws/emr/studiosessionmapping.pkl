/// AWS::EMR::StudioSessionMapping
///
/// Generated by rain build --pkl-class AWS::EMR::StudioSessionMapping
module aws.emr.studiosessionmapping

import "../../cloudformation.pkl"

typealias StudioSessionMappingIamPolicyArn = String|Mapping


/// An example resource schema demonstrating some basic constructs and validation rules.
open class StudioSessionMapping extends cloudformation.Resource {

    Type = "AWS::EMR::StudioSessionMapping"


    /// The name of the user or group. For more information, see UserName and DisplayName in the AWS SSO Identity Store API Reference. Either IdentityName or IdentityId must be specified.
    hidden IdentityName: String|Mapping

    /// Specifies whether the identity to map to the Studio is a user or a group.
    hidden IdentityType: StudioSessionMappingIdentityType|Mapping

    /// The Amazon Resource Name (ARN) for the session policy that will be applied to the user or group. Session policies refine Studio user permissions without the need to use multiple IAM user roles.
    hidden SessionPolicyArn: StudioSessionMappingIamPolicyArn

    /// The ID of the Amazon EMR Studio to which the user or group will be mapped.
    hidden StudioId: String(matches(Regex(#"^es-[0-9A-Z]+"#)))|Mapping

    Properties {
        ["IdentityName"] = if (IdentityName == null) null else IdentityName
        ["IdentityType"] = if (IdentityType == null) null else IdentityType
        ["SessionPolicyArn"] = if (SessionPolicyArn == null) null else SessionPolicyArn
        ["StudioId"] = if (StudioId == null) null else StudioId
    }

}


typealias StudioSessionMappingIdentityType = "USER"|"GROUP"
