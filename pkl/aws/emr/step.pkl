/// AWS::EMR::Step
///
/// Generated by rain build --pkl-class AWS::EMR::Step
module aws.emr.step

import "../../cloudformation.pkl"

/// 
open class StepKeyValue {
    Key: (String|Mapping)?
    Value: (String|Mapping)?
}

/// 
open class StepHadoopJarStepConfig {
    Args: (Listing<String|Mapping>)?
    Jar: String|Mapping
    MainClass: (String|Mapping)?
    StepProperties: (Listing<StepKeyValue>)?
}

/// Schema for AWS::EMR::Step
open class Step extends cloudformation.Resource {

    Type = "AWS::EMR::Step"


    /// The HadoopJarStepConfig property type specifies a job flow step consisting of a JAR file whose main function will be executed. The main function submits a job for the cluster to execute as a step on the master node, and then waits for the job to finish or fail before executing subsequent steps.
    hidden HadoopJarStep: StepHadoopJarStepConfig

    /// A string that uniquely identifies the cluster (job flow).
    hidden JobFlowId: String|Mapping

    /// The name of the cluster step.
    hidden Name: String|Mapping

    /// This specifies what action to take when the cluster step fails. Possible values are CANCEL_AND_WAIT and CONTINUE.
    hidden ActionOnFailure: String|Mapping

    Properties {
        ["HadoopJarStep"] = if (HadoopJarStep == null) null else HadoopJarStep
        ["JobFlowId"] = if (JobFlowId == null) null else JobFlowId
        ["Name"] = if (Name == null) null else Name
        ["ActionOnFailure"] = if (ActionOnFailure == null) null else ActionOnFailure
    }

}
