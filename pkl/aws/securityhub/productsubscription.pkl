/// AWS::SecurityHub::ProductSubscription
///
/// Generated by rain build --pkl-class AWS::SecurityHub::ProductSubscription
module aws.securityhub.productsubscription

import "../../cloudformation.pkl"

/// The AWS::SecurityHub::ProductSubscription resource represents a subscription to a service that is allowed to generate findings for your Security Hub account. One product subscription resource is created for each product enabled.
open class ProductSubscription extends cloudformation.Resource {

    Type = "AWS::SecurityHub::ProductSubscription"


    /// The generic ARN of the product being subscribed to
    hidden ProductArn: String(matches(Regex(#"arn:aws\S*:securityhub:\S*"#)))|Mapping

    Properties {
        ["ProductArn"] = if (ProductArn == null) null else ProductArn
    }

}
