/// AWS::SecurityHub::DelegatedAdmin
///
/// Generated by rain build --pkl-class AWS::SecurityHub::DelegatedAdmin
module aws.securityhub.delegatedadmin

import "../../cloudformation.pkl"

/// The AWS::SecurityHub::DelegatedAdmin resource represents the AWS Security Hub delegated admin account in your organization. One delegated admin resource is allowed to create for the organization in each region in which you configure the AdminAccountId.
open class DelegatedAdmin extends cloudformation.Resource {

    Type = "AWS::SecurityHub::DelegatedAdmin"


    /// The Amazon Web Services account identifier of the account to designate as the Security Hub administrator account
    hidden AdminAccountId: String(matches(Regex(#"^[0-9]{12}$"#)))|Mapping

    Properties {
        ["AdminAccountId"] = if (AdminAccountId == null) null else AdminAccountId
    }

}
