/// AWS::EC2::VPNGateway
///
/// Generated by rain build --pkl-class AWS::EC2::VPNGateway
module aws.ec2.vpngateway

import "../../cloudformation.pkl"

/// 
open class VPNGatewayTag {
    Key: String|Mapping
    Value: String|Mapping
}

/// Schema for EC2 VPN Gateway
open class VPNGateway extends cloudformation.Resource {

    Type = "AWS::EC2::VPNGateway"


    /// The private Autonomous System Number (ASN) for the Amazon side of a BGP session.
    hidden AmazonSideAsn: (Int|Mapping)?

    /// Any tags assigned to the virtual private gateway.
    hidden Tags: (Listing<VPNGatewayTag>)?

    /// The type of VPN connection the virtual private gateway supports.
    hidden TypeProperty: String|Mapping

    Properties {
        ["AmazonSideAsn"] = if (AmazonSideAsn == null) null else AmazonSideAsn
        ["Tags"] = if (Tags == null) null else Tags
        ["Type"] = if (TypeProperty == null) null else TypeProperty
    }

}
