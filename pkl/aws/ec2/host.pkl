/// AWS::EC2::Host
///
/// Generated by rain build --pkl-class AWS::EC2::Host
module aws.ec2.host

import "../../cloudformation.pkl"

/// Resource Type definition for AWS::EC2::Host
open class Host extends cloudformation.Resource {

    Type = "AWS::EC2::Host"


    /// The ID of the Outpost hardware asset.
    hidden AssetId: (String|Mapping)?

    /// Indicates whether the host accepts any untargeted instance launches that match its instance type configuration, or if it only accepts Host tenancy instance launches that specify its unique host ID.
    hidden AutoPlacement: (String|Mapping)?

    /// The Availability Zone in which to allocate the Dedicated Host.
    hidden AvailabilityZone: String|Mapping

    /// Automatically allocates a new dedicated host and moves your instances on to it if a degradation is detected on your current host.
    hidden HostMaintenance: (String|Mapping)?

    /// Indicates whether to enable or disable host recovery for the Dedicated Host. Host recovery is disabled by default.
    hidden HostRecovery: (String|Mapping)?

    /// Specifies the instance family to be supported by the Dedicated Hosts. If you specify an instance family, the Dedicated Hosts support multiple instance types within that instance family.
    hidden InstanceFamily: (String|Mapping)?

    /// Specifies the instance type to be supported by the Dedicated Hosts. If you specify an instance type, the Dedicated Hosts support instances of the specified instance type only.
    hidden InstanceType: (String|Mapping)?

    /// The Amazon Resource Name (ARN) of the Amazon Web Services Outpost on which to allocate the Dedicated Host.
    hidden OutpostArn: (String|Mapping)?

    Properties {
        ["AssetId"] = if (AssetId == null) null else AssetId
        ["AutoPlacement"] = if (AutoPlacement == null) null else AutoPlacement
        ["AvailabilityZone"] = if (AvailabilityZone == null) null else AvailabilityZone
        ["HostMaintenance"] = if (HostMaintenance == null) null else HostMaintenance
        ["HostRecovery"] = if (HostRecovery == null) null else HostRecovery
        ["InstanceFamily"] = if (InstanceFamily == null) null else InstanceFamily
        ["InstanceType"] = if (InstanceType == null) null else InstanceType
        ["OutpostArn"] = if (OutpostArn == null) null else OutpostArn
    }

}
