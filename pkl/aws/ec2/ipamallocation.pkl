/// AWS::EC2::IPAMAllocation
///
/// Generated by rain build --pkl-class AWS::EC2::IPAMAllocation
module aws.ec2.ipamallocation

import "../../cloudformation.pkl"

typealias IPAMAllocationCidr = String|Mapping


/// Resource Schema of AWS::EC2::IPAMAllocation Type
open class IPAMAllocation extends cloudformation.Resource {

    Type = "AWS::EC2::IPAMAllocation"


    /// 
    hidden Cidr: (IPAMAllocationCidr)?

    /// 
    hidden Description: (String|Mapping)?

    /// Id of the IPAM Pool.
    hidden IpamPoolId: String|Mapping

    /// The desired netmask length of the allocation. If set, IPAM will choose a block of free space with this size and return the CIDR representing it.
    hidden NetmaskLength: (Int|Mapping)?

    Properties {
        ["Cidr"] = if (Cidr == null) null else Cidr
        ["Description"] = if (Description == null) null else Description
        ["IpamPoolId"] = if (IpamPoolId == null) null else IpamPoolId
        ["NetmaskLength"] = if (NetmaskLength == null) null else NetmaskLength
    }

}
