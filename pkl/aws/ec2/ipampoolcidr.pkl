/// AWS::EC2::IPAMPoolCidr
///
/// Generated by rain build --pkl-class AWS::EC2::IPAMPoolCidr
module aws.ec2.ipampoolcidr

import "../../cloudformation.pkl"

/// Resource Schema of AWS::EC2::IPAMPoolCidr Type
open class IPAMPoolCidr extends cloudformation.Resource {

    Type = "AWS::EC2::IPAMPoolCidr"


    /// The desired netmask length of the provision. If set, IPAM will choose a block of free space with this size and return the CIDR representing it.
    hidden NetmaskLength: (Int|Mapping)?

    /// Id of the IPAM Pool.
    hidden IpamPoolId: String|Mapping

    /// Represents a single IPv4 or IPv6 CIDR
    hidden Cidr: (String|Mapping)?

    Properties {
        ["NetmaskLength"] = if (NetmaskLength == null) null else NetmaskLength
        ["IpamPoolId"] = if (IpamPoolId == null) null else IpamPoolId
        ["Cidr"] = if (Cidr == null) null else Cidr
    }

}
