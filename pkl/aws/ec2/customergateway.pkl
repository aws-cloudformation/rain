/// AWS::EC2::CustomerGateway
///
/// Generated by rain build --pkl-class AWS::EC2::CustomerGateway
module aws.ec2.customergateway

import "../../cloudformation.pkl"

/// 
open class CustomerGatewayTag {
    Key: String|Mapping
    Value: String|Mapping
}

/// Resource Type definition for AWS::EC2::CustomerGateway
open class CustomerGateway extends cloudformation.Resource {

    Type = "AWS::EC2::CustomerGateway"


    /// One or more tags for the customer gateway.
    hidden Tags: (Listing<CustomerGatewayTag>)?

    /// The type of VPN connection that this customer gateway supports.
    hidden TypeProperty: String|Mapping

    /// A name for the customer gateway device.
    hidden DeviceName: (String|Mapping)?

    /// For devices that support BGP, the customer gateway's BGP ASN.
    hidden BgpAsn: Int|Mapping

    /// The internet-routable IP address for the customer gateway's outside interface. The address must be static.
    hidden IpAddress: String|Mapping

    Properties {
        ["Tags"] = if (Tags == null) null else Tags
        ["Type"] = if (TypeProperty == null) null else TypeProperty
        ["DeviceName"] = if (DeviceName == null) null else DeviceName
        ["BgpAsn"] = if (BgpAsn == null) null else BgpAsn
        ["IpAddress"] = if (IpAddress == null) null else IpAddress
    }

}
