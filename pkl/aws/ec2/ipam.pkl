/// AWS::EC2::IPAM
///
/// Generated by rain build --pkl-class AWS::EC2::IPAM
module aws.ec2.ipam

import "../../cloudformation.pkl"

/// The regions IPAM is enabled for. Allows pools to be created in these regions, as well as enabling monitoring
open class IPAMIpamOperatingRegion {
    RegionName: String|Mapping
}

/// A key-value pair to associate with a resource.
open class IPAMTag {
    Key: String|Mapping
    Value: String|Mapping
}

/// Resource Schema of AWS::EC2::IPAM Type
open class IPAM extends cloudformation.Resource {

    Type = "AWS::EC2::IPAM"


    /// 
    hidden Description: (String|Mapping)?

    /// The regions IPAM is enabled for. Allows pools to be created in these regions, as well as enabling monitoring
    hidden OperatingRegions: (Listing<IPAMIpamOperatingRegion>)?

    /// An array of key-value pairs to apply to this resource.
    hidden Tags: (Listing<IPAMTag>)?

    /// The tier of the IPAM.
    hidden Tier: (IPAMTier|Mapping)?

    Properties {
        ["Description"] = if (Description == null) null else Description
        ["OperatingRegions"] = if (OperatingRegions == null) null else OperatingRegions
        ["Tags"] = if (Tags == null) null else Tags
        ["Tier"] = if (Tier == null) null else Tier
    }

}


typealias IPAMTier = "free"|"advanced"
