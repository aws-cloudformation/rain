/// AWS::EC2::KeyPair
///
/// Generated by rain build --pkl-class AWS::EC2::KeyPair
module aws.ec2.keypair

import "../../cloudformation.pkl"

/// A key-value pair to associate with a resource.
open class KeyPairTag {
    Key: String|Mapping
    Value: String|Mapping
}

/// The AWS::EC2::KeyPair creates an SSH key pair
open class KeyPair extends cloudformation.Resource {

    Type = "AWS::EC2::KeyPair"


    /// Plain text public key to import
    hidden PublicKeyMaterial: (String|Mapping)?

    /// An array of key-value pairs to apply to this resource.
    hidden Tags: (Listing<KeyPairTag>)?

    /// The name of the SSH key pair
    hidden KeyName: String|Mapping

    /// The crypto-system used to generate a key pair.
    hidden KeyType: (KeyPairKeyType|Mapping)?

    /// The format of the private key
    hidden KeyFormat: (KeyPairKeyFormat|Mapping)?

    Properties {
        ["PublicKeyMaterial"] = if (PublicKeyMaterial == null) null else PublicKeyMaterial
        ["Tags"] = if (Tags == null) null else Tags
        ["KeyName"] = if (KeyName == null) null else KeyName
        ["KeyType"] = if (KeyType == null) null else KeyType
        ["KeyFormat"] = if (KeyFormat == null) null else KeyFormat
    }

}


typealias KeyPairKeyType = "rsa"|"ed25519"

typealias KeyPairKeyFormat = "pem"|"ppk"
