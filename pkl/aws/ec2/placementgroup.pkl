/// AWS::EC2::PlacementGroup
///
/// Generated by rain build --pkl-class AWS::EC2::PlacementGroup
module aws.ec2.placementgroup

import "../../cloudformation.pkl"

/// A key-value pair to associate with a resource.
open class PlacementGroupTag {
    Key: String|Mapping
    Value: String|Mapping
}

/// Resource Type definition for AWS::EC2::PlacementGroup
open class PlacementGroup extends cloudformation.Resource {

    Type = "AWS::EC2::PlacementGroup"


    /// The placement strategy.
    hidden Strategy: (String|Mapping)?

    /// The Spread Level of Placement Group is an enum where it accepts either host or rack when strategy is spread
    hidden SpreadLevel: (String|Mapping)?

    /// The number of partitions. Valid only when **Strategy** is set to `partition`
    hidden PartitionCount: (Int|Mapping)?

    /// An array of key-value pairs to apply to this resource.
    hidden Tags: (Listing<PlacementGroupTag>)?

    Properties {
        ["Strategy"] = if (Strategy == null) null else Strategy
        ["SpreadLevel"] = if (SpreadLevel == null) null else SpreadLevel
        ["PartitionCount"] = if (PartitionCount == null) null else PartitionCount
        ["Tags"] = if (Tags == null) null else Tags
    }

}
