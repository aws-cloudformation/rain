/// AWS::EC2::SecurityGroupIngress
///
/// Generated by rain build --pkl-class AWS::EC2::SecurityGroupIngress
module aws.ec2.securitygroupingress

import "../../cloudformation.pkl"

/// Resource Type definition for AWS::EC2::SecurityGroupIngress
open class SecurityGroupIngress extends cloudformation.Resource {

    Type = "AWS::EC2::SecurityGroupIngress"


    /// The IPv4 ranges
    hidden CidrIp: (String|Mapping)?

    /// [VPC only] The IPv6 ranges
    hidden CidrIpv6: (String|Mapping)?

    /// Updates the description of an ingress (inbound) security group rule. You can replace an existing description, or add a description to a rule that did not have one previously
    hidden Description: (String|Mapping)?

    /// The start of port range for the TCP and UDP protocols, or an ICMP/ICMPv6 type number. A value of -1 indicates all ICMP/ICMPv6 types. If you specify all ICMP/ICMPv6 types, you must specify all codes.
    ///
    /// 
    /// Use this for ICMP and any protocol that uses ports.
    hidden FromPort: (Int|Mapping)?

    /// The ID of the security group. You must specify either the security group ID or the security group name in the request. For security groups in a nondefault VPC, you must specify the security group ID.
    ///
    /// 
    /// You must specify the GroupName property or the GroupId property. For security groups that are in a VPC, you must use the GroupId property.
    hidden GroupId: (String|Mapping)?

    /// The name of the security group.
    hidden GroupName: (String|Mapping)?

    /// The IP protocol name (tcp, udp, icmp, icmpv6) or number (see Protocol Numbers).
    ///
    /// 
    /// [VPC only] Use -1 to specify all protocols. When authorizing security group rules, specifying -1 or a protocol number other than tcp, udp, icmp, or icmpv6 allows traffic on all ports, regardless of any port range you specify. For tcp, udp, and icmp, you must specify a port range. For icmpv6, the port range is optional; if you omit the port range, traffic for all types and codes is allowed.
    hidden IpProtocol: String|Mapping

    /// [EC2-VPC only] The ID of a prefix list.
    ///
    /// 
    /// 
    hidden SourcePrefixListId: (String|Mapping)?

    /// The ID of the security group. You must specify either the security group ID or the security group name. For security groups in a nondefault VPC, you must specify the security group ID.
    hidden SourceSecurityGroupId: (String|Mapping)?

    /// [EC2-Classic, default VPC] The name of the source security group.
    ///
    /// 
    /// You must specify the GroupName property or the GroupId property. For security groups that are in a VPC, you must use the GroupId property.
    hidden SourceSecurityGroupName: (String|Mapping)?

    /// [nondefault VPC] The AWS account ID that owns the source security group. You can't specify this property with an IP address range.
    ///
    /// 
    /// If you specify SourceSecurityGroupName or SourceSecurityGroupId and that security group is owned by a different account than the account creating the stack, you must specify the SourceSecurityGroupOwnerId; otherwise, this property is optional.
    hidden SourceSecurityGroupOwnerId: (String|Mapping)?

    /// The end of port range for the TCP and UDP protocols, or an ICMP/ICMPv6 code. A value of -1 indicates all ICMP/ICMPv6 codes for the specified ICMP type. If you specify all ICMP/ICMPv6 types, you must specify all codes.
    ///
    /// 
    /// Use this for ICMP and any protocol that uses ports.
    hidden ToPort: (Int|Mapping)?

    Properties {
        ["CidrIp"] = if (CidrIp == null) null else CidrIp
        ["CidrIpv6"] = if (CidrIpv6 == null) null else CidrIpv6
        ["Description"] = if (Description == null) null else Description
        ["FromPort"] = if (FromPort == null) null else FromPort
        ["GroupId"] = if (GroupId == null) null else GroupId
        ["GroupName"] = if (GroupName == null) null else GroupName
        ["IpProtocol"] = if (IpProtocol == null) null else IpProtocol
        ["SourcePrefixListId"] = if (SourcePrefixListId == null) null else SourcePrefixListId
        ["SourceSecurityGroupId"] = if (SourceSecurityGroupId == null) null else SourceSecurityGroupId
        ["SourceSecurityGroupName"] = if (SourceSecurityGroupName == null) null else SourceSecurityGroupName
        ["SourceSecurityGroupOwnerId"] = if (SourceSecurityGroupOwnerId == null) null else SourceSecurityGroupOwnerId
        ["ToPort"] = if (ToPort == null) null else ToPort
    }

}
