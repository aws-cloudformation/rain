/// AWS::NetworkManager::Link
///
/// Generated by rain build --pkl-class AWS::NetworkManager::Link
module aws.networkmanager.link

import "../../cloudformation.pkl"

/// A key-value pair to associate with a link resource.
open class Tag {
    Value: String|Mapping
    Key: String|Mapping
}

/// The bandwidth for the link.
open class Bandwidth {
    DownloadSpeed: (Int|Mapping)?
    UploadSpeed: (Int|Mapping)?
}

/// The AWS::NetworkManager::Link type describes a link.
open class Link extends cloudformation.Resource {

    Type = "AWS::NetworkManager::Link"


    /// The provider of the link.
    hidden Provider: (String|Mapping)?

    /// The description of the link.
    hidden Description: (String|Mapping)?

    /// The ID of the global network.
    hidden GlobalNetworkId: String|Mapping

    /// The ID of the site
    hidden SiteId: String|Mapping

    /// The Bandwidth for the link.
    hidden Bandwidth: Bandwidth

    /// The type of the link.
    hidden TypeProperty: (String|Mapping)?

    /// The tags for the link.
    hidden Tags: (Listing<Tag>)?

    Properties {
        ["Provider"] = if (Provider == null) null else Provider
        ["Description"] = if (Description == null) null else Description
        ["GlobalNetworkId"] = if (GlobalNetworkId == null) null else GlobalNetworkId
        ["SiteId"] = if (SiteId == null) null else SiteId
        ["Bandwidth"] = if (Bandwidth == null) null else Bandwidth
        ["Type"] = if (TypeProperty == null) null else TypeProperty
        ["Tags"] = if (Tags == null) null else Tags
    }

}
