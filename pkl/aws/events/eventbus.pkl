/// AWS::Events::EventBus
///
/// Generated by rain build --pkl-class AWS::Events::EventBus
module aws.events.eventbus

import "../../cloudformation.pkl"

/// 
open class EventBusTag {
    Key: String|Mapping
    Value: String|Mapping
}

/// Resource type definition for AWS::Events::EventBus
open class EventBus extends cloudformation.Resource {

    Type = "AWS::Events::EventBus"


    /// If you are creating a partner event bus, this specifies the partner event source that the new event bus will be matched with.
    hidden EventSourceName: (String|Mapping)?

    /// The name of the event bus.
    hidden Name: String|Mapping

    /// A JSON string that describes the permission policy statement for the event bus.
    hidden Policy: (Dynamic)?

    /// Any tags assigned to the event bus.
    hidden Tags: (Listing<EventBusTag>)?

    Properties {
        ["EventSourceName"] = if (EventSourceName == null) null else EventSourceName
        ["Name"] = if (Name == null) null else Name
        ["Policy"] = if (Policy == null) null else Policy
        ["Tags"] = if (Tags == null) null else Tags
    }

}
