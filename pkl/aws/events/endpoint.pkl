/// AWS::Events::Endpoint
///
/// Generated by rain build --pkl-class AWS::Events::Endpoint
module aws.events.endpoint

import "../../cloudformation.pkl"

/// 
open class EndpointPrimary {
    HealthCheck: EndpointHealthCheck
}

/// 
open class EndpointSecondary {
    Route: EndpointRoute
}

typealias EndpointEventBusArn = String|Mapping


/// 
open class EndpointEndpointEventBus {
    EventBusArn: EndpointEventBusArn
}

typealias EndpointHealthCheck = String|Mapping


/// 
open class EndpointFailoverConfig {
    Primary: EndpointPrimary
    Secondary: EndpointSecondary
}

/// 
open class EndpointReplicationConfig {
    State: EndpointReplicationState
}

/// 
open class EndpointRoutingConfig {
    FailoverConfig: EndpointFailoverConfig
}

typealias EndpointEventBuses = Listing<(EndpointEndpointEventBus)?>


typealias EndpointReplicationState = String|Mapping


typealias EndpointRoute = String|Mapping


/// Resource Type definition for AWS::Events::Endpoint.
open class Endpoint extends cloudformation.Resource {

    Type = "AWS::Events::Endpoint"


    /// 
    hidden RoutingConfig: EndpointRoutingConfig

    /// 
    hidden Name: (String(matches(Regex(#"^[\.\-_A-Za-z0-9]+$"#)))|Mapping)?

    /// 
    hidden RoleArn: (String(matches(Regex(#"^arn:aws[a-z-]*:iam::\d{12}:role\/[\w+=,.@/-]+$"#)))|Mapping)?

    /// 
    hidden Description: (String(matches(Regex(#".*"#)))|Mapping)?

    /// 
    hidden ReplicationConfig: (EndpointReplicationConfig)?

    /// 
    hidden EventBuses: EndpointEventBuses

    Properties {
        ["RoutingConfig"] = if (RoutingConfig == null) null else RoutingConfig
        ["Name"] = if (Name == null) null else Name
        ["RoleArn"] = if (RoleArn == null) null else RoleArn
        ["Description"] = if (Description == null) null else Description
        ["ReplicationConfig"] = if (ReplicationConfig == null) null else ReplicationConfig
        ["EventBuses"] = if (EventBuses == null) null else EventBuses
    }

}
