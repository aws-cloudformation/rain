/// AWS::SupportApp::AccountAlias
///
/// Generated by rain build --pkl-class AWS::SupportApp::AccountAlias
module aws.supportapp.accountalias

import "../../cloudformation.pkl"

/// An AWS Support App resource that creates, updates, reads, and deletes a customer's account alias.
open class AccountAlias extends cloudformation.Resource {

    Type = "AWS::SupportApp::AccountAlias"


    /// An account alias associated with a customer's account.
    hidden AccountAlias: String(matches(Regex(#"^[\w\- ]+$"#)))|Mapping

    Properties {
        ["AccountAlias"] = if (AccountAlias == null) null else AccountAlias
    }

}
