/// AWS::SupportApp::SlackChannelConfiguration
///
/// Generated by rain build --pkl-class AWS::SupportApp::SlackChannelConfiguration
module aws.supportapp.slackchannelconfiguration

import "../../cloudformation.pkl"

/// An AWS Support App resource that creates, updates, lists and deletes Slack channel configurations.
open class SlackChannelConfiguration extends cloudformation.Resource {

    Type = "AWS::SupportApp::SlackChannelConfiguration"


    /// The channel ID in Slack, which identifies a channel within a workspace.
    hidden ChannelId: String(matches(Regex(#"^\S+$"#)))|Mapping

    /// The channel name in Slack.
    hidden ChannelName: (String(matches(Regex(#"^.+$"#)))|Mapping)?

    /// The Amazon Resource Name (ARN) of an IAM role that grants the AWS Support App access to perform operations for AWS services.
    hidden ChannelRoleArn: String(matches(Regex(#"^arn:aws[-a-z0-9]*:iam::[0-9]{12}:role\/(.+)$"#)))|Mapping

    /// Whether to notify when a correspondence is added to a case.
    hidden NotifyOnAddCorrespondenceToCase: (Boolean|Mapping)?

    /// The severity level of a support case that a customer wants to get notified for.
    hidden NotifyOnCaseSeverity: SlackChannelConfigurationNotifyOnCaseSeverity|Mapping

    /// Whether to notify when a case is created or reopened.
    hidden NotifyOnCreateOrReopenCase: (Boolean|Mapping)?

    /// Whether to notify when a case is resolved.
    hidden NotifyOnResolveCase: (Boolean|Mapping)?

    /// The team ID in Slack, which uniquely identifies a workspace.
    hidden TeamId: String(matches(Regex(#"^\S+$"#)))|Mapping

    Properties {
        ["ChannelId"] = if (ChannelId == null) null else ChannelId
        ["ChannelName"] = if (ChannelName == null) null else ChannelName
        ["ChannelRoleArn"] = if (ChannelRoleArn == null) null else ChannelRoleArn
        ["NotifyOnAddCorrespondenceToCase"] = if (NotifyOnAddCorrespondenceToCase == null) null else NotifyOnAddCorrespondenceToCase
        ["NotifyOnCaseSeverity"] = if (NotifyOnCaseSeverity == null) null else NotifyOnCaseSeverity
        ["NotifyOnCreateOrReopenCase"] = if (NotifyOnCreateOrReopenCase == null) null else NotifyOnCreateOrReopenCase
        ["NotifyOnResolveCase"] = if (NotifyOnResolveCase == null) null else NotifyOnResolveCase
        ["TeamId"] = if (TeamId == null) null else TeamId
    }

}


typealias SlackChannelConfigurationNotifyOnCaseSeverity = "none"|"all"|"high"
