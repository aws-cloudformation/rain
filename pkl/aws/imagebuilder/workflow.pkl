/// AWS::ImageBuilder::Workflow
///
/// Generated by rain build --pkl-class AWS::ImageBuilder::Workflow
module aws.imagebuilder.workflow

import "../../cloudformation.pkl"

/// Resource schema for AWS::ImageBuilder::Workflow
open class Workflow extends cloudformation.Resource {

    Type = "AWS::ImageBuilder::Workflow"


    /// The change description of the workflow.
    hidden ChangeDescription: (String|Mapping)?

    /// The data of the workflow.
    hidden Data: (String|Mapping)?

    /// The description of the workflow.
    hidden Description: (String|Mapping)?

    /// The KMS key identifier used to encrypt the workflow.
    hidden KmsKeyId: (String|Mapping)?

    /// The name of the workflow.
    hidden Name: String|Mapping

    /// The tags associated with the workflow.
    hidden Tags: (WorkflowWorkflowTags)?

    /// The type of the workflow denotes whether the workflow is used to build, test, or distribute.
    hidden TypeProperty: WorkflowType|Mapping

    /// The uri of the workflow.
    hidden Uri: (String|Mapping)?

    /// The version of the workflow.
    hidden Version: String|Mapping

    Properties {
        ["ChangeDescription"] = if (ChangeDescription == null) null else ChangeDescription
        ["Data"] = if (Data == null) null else Data
        ["Description"] = if (Description == null) null else Description
        ["KmsKeyId"] = if (KmsKeyId == null) null else KmsKeyId
        ["Name"] = if (Name == null) null else Name
        ["Tags"] = if (Tags == null) null else Tags
        ["Type"] = if (TypeProperty == null) null else TypeProperty
        ["Uri"] = if (Uri == null) null else Uri
        ["Version"] = if (Version == null) null else Version
    }

}


typealias WorkflowWorkflowTags = Mapping<String, Any>

typealias WorkflowType = "BUILD"|"TEST"|"DISTRIBUTION"
