/// AWS::Redshift::EndpointAuthorization
///
/// Generated by rain build --pkl-class AWS::Redshift::EndpointAuthorization
module aws.redshift.endpointauthorization

import "../../cloudformation.pkl"

/// 
open class AwsAccount {
}

/// 
open class VpcId {
}

/// Describes an endpoint authorization for authorizing Redshift-managed VPC endpoint access to a cluster across AWS accounts.
open class EndpointAuthorization extends cloudformation.Resource {

    Type = "AWS::Redshift::EndpointAuthorization"


    /// The virtual private cloud (VPC) identifiers to grant or revoke access to.
    hidden VpcIds: (Listing<VpcId>)?

    ///  Indicates whether to force the revoke action. If true, the Redshift-managed VPC endpoints associated with the endpoint authorization are also deleted.
    hidden Force: (Boolean|Mapping)?

    /// The cluster identifier.
    hidden ClusterIdentifier: String(matches(Regex(#"^(?=^[a-z][a-z0-9]*(-[a-z0-9]+)*$).{1,63}$"#)))|Mapping

    /// The target AWS account ID to grant or revoke access for.
    hidden Account: AwsAccount

    Properties {
        ["VpcIds"] = if (VpcIds == null) null else VpcIds
        ["Force"] = if (Force == null) null else Force
        ["ClusterIdentifier"] = if (ClusterIdentifier == null) null else ClusterIdentifier
        ["Account"] = if (Account == null) null else Account
    }

}
