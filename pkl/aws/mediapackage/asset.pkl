/// AWS::MediaPackage::Asset
///
/// Generated by rain build --pkl-class AWS::MediaPackage::Asset
module aws.mediapackage.asset

import "../../cloudformation.pkl"

/// 
open class Tag {
    Key: String|Mapping
    Value: String|Mapping
}

/// The endpoint URL used to access an Asset using one PackagingConfiguration.
open class EgressEndpoint {
    PackagingConfigurationId: String|Mapping
    Url: String|Mapping
}

/// Resource schema for AWS::MediaPackage::Asset
open class Asset extends cloudformation.Resource {

    Type = "AWS::MediaPackage::Asset"


    /// The resource ID to include in SPEKE key requests.
    hidden ResourceId: (String|Mapping)?

    /// ARN of the source object in S3.
    hidden SourceArn: String|Mapping

    /// The list of egress endpoints available for the Asset.
    hidden EgressEndpoints: (Listing<EgressEndpoint>)?

    /// The unique identifier for the Asset.
    hidden Id: String|Mapping

    /// The ID of the PackagingGroup for the Asset.
    hidden PackagingGroupId: String|Mapping

    /// The IAM role_arn used to access the source S3 bucket.
    hidden SourceRoleArn: String|Mapping

    /// A collection of tags associated with a resource
    hidden Tags: (Listing<Tag>)?

    Properties {
        ["ResourceId"] = if (ResourceId == null) null else ResourceId
        ["SourceArn"] = if (SourceArn == null) null else SourceArn
        ["EgressEndpoints"] = if (EgressEndpoints == null) null else EgressEndpoints
        ["Id"] = if (Id == null) null else Id
        ["PackagingGroupId"] = if (PackagingGroupId == null) null else PackagingGroupId
        ["SourceRoleArn"] = if (SourceRoleArn == null) null else SourceRoleArn
        ["Tags"] = if (Tags == null) null else Tags
    }

}
