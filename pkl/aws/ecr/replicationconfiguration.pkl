/// AWS::ECR::ReplicationConfiguration
///
/// Generated by rain build --pkl-class AWS::ECR::ReplicationConfiguration
module aws.ecr.replicationconfiguration

import "../../cloudformation.pkl"

/// An array of objects representing the details of a repository filter.
open class RepositoryFilter {
    Filter: Filter
    FilterType: FilterType
}

/// The repository filter to be applied for replication.
open class Filter {
}

/// Type of repository filter
open class FilterType {
}

/// An array of objects representing the details of a replication destination.
open class ReplicationDestination {
    Region: Region
    RegistryId: RegistryId
}

/// The account ID of the destination registry to replicate to.
open class RegistryId {
}

/// A Region to replicate to.
open class Region {
}

/// An object representing the replication configuration for a registry.
open class ReplicationConfiguration {
    Rules: Listing<ReplicationRule>
}

/// An array of objects representing the details of a replication destination.
open class ReplicationRule {
    RepositoryFilters: (Listing<RepositoryFilter>)?
    Destinations: Listing<ReplicationDestination>
}

/// The AWS::ECR::ReplicationConfiguration resource configures the replication destinations for an Amazon Elastic Container Registry (Amazon Private ECR). For more information, see https://docs.aws.amazon.com/AmazonECR/latest/userguide/replication.html
open class ReplicationConfiguration extends cloudformation.Resource {

    Type = "AWS::ECR::ReplicationConfiguration"


    /// 
    hidden ReplicationConfiguration: ReplicationConfiguration

    Properties {
        ["ReplicationConfiguration"] = if (ReplicationConfiguration == null) null else ReplicationConfiguration
    }

}
