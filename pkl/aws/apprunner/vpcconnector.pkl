/// AWS::AppRunner::VpcConnector
///
/// Generated by rain build --pkl-class AWS::AppRunner::VpcConnector
module aws.apprunner.vpcconnector

import "../../cloudformation.pkl"

/// 
open class VpcConnectorTag {
    Key: (String|Mapping)?
    Value: (String|Mapping)?
}

/// The AWS::AppRunner::VpcConnector resource specifies an App Runner VpcConnector.
open class VpcConnector extends cloudformation.Resource {

    Type = "AWS::AppRunner::VpcConnector"


    /// A list of IDs of security groups that App Runner should use for access to AWS resources under the specified subnets. If not specified, App Runner uses the default security group of the Amazon VPC. The default security group allows all outbound traffic.
    hidden SecurityGroups: (Listing<String|Mapping>)?

    /// A list of IDs of subnets that App Runner should use when it associates your service with a custom Amazon VPC. Specify IDs of subnets of a single Amazon VPC. App Runner determines the Amazon VPC from the subnets you specify.
    hidden Subnets: Listing<String|Mapping>

    /// A list of metadata items that you can associate with your VPC connector resource. A tag is a key-value pair.
    hidden Tags: (Listing<VpcConnectorTag>)?

    /// A name for the VPC connector. If you don't specify a name, AWS CloudFormation generates a name for your VPC connector.
    hidden VpcConnectorName: (String(matches(Regex(#"^[A-Za-z0-9][A-Za-z0-9-\\_]{3,39}$"#)))|Mapping)?

    Properties {
        ["SecurityGroups"] = if (SecurityGroups == null) null else SecurityGroups
        ["Subnets"] = if (Subnets == null) null else Subnets
        ["Tags"] = if (Tags == null) null else Tags
        ["VpcConnectorName"] = if (VpcConnectorName == null) null else VpcConnectorName
    }

}
