/// AWS::SecurityLake::AwsLogSource
///
/// Generated by rain build --pkl-class AWS::SecurityLake::AwsLogSource
module aws.securitylake.awslogsource

import "../../cloudformation.pkl"

/// Resource Type definition for AWS::SecurityLake::AwsLogSource
open class AwsLogSource extends cloudformation.Resource {

    Type = "AWS::SecurityLake::AwsLogSource"


    /// AWS account where you want to collect logs from.
    hidden Accounts: (Listing<String|Mapping>)?

    /// The ARN for the data lake.
    hidden DataLakeArn: String|Mapping

    /// The name for a AWS source. This must be a Regionally unique value.
    hidden SourceName: String|Mapping

    /// The version for a AWS source. This must be a Regionally unique value.
    hidden SourceVersion: String(matches(Regex(#"^(latest|[0-9]\.[0-9])$"#)))|Mapping

    Properties {
        ["Accounts"] = if (Accounts == null) null else Accounts
        ["DataLakeArn"] = if (DataLakeArn == null) null else DataLakeArn
        ["SourceName"] = if (SourceName == null) null else SourceName
        ["SourceVersion"] = if (SourceVersion == null) null else SourceVersion
    }

}
