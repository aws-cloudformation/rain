/// AWS::SecurityLake::DataLake
///
/// Generated by rain build --pkl-class AWS::SecurityLake::DataLake
module aws.securitylake.datalake

import "../../cloudformation.pkl"

typealias DataLakeDays = Int|Mapping


/// Provides encryption details of Amazon Security Lake object.
open class DataLakeEncryptionConfiguration {
    KmsKeyId: (String|Mapping)?
}

/// Provides data expiration details of Amazon Security Lake object.
open class DataLakeExpiration {
    Days: (DataLakeDays)?
}

/// Provides lifecycle details of Amazon Security Lake object.
open class DataLakeLifecycleConfiguration {
    Expiration: (DataLakeExpiration)?
    Transitions: (Listing<DataLakeTransitions>)?
}

typealias DataLakeRegions = Listing<(String(matches(Regex(#"^(af|ap|ca|eu|me|sa|us)-(central|north|(north(?:east|west))|south|south(?:east|west)|east|west)-\d+$"#)))|Mapping)?>


/// Provides replication details of Amazon Security Lake object.
open class DataLakeReplicationConfiguration {
    Regions: (DataLakeRegions)?
    RoleArn: (String(matches(Regex(#"^arn:.*$"#)))|Mapping)?
}

/// A key-value pair to associate with a resource.
open class DataLakeTag {
    Key: String|Mapping
    Value: String|Mapping
}

/// 
open class DataLakeTransitions {
    Days: (Int|Mapping)?
    StorageClass: (String|Mapping)?
}

/// Resource Type definition for AWS::SecurityLake::DataLake
open class DataLake extends cloudformation.Resource {

    Type = "AWS::SecurityLake::DataLake"


    /// 
    hidden EncryptionConfiguration: (DataLakeEncryptionConfiguration)?

    /// 
    hidden LifecycleConfiguration: (DataLakeLifecycleConfiguration)?

    /// The Amazon Resource Name (ARN) used to index AWS Glue table partitions that are generated by the ingestion and normalization of AWS log sources and custom sources.
    hidden MetaStoreManagerRoleArn: (String(matches(Regex(#"^arn:.*$"#)))|Mapping)?

    /// 
    hidden ReplicationConfiguration: (DataLakeReplicationConfiguration)?

    /// 
    hidden Tags: (Listing<DataLakeTag>)?

    Properties {
        ["EncryptionConfiguration"] = if (EncryptionConfiguration == null) null else EncryptionConfiguration
        ["LifecycleConfiguration"] = if (LifecycleConfiguration == null) null else LifecycleConfiguration
        ["MetaStoreManagerRoleArn"] = if (MetaStoreManagerRoleArn == null) null else MetaStoreManagerRoleArn
        ["ReplicationConfiguration"] = if (ReplicationConfiguration == null) null else ReplicationConfiguration
        ["Tags"] = if (Tags == null) null else Tags
    }

}
