/// AWS::SecurityLake::Subscriber
///
/// Generated by rain build --pkl-class AWS::SecurityLake::Subscriber
module aws.securitylake.subscriber

import "../../cloudformation.pkl"

typealias AccessTypesArray = "LAKEFORMATION"|"S3"

typealias SubscriberAccessTypes = Listing<(AccessTypesArray|Mapping)?>


/// Amazon Security Lake supports log and event collection for natively supported AWS services.
open class SubscriberAwsLogSource {
    SourceName: (String|Mapping)?
    SourceVersion: (String(matches(Regex(#"^(latest|[0-9]\.[0-9])$"#)))|Mapping)?
}

/// 
open class SubscriberCustomLogSource {
    SourceName: (String(matches(Regex(#"^[\\\w\-_:/.]*$"#)))|Mapping)?
    SourceVersion: (String(matches(Regex(#"^[A-Za-z0-9\-\.\_]*$"#)))|Mapping)?
}

typealias SubscriberSource = SubscriberSource0|SubscriberSource1


/// 
open class SubscriberSource0 {
    AwsLogSource: SubscriberAwsLogSource
}

/// 
open class SubscriberSource1 {
    CustomLogSource: SubscriberCustomLogSource
}

/// 
open class SubscriberTag {
    Key: String|Mapping
    Value: String|Mapping
}

/// Resource Type definition for AWS::SecurityLake::Subscriber
open class Subscriber extends cloudformation.Resource {

    Type = "AWS::SecurityLake::Subscriber"


    /// 
    hidden AccessTypes: SubscriberAccessTypes

    /// The ARN for the data lake.
    hidden DataLakeArn: String|Mapping

    /// The supported AWS services from which logs and events are collected.
    hidden Sources: Listing<SubscriberSource>

    /// The description for your subscriber account in Security Lake.
    hidden SubscriberDescription: (String|Mapping)?

    /// The AWS identity used to access your data.
    hidden SubscriberIdentity: Dynamic

    /// The name of your Security Lake subscriber account.
    hidden SubscriberName: String(matches(Regex(#"^[\\\w\s\-_:/,.@=+]*$"#)))|Mapping

    /// An array of objects, one for each tag to associate with the subscriber. For each tag, you must specify both a tag key and a tag value. A tag value cannot be null, but it can be an empty string.
    hidden Tags: (Listing<SubscriberTag>)?

    Properties {
        ["AccessTypes"] = if (AccessTypes == null) null else AccessTypes
        ["DataLakeArn"] = if (DataLakeArn == null) null else DataLakeArn
        ["Sources"] = if (Sources == null) null else Sources
        ["SubscriberDescription"] = if (SubscriberDescription == null) null else SubscriberDescription
        ["SubscriberIdentity"] = if (SubscriberIdentity == null) null else SubscriberIdentity
        ["SubscriberName"] = if (SubscriberName == null) null else SubscriberName
        ["Tags"] = if (Tags == null) null else Tags
    }

}
