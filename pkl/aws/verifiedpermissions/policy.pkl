/// AWS::VerifiedPermissions::Policy
///
/// Generated by rain build --pkl-class AWS::VerifiedPermissions::Policy
module aws.verifiedpermissions.policy

import "../../cloudformation.pkl"

/// 
open class TemplateLinkedPolicyDefinition {
    PolicyTemplateId: String(matches(Regex(#"^[a-zA-Z0-9-]*$"#)))|Mapping
    Principal: (EntityIdentifier)?
    Resource: (EntityIdentifier)?
}

/// 
open class EntityIdentifier {
    EntityType: String(matches(Regex(#"^.*$"#)))|Mapping
    EntityId: String(matches(Regex(#"^.*$"#)))|Mapping
}

/// 
open class PolicyDefinition {
}

/// 
open class PolicyType {
}

/// 
open class StaticPolicyDefinition {
    Description: (String|Mapping)?
    Statement: String|Mapping
}

/// Definition of AWS::VerifiedPermissions::Policy Resource Type
open class Policy extends cloudformation.Resource {

    Type = "AWS::VerifiedPermissions::Policy"


    /// 
    hidden Definition: PolicyDefinition

    /// 
    hidden PolicyStoreId: String(matches(Regex(#"^[a-zA-Z0-9-]*$"#)))|Mapping

    Properties {
        ["Definition"] = if (Definition == null) null else Definition
        ["PolicyStoreId"] = if (PolicyStoreId == null) null else PolicyStoreId
    }

}
