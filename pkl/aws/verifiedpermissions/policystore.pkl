/// AWS::VerifiedPermissions::PolicyStore
///
/// Generated by rain build --pkl-class AWS::VerifiedPermissions::PolicyStore
module aws.verifiedpermissions.policystore

import "../../cloudformation.pkl"

/// 
open class PolicyStoreSchemaDefinition {
    CedarJson: (PolicyStoreSchemaJson)?
}

typealias PolicyStoreSchemaJson = String|Mapping


typealias PolicyStoreValidationMode = String|Mapping


/// 
open class PolicyStoreValidationSettings {
    Mode: PolicyStoreValidationMode
}

/// Definition of AWS::VerifiedPermissions::PolicyStore Resource Type
open class PolicyStore extends cloudformation.Resource {

    Type = "AWS::VerifiedPermissions::PolicyStore"


    /// 
    hidden Description: (String|Mapping)?

    /// 
    hidden Schema: (PolicyStoreSchemaDefinition)?

    /// 
    hidden ValidationSettings: PolicyStoreValidationSettings

    Properties {
        ["Description"] = if (Description == null) null else Description
        ["Schema"] = if (Schema == null) null else Schema
        ["ValidationSettings"] = if (ValidationSettings == null) null else ValidationSettings
    }

}
