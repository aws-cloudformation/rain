/// AWS::GlobalAccelerator::Listener
///
/// Generated by rain build --pkl-class AWS::GlobalAccelerator::Listener
module aws.globalaccelerator.listener

import "../../cloudformation.pkl"

typealias ListenerPort = Int|Mapping


/// A port range to support for connections from  clients to your accelerator.
open class ListenerPortRange {
    ToPort: ListenerPort
    FromPort: ListenerPort
}

/// Resource Type definition for AWS::GlobalAccelerator::Listener
open class Listener extends cloudformation.Resource {

    Type = "AWS::GlobalAccelerator::Listener"


    /// The protocol for the listener.
    hidden Protocol: ListenerProtocol|Mapping

    /// Client affinity lets you direct all requests from a user to the same endpoint.
    hidden ClientAffinity: (ListenerClientAffinity|Mapping)?

    /// The Amazon Resource Name (ARN) of the accelerator.
    hidden AcceleratorArn: String|Mapping

    /// 
    hidden PortRanges: Listing<ListenerPortRange>

    Properties {
        ["Protocol"] = if (Protocol == null) null else Protocol
        ["ClientAffinity"] = if (ClientAffinity == null) null else ClientAffinity
        ["AcceleratorArn"] = if (AcceleratorArn == null) null else AcceleratorArn
        ["PortRanges"] = if (PortRanges == null) null else PortRanges
    }

}


typealias ListenerProtocol = "TCP"|"UDP"

typealias ListenerClientAffinity = "NONE"|"SOURCE_IP"
