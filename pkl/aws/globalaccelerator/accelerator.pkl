/// AWS::GlobalAccelerator::Accelerator
///
/// Generated by rain build --pkl-class AWS::GlobalAccelerator::Accelerator
module aws.globalaccelerator.accelerator

import "../../cloudformation.pkl"

/// Tag is a key-value pair associated with accelerator.
open class Tag {
    Key: String|Mapping
    Value: String|Mapping
}

/// An IPV4 address
open class IpAddress {
}

/// Resource Type definition for AWS::GlobalAccelerator::Accelerator
open class Accelerator extends cloudformation.Resource {

    Type = "AWS::GlobalAccelerator::Accelerator"


    /// The IP addresses from BYOIP Prefix pool.
    hidden IpAddresses: (Listing<IpAddress>)?

    /// Indicates whether an accelerator is enabled. The value is true or false.
    hidden Enabled: (Boolean|Mapping)?

    /// Name of accelerator.
    hidden Name: String(matches(Regex(#"^[a-zA-Z0-9_-]{0,64}$"#)))|Mapping

    /// IP Address type.
    hidden IpAddressType: (AcceleratorIpAddressType|Mapping)?

    /// 
    hidden Tags: (Listing<Tag>)?

    Properties {
        ["IpAddresses"] = if (IpAddresses == null) null else IpAddresses
        ["Enabled"] = if (Enabled == null) null else Enabled
        ["Name"] = if (Name == null) null else Name
        ["IpAddressType"] = if (IpAddressType == null) null else IpAddressType
        ["Tags"] = if (Tags == null) null else Tags
    }

}


typealias AcceleratorIpAddressType = "IPV4"|"DUAL_STACK"
