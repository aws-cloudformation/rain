/// AWS::IVSChat::Room
///
/// Generated by rain build --pkl-class AWS::IVSChat::Room
module aws.ivschat.room

import "../../cloudformation.pkl"

typealias MessageReviewHandlerFallbackResult = "ALLOW"|"DENY"

/// Configuration information for optional review of messages.
open class RoomMessageReviewHandler {
    FallbackResult: (MessageReviewHandlerFallbackResult|Mapping)?
    Uri: (String(matches(Regex(#"^$|^arn:aws:lambda:[a-z0-9-]+:[0-9]{12}:function:.+"#)))|Mapping)?
}

/// A key-value pair to associate with a resource.
open class RoomTag {
    Key: String|Mapping
    Value: String|Mapping
}

/// Resource type definition for AWS::IVSChat::Room.
open class Room extends cloudformation.Resource {

    Type = "AWS::IVSChat::Room"


    /// Array of logging configuration identifiers attached to the room.
    hidden LoggingConfigurationIdentifiers: (Listing<String|Mapping>)?

    /// The maximum number of characters in a single message.
    hidden MaximumMessageLength: (Int|Mapping)?

    /// The maximum number of messages per second that can be sent to the room.
    hidden MaximumMessageRatePerSecond: (Int|Mapping)?

    /// 
    hidden MessageReviewHandler: (RoomMessageReviewHandler)?

    /// The name of the room. The value does not need to be unique.
    hidden Name: (String(matches(Regex(#"^[a-zA-Z0-9-_]*$"#)))|Mapping)?

    /// An array of key-value pairs to apply to this resource.
    hidden Tags: (Listing<RoomTag>)?

    Properties {
        ["LoggingConfigurationIdentifiers"] = if (LoggingConfigurationIdentifiers == null) null else LoggingConfigurationIdentifiers
        ["MaximumMessageLength"] = if (MaximumMessageLength == null) null else MaximumMessageLength
        ["MaximumMessageRatePerSecond"] = if (MaximumMessageRatePerSecond == null) null else MaximumMessageRatePerSecond
        ["MessageReviewHandler"] = if (MessageReviewHandler == null) null else MessageReviewHandler
        ["Name"] = if (Name == null) null else Name
        ["Tags"] = if (Tags == null) null else Tags
    }

}
