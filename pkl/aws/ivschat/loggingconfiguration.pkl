/// AWS::IVSChat::LoggingConfiguration
///
/// Generated by rain build --pkl-class AWS::IVSChat::LoggingConfiguration
module aws.ivschat.loggingconfiguration

import "../../cloudformation.pkl"

/// Kinesis Firehose destination configuration for IVS Chat logging.
open class FirehoseDestinationConfiguration {
    DeliveryStreamName: String(matches(Regex(#"^[a-zA-Z0-9_.-]+$"#)))|Mapping
}

/// S3 destination configuration for IVS Chat logging.
open class S3DestinationConfiguration {
    BucketName: String(matches(Regex(#"^[a-z0-9-.]+$"#)))|Mapping
}

/// A key-value pair to associate with a resource.
open class Tag {
    Key: String|Mapping
    Value: String|Mapping
}

/// Destination configuration for IVS Chat logging.
open class DestinationConfiguration {
    Firehose: (FirehoseDestinationConfiguration)?
    S3: (S3DestinationConfiguration)?
    CloudWatchLogs: (CloudWatchLogsDestinationConfiguration)?
}

/// CloudWatch destination configuration for IVS Chat logging.
open class CloudWatchLogsDestinationConfiguration {
    LogGroupName: String(matches(Regex(#"^[\.\-_/#A-Za-z0-9]+$"#)))|Mapping
}

/// Resource type definition for AWS::IVSChat::LoggingConfiguration.
open class LoggingConfiguration extends cloudformation.Resource {

    Type = "AWS::IVSChat::LoggingConfiguration"


    /// 
    hidden DestinationConfiguration: DestinationConfiguration

    /// The name of the logging configuration. The value does not need to be unique.
    hidden Name: (String(matches(Regex(#"^[a-zA-Z0-9-_]*$"#)))|Mapping)?

    /// An array of key-value pairs to apply to this resource.
    hidden Tags: (Listing<Tag>)?

    Properties {
        ["DestinationConfiguration"] = if (DestinationConfiguration == null) null else DestinationConfiguration
        ["Name"] = if (Name == null) null else Name
        ["Tags"] = if (Tags == null) null else Tags
    }

}
