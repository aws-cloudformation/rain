/// AWS::AccessAnalyzer::Analyzer
///
/// Generated by rain build --pkl-class AWS::AccessAnalyzer::Analyzer
module aws.accessanalyzer.analyzer

import "../../cloudformation.pkl"

/// An Access Analyzer archive rule. Archive rules automatically archive new findings that meet the criteria you define when you create the rule.
open class AnalyzerArchiveRule {
    Filter: Listing<AnalyzerFilter>
    RuleName: String|Mapping
}

/// 
open class AnalyzerFilter {
    Contains: (Listing<String|Mapping>)?
    Eq: (Listing<String|Mapping>)?
    Exists: (Boolean|Mapping)?
    Neq: (Listing<String|Mapping>)?
    Property: String|Mapping
}

/// A key-value pair to associate with a resource.
open class AnalyzerTag {
    Key: String|Mapping
    Value: String|Mapping
}

/// The Configuration for Unused Access Analyzer
open class AnalyzerUnusedAccessConfiguration {
    UnusedAccessAge: (Int|Mapping)?
}

/// The AWS::AccessAnalyzer::Analyzer type specifies an analyzer of the user's account
open class Analyzer extends cloudformation.Resource {

    Type = "AWS::AccessAnalyzer::Analyzer"


    /// The configuration for the analyzer
    hidden AnalyzerConfiguration: (Dynamic)?

    /// Analyzer name
    hidden AnalyzerName: (String|Mapping)?

    /// 
    hidden ArchiveRules: (Listing<AnalyzerArchiveRule>)?

    /// An array of key-value pairs to apply to this resource.
    hidden Tags: (Listing<AnalyzerTag>)?

    /// The type of the analyzer, must be one of ACCOUNT, ORGANIZATION, ACCOUNT_UNUSED_ACCESS or ORGANIZATION_UNUSED_ACCESS
    hidden TypeProperty: String|Mapping

    Properties {
        ["AnalyzerConfiguration"] = if (AnalyzerConfiguration == null) null else AnalyzerConfiguration
        ["AnalyzerName"] = if (AnalyzerName == null) null else AnalyzerName
        ["ArchiveRules"] = if (ArchiveRules == null) null else ArchiveRules
        ["Tags"] = if (Tags == null) null else Tags
        ["Type"] = if (TypeProperty == null) null else TypeProperty
    }

}
