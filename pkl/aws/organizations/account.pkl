/// AWS::Organizations::Account
///
/// Generated by rain build --pkl-class AWS::Organizations::Account
module aws.organizations.account

import "../../cloudformation.pkl"

/// A custom key-value pair associated with a resource within your organization.
open class Tag {
    Key: String(matches(Regex(#"[\s\S]*"#)))|Mapping
    Value: String(matches(Regex(#"[\s\S]*"#)))|Mapping
}

/// You can use AWS::Organizations::Account to manage accounts in organization.
open class Account extends cloudformation.Resource {

    Type = "AWS::Organizations::Account"


    /// The name of an IAM role that AWS Organizations automatically preconfigures in the new member account. Default name is OrganizationAccountAccessRole if not specified.
    hidden RoleName: (String(matches(Regex(#"[\w+=,.@-]{1,64}"#)))|Mapping)?

    /// List of parent nodes for the member account. Currently only one parent at a time is supported. Default is root.
    hidden ParentIds: (Listing<String|Mapping>)?

    /// The friendly name of the member account.
    hidden AccountName: String(matches(Regex(#"[\u0020-\u007E]+"#)))|Mapping

    /// The email address of the owner to assign to the new member account.
    hidden Email: String(matches(Regex(#"[^\s@]+@[^\s@]+\.[^\s@]+"#)))|Mapping

    /// A list of tags that you want to attach to the newly created account. For each tag in the list, you must specify both a tag key and a value.
    hidden Tags: (Listing<Tag>)?

    Properties {
        ["RoleName"] = if (RoleName == null) null else RoleName
        ["ParentIds"] = if (ParentIds == null) null else ParentIds
        ["AccountName"] = if (AccountName == null) null else AccountName
        ["Email"] = if (Email == null) null else Email
        ["Tags"] = if (Tags == null) null else Tags
    }

}
