/// AWS::APS::Scraper
///
/// Generated by rain build --pkl-class AWS::APS::Scraper
module aws.aps.scraper

import "../../cloudformation.pkl"

/// Scraper metrics destination
open class ScraperDestination {
    AmpConfiguration: (Dynamic)?
}

/// Scraper configuration
open class ScraperScrapeConfiguration {
    ConfigurationBlob: (String|Mapping)?
}

typealias ScraperSecurityGroupId = String|Mapping


/// Scraper metrics source
open class ScraperSource {
    EksConfiguration: (Dynamic)?
}

typealias ScraperSubnetId = String|Mapping


/// A key-value pair to associate with a resource.
open class ScraperTag {
    Key: String|Mapping
    Value: String|Mapping
}

/// Resource Type definition for AWS::APS::Scraper
open class Scraper extends cloudformation.Resource {

    Type = "AWS::APS::Scraper"


    /// Scraper alias.
    hidden Alias: (String(matches(Regex(#"^[0-9A-Za-z][-.0-9A-Z_a-z]*$"#)))|Mapping)?

    /// 
    hidden Destination: ScraperDestination

    /// 
    hidden ScrapeConfiguration: ScraperScrapeConfiguration

    /// 
    hidden Source: ScraperSource

    /// An array of key-value pairs to apply to this resource.
    hidden Tags: (Listing<ScraperTag>)?

    Properties {
        ["Alias"] = if (Alias == null) null else Alias
        ["Destination"] = if (Destination == null) null else Destination
        ["ScrapeConfiguration"] = if (ScrapeConfiguration == null) null else ScrapeConfiguration
        ["Source"] = if (Source == null) null else Source
        ["Tags"] = if (Tags == null) null else Tags
    }

}
