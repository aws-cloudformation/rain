/// AWS::IoTWireless::NetworkAnalyzerConfiguration
///
/// Generated by rain build --pkl-class AWS::IoTWireless::NetworkAnalyzerConfiguration
module aws.iotwireless.networkanalyzerconfiguration

import "../../cloudformation.pkl"

/// 
open class WirelessDeviceFrameInfo {
}

/// 
open class LogLevel {
}

/// A key-value pair to associate with a resource.
open class Tag {
    Key: String|Mapping
    Value: String|Mapping
}

/// Create and manage NetworkAnalyzerConfiguration resource.
open class NetworkAnalyzerConfiguration extends cloudformation.Resource {

    Type = "AWS::IoTWireless::NetworkAnalyzerConfiguration"


    /// An array of key-value pairs to apply to this resource.
    hidden Tags: (Listing<Tag>)?

    /// Name of the network analyzer configuration
    hidden Name: String(matches(Regex(#"^[a-zA-Z0-9-_]+$"#)))|Mapping

    /// The description of the new resource
    hidden Description: (String|Mapping)?

    /// Trace content for your wireless gateway and wireless device resources
    hidden TraceContent: (Dynamic)?

    /// List of wireless gateway resources that have been added to the network analyzer configuration
    hidden WirelessDevices: (Listing<String|Mapping>)?

    /// List of wireless gateway resources that have been added to the network analyzer configuration
    hidden WirelessGateways: (Listing<String|Mapping>)?

    Properties {
        ["Tags"] = if (Tags == null) null else Tags
        ["Name"] = if (Name == null) null else Name
        ["Description"] = if (Description == null) null else Description
        ["TraceContent"] = if (TraceContent == null) null else TraceContent
        ["WirelessDevices"] = if (WirelessDevices == null) null else WirelessDevices
        ["WirelessGateways"] = if (WirelessGateways == null) null else WirelessGateways
    }

}
