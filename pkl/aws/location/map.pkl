/// AWS::Location::Map
///
/// Generated by rain build --pkl-class AWS::Location::Map
module aws.location.map

import "../../cloudformation.pkl"

/// 
open class MapMapConfiguration {
    Style: String(matches(Regex(#"^[-._\w]+$"#)))|Mapping
    PoliticalView: (String(matches(Regex(#"^[A-Z]{3}$"#)))|Mapping)?
    CustomLayers: (Listing<String|Mapping>)?
}

typealias MapPricingPlan = String|Mapping


typealias MapTagMap = Mapping<String, Any>


/// A key-value pair to associate with a resource.
open class MapTag {
    Key: String(matches(Regex(#"^[a-zA-Z+-=._:/]+$"#)))|Mapping
    Value: String(matches(Regex(#"^[A-Za-z0-9 _=@:.+-/]*$"#)))|Mapping
}

typealias Mapiso8601UTC = String|Mapping


/// Definition of AWS::Location::Map Resource Type
open class Map extends cloudformation.Resource {

    Type = "AWS::Location::Map"


    /// 
    hidden MapName: String(matches(Regex(#"^[-._\w]+$"#)))|Mapping

    /// 
    hidden PricingPlan: (MapPricingPlan)?

    /// 
    hidden Description: (String|Mapping)?

    /// An array of key-value pairs to apply to this resource.
    hidden Tags: (Listing<MapTag>)?

    /// 
    hidden Configuration: MapMapConfiguration

    Properties {
        ["MapName"] = if (MapName == null) null else MapName
        ["PricingPlan"] = if (PricingPlan == null) null else PricingPlan
        ["Description"] = if (Description == null) null else Description
        ["Tags"] = if (Tags == null) null else Tags
        ["Configuration"] = if (Configuration == null) null else Configuration
    }

}
