/// AWS::Location::PlaceIndex
///
/// Generated by rain build --pkl-class AWS::Location::PlaceIndex
module aws.location.placeindex

import "../../cloudformation.pkl"

/// The datetime value in ISO 8601 format. The timezone is always UTC. (YYYY-MM-DDThh:mm:ss.sssZ)
open class iso8601UTC {
}

/// 
open class DataSourceConfiguration {
    IntendedUse: (IntendedUse)?
}

/// 
open class IntendedUse {
}

/// 
open class PricingPlan {
}

/// 
open class TagMap {
}

/// A key-value pair to associate with a resource.
open class Tag {
    Key: String(matches(Regex(#"^[a-zA-Z+-=._:/]+$"#)))|Mapping
    Value: String(matches(Regex(#"^[A-Za-z0-9 _=@:.+-/]*$"#)))|Mapping
}

/// Definition of AWS::Location::PlaceIndex Resource Type
open class PlaceIndex extends cloudformation.Resource {

    Type = "AWS::Location::PlaceIndex"


    /// 
    hidden Description: (String|Mapping)?

    /// 
    hidden PricingPlan: (PricingPlan)?

    /// An array of key-value pairs to apply to this resource.
    hidden Tags: (Listing<Tag>)?

    /// 
    hidden IndexName: String(matches(Regex(#"^[-._\w]+$"#)))|Mapping

    /// 
    hidden DataSource: String|Mapping

    /// 
    hidden DataSourceConfiguration: (DataSourceConfiguration)?

    Properties {
        ["Description"] = if (Description == null) null else Description
        ["PricingPlan"] = if (PricingPlan == null) null else PricingPlan
        ["Tags"] = if (Tags == null) null else Tags
        ["IndexName"] = if (IndexName == null) null else IndexName
        ["DataSource"] = if (DataSource == null) null else DataSource
        ["DataSourceConfiguration"] = if (DataSourceConfiguration == null) null else DataSourceConfiguration
    }

}
