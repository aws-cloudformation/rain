/// AWS::Route53::CidrCollection
///
/// Generated by rain build --pkl-class AWS::Route53::CidrCollection
module aws.route53.cidrcollection

import "../../cloudformation.pkl"

/// 
open class CidrCollectionLocation {
    LocationName: String|Mapping
    CidrList: Listing<String|Mapping>
}

/// Resource schema for AWS::Route53::CidrCollection.
open class CidrCollection extends cloudformation.Resource {

    Type = "AWS::Route53::CidrCollection"


    /// A unique name for the CIDR collection.
    hidden Name: String(matches(Regex(#"^[0-9A-Za-z_\-]+$"#)))|Mapping

    /// A complex type that contains information about the list of CIDR locations.
    hidden Locations: (Listing<CidrCollectionLocation>)?

    Properties {
        ["Name"] = if (Name == null) null else Name
        ["Locations"] = if (Locations == null) null else Locations
    }

}
