/// AWS::AutoScaling::WarmPool
///
/// Generated by rain build --pkl-class AWS::AutoScaling::WarmPool
module aws.autoscaling.warmpool

import "../../cloudformation.pkl"

/// 
open class WarmPoolInstanceReusePolicy {
    ReuseOnScaleIn: (Boolean|Mapping)?
}

/// Resource schema for AWS::AutoScaling::WarmPool.
open class WarmPool extends cloudformation.Resource {

    Type = "AWS::AutoScaling::WarmPool"


    /// 
    hidden AutoScalingGroupName: String|Mapping

    /// 
    hidden InstanceReusePolicy: (WarmPoolInstanceReusePolicy)?

    /// 
    hidden MaxGroupPreparedCapacity: (Int|Mapping)?

    /// 
    hidden MinSize: (Int|Mapping)?

    /// 
    hidden PoolState: (String|Mapping)?

    Properties {
        ["AutoScalingGroupName"] = if (AutoScalingGroupName == null) null else AutoScalingGroupName
        ["InstanceReusePolicy"] = if (InstanceReusePolicy == null) null else InstanceReusePolicy
        ["MaxGroupPreparedCapacity"] = if (MaxGroupPreparedCapacity == null) null else MaxGroupPreparedCapacity
        ["MinSize"] = if (MinSize == null) null else MinSize
        ["PoolState"] = if (PoolState == null) null else PoolState
    }

}
