/// AWS::NeptuneGraph::PrivateGraphEndpoint
///
/// Generated by rain build --pkl-class AWS::NeptuneGraph::PrivateGraphEndpoint
module aws.neptunegraph.privategraphendpoint

import "../../cloudformation.pkl"

/// The AWS::NeptuneGraph::PrivateGraphEndpoint resource creates an Amazon NeptuneGraph PrivateGraphEndpoint.
open class PrivateGraphEndpoint extends cloudformation.Resource {

    Type = "AWS::NeptuneGraph::PrivateGraphEndpoint"


    /// The auto-generated Graph Id assigned by the service.
    hidden GraphIdentifier: String|Mapping

    /// The security group Ids associated with the VPC where you want the private graph endpoint to be created, ie, the graph will be reachable from within the VPC.
    hidden SecurityGroupIds: (Listing<String|Mapping>)?

    /// The subnet Ids associated with the VPC where you want the private graph endpoint to be created, ie, the graph will be reachable from within the VPC.
    hidden SubnetIds: (Listing<String|Mapping>)?

    /// The VPC where you want the private graph endpoint to be created, ie, the graph will be reachable from within the VPC.
    hidden VpcId: String|Mapping

    Properties {
        ["GraphIdentifier"] = if (GraphIdentifier == null) null else GraphIdentifier
        ["SecurityGroupIds"] = if (SecurityGroupIds == null) null else SecurityGroupIds
        ["SubnetIds"] = if (SubnetIds == null) null else SubnetIds
        ["VpcId"] = if (VpcId == null) null else VpcId
    }

}
