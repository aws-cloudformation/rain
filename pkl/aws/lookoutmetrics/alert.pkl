/// AWS::LookoutMetrics::Alert
///
/// Generated by rain build --pkl-class AWS::LookoutMetrics::Alert
module aws.lookoutmetrics.alert

import "../../cloudformation.pkl"

/// Configuration options for a Lambda alert action.
open class LambdaConfiguration {
    RoleArn: Arn
    LambdaArn: Arn
}

/// 
open class Arn {
}

/// 
open class Action {
    LambdaConfiguration: (LambdaConfiguration)?
    SNSConfiguration: (SNSConfiguration)?
}

/// Configuration options for an SNS alert action.
open class SNSConfiguration {
    RoleArn: Arn
    SnsTopicArn: Arn
}

/// Resource Type definition for AWS::LookoutMetrics::Alert
open class Alert extends cloudformation.Resource {

    Type = "AWS::LookoutMetrics::Alert"


    /// The name of the alert. If not provided, a name is generated automatically.
    hidden AlertName: (String(matches(Regex(#"^[a-zA-Z0-9][a-zA-Z0-9\-_]*"#)))|Mapping)?

    /// A description for the alert.
    hidden AlertDescription: (String(matches(Regex(#".*\S.*"#)))|Mapping)?

    /// The Amazon resource name (ARN) of the Anomaly Detector to alert.
    hidden AnomalyDetectorArn: String(matches(Regex(#"arn:([a-z\d-]+):.*:.*:.*:.+"#)))|Mapping

    /// A number between 0 and 100 (inclusive) that tunes the sensitivity of the alert.
    hidden AlertSensitivityThreshold: Int|Mapping

    /// The action to be taken by the alert when an anomaly is detected.
    hidden Action: Action

    Properties {
        ["AlertName"] = if (AlertName == null) null else AlertName
        ["AlertDescription"] = if (AlertDescription == null) null else AlertDescription
        ["AnomalyDetectorArn"] = if (AnomalyDetectorArn == null) null else AnomalyDetectorArn
        ["AlertSensitivityThreshold"] = if (AlertSensitivityThreshold == null) null else AlertSensitivityThreshold
        ["Action"] = if (Action == null) null else Action
    }

}
