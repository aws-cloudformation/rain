/// AWS::ECS::PrimaryTaskSet
///
/// Generated by rain build --pkl-class AWS::ECS::PrimaryTaskSet
module aws.ecs.primarytaskset

import "../../cloudformation.pkl"

/// A pseudo-resource that manages which of your ECS task sets is primary.
open class PrimaryTaskSet extends cloudformation.Resource {

    Type = "AWS::ECS::PrimaryTaskSet"


    /// The short name or full Amazon Resource Name (ARN) of the cluster that hosts the service to create the task set in.
    hidden Cluster: String|Mapping

    /// The short name or full Amazon Resource Name (ARN) of the service to create the task set in.
    hidden Service: String|Mapping

    /// The ID or full Amazon Resource Name (ARN) of the task set.
    hidden TaskSetId: String|Mapping

    Properties {
        ["Cluster"] = if (Cluster == null) null else Cluster
        ["Service"] = if (Service == null) null else Service
        ["TaskSetId"] = if (TaskSetId == null) null else TaskSetId
    }

}
