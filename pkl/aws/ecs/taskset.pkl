/// AWS::ECS::TaskSet
///
/// Generated by rain build --pkl-class AWS::ECS::TaskSet
module aws.ecs.taskset

import "../../cloudformation.pkl"

typealias AwsVpcConfigurationAssignPublicIp = "DISABLED"|"ENABLED"

/// The VPC subnets and security groups associated with a task. All specified subnets and security groups must be from the same VPC.
open class TaskSetAwsVpcConfiguration {
    AssignPublicIp: (AwsVpcConfigurationAssignPublicIp|Mapping)?
    SecurityGroups: (Listing<String|Mapping>)?
    Subnets: Listing<String|Mapping>
}

/// A load balancer object representing the load balancer to use with the task set. The supported load balancer types are either an Application Load Balancer or a Network Load Balancer. 
open class TaskSetLoadBalancer {
    ContainerName: (String|Mapping)?
    ContainerPort: (Int|Mapping)?
    TargetGroupArn: (String|Mapping)?
}

/// An object representing the network configuration for a task or service.
open class TaskSetNetworkConfiguration {
    AwsVpcConfiguration: (TaskSetAwsVpcConfiguration)?
}

typealias ScaleUnit = "PERCENT"

/// 
open class TaskSetScale {
    Unit: (ScaleUnit|Mapping)?
    Value: (Number|Mapping)?
}

/// 
open class TaskSetServiceRegistry {
    ContainerName: (String|Mapping)?
    ContainerPort: (Int|Mapping)?
    Port: (Int|Mapping)?
    RegistryArn: (String|Mapping)?
}

/// 
open class TaskSetTag {
    Key: (String|Mapping)?
    Value: (String|Mapping)?
}

/// Create a task set in the specified cluster and service. This is used when a service uses the EXTERNAL deployment controller type. For more information, see https://docs.aws.amazon.com/AmazonECS/latest/developerguide/deployment-types.htmlin the Amazon Elastic Container Service Developer Guide.
open class TaskSet extends cloudformation.Resource {

    Type = "AWS::ECS::TaskSet"


    /// The short name or full Amazon Resource Name (ARN) of the cluster that hosts the service to create the task set in.
    hidden Cluster: String|Mapping

    /// An optional non-unique tag that identifies this task set in external systems. If the task set is associated with a service discovery registry, the tasks in this task set will have the ECS_TASK_SET_EXTERNAL_ID AWS Cloud Map attribute set to the provided value. 
    hidden ExternalId: (String|Mapping)?

    /// The launch type that new tasks in the task set will use. For more information, see https://docs.aws.amazon.com/AmazonECS/latest/developerguide/launch_types.html in the Amazon Elastic Container Service Developer Guide. 
    hidden LaunchType: (TaskSetLaunchType|Mapping)?

    /// 
    hidden LoadBalancers: (Listing<TaskSetLoadBalancer>)?

    /// 
    hidden NetworkConfiguration: (TaskSetNetworkConfiguration)?

    /// The platform version that the tasks in the task set should use. A platform version is specified only for tasks using the Fargate launch type. If one isn't specified, the LATEST platform version is used by default.
    hidden PlatformVersion: (String|Mapping)?

    /// A floating-point percentage of the desired number of tasks to place and keep running in the task set.
    hidden Scale: (TaskSetScale)?

    /// The short name or full Amazon Resource Name (ARN) of the service to create the task set in.
    hidden Service: String|Mapping

    /// The details of the service discovery registries to assign to this task set. For more information, see https://docs.aws.amazon.com/AmazonECS/latest/developerguide/service-discovery.html.
    hidden ServiceRegistries: (Listing<TaskSetServiceRegistry>)?

    /// 
    hidden Tags: (Listing<TaskSetTag>)?

    /// The short name or full Amazon Resource Name (ARN) of the task definition for the tasks in the task set to use.
    hidden TaskDefinition: String|Mapping

    Properties {
        ["Cluster"] = if (Cluster == null) null else Cluster
        ["ExternalId"] = if (ExternalId == null) null else ExternalId
        ["LaunchType"] = if (LaunchType == null) null else LaunchType
        ["LoadBalancers"] = if (LoadBalancers == null) null else LoadBalancers
        ["NetworkConfiguration"] = if (NetworkConfiguration == null) null else NetworkConfiguration
        ["PlatformVersion"] = if (PlatformVersion == null) null else PlatformVersion
        ["Scale"] = if (Scale == null) null else Scale
        ["Service"] = if (Service == null) null else Service
        ["ServiceRegistries"] = if (ServiceRegistries == null) null else ServiceRegistries
        ["Tags"] = if (Tags == null) null else Tags
        ["TaskDefinition"] = if (TaskDefinition == null) null else TaskDefinition
    }

}


typealias TaskSetLaunchType = "EC2"|"FARGATE"
