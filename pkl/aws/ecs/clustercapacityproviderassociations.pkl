/// AWS::ECS::ClusterCapacityProviderAssociations
///
/// Generated by rain build --pkl-class AWS::ECS::ClusterCapacityProviderAssociations
module aws.ecs.clustercapacityproviderassociations

import "../../cloudformation.pkl"

/// List of capacity providers to associate with the cluster
open class CapacityProviders {
}

/// If using ec2 auto-scaling, the name of the associated capacity provider. Otherwise FARGATE, FARGATE_SPOT.
open class CapacityProvider {
}

/// The name of the cluster
open class Cluster {
}

/// List of capacity providers to associate with the cluster
open class DefaultCapacityProviderStrategy {
}

/// 
open class CapacityProviderStrategy {
    Base: (Int|Mapping)?
    Weight: (Int|Mapping)?
    CapacityProvider: CapacityProvider
}

/// Associate a set of ECS Capacity Providers with a specified ECS Cluster
open class ClusterCapacityProviderAssociations extends cloudformation.Resource {

    Type = "AWS::ECS::ClusterCapacityProviderAssociations"


    /// 
    hidden CapacityProviders: CapacityProviders

    /// 
    hidden Cluster: Cluster

    /// 
    hidden DefaultCapacityProviderStrategy: DefaultCapacityProviderStrategy

    Properties {
        ["CapacityProviders"] = if (CapacityProviders == null) null else CapacityProviders
        ["Cluster"] = if (Cluster == null) null else Cluster
        ["DefaultCapacityProviderStrategy"] = if (DefaultCapacityProviderStrategy == null) null else DefaultCapacityProviderStrategy
    }

}
