/// AWS::IVS::Channel
///
/// Generated by rain build --pkl-class AWS::IVS::Channel
module aws.ivs.channel

import "../../cloudformation.pkl"

/// 
open class ChannelTag {
    Key: String|Mapping
    Value: String|Mapping
}

/// Resource Type definition for AWS::IVS::Channel
open class Channel extends cloudformation.Resource {

    Type = "AWS::IVS::Channel"


    /// Optional transcode preset for the channel. This is selectable only for ADVANCED_HD and ADVANCED_SD channel types. For those channel types, the default preset is HIGHER_BANDWIDTH_DELIVERY. For other channel types (BASIC and STANDARD), preset is the empty string ("").
    hidden Preset: (ChannelPreset|Mapping)?

    /// Whether the channel is authorized.
    hidden Authorized: (Boolean|Mapping)?

    /// Channel type, which determines the allowable resolution and bitrate. If you exceed the allowable resolution or bitrate, the stream probably will disconnect immediately.
    hidden TypeProperty: (ChannelType|Mapping)?

    /// A list of key-value pairs that contain metadata for the asset model.
    hidden Tags: (Listing<ChannelTag>)?

    /// Recording Configuration ARN. A value other than an empty string indicates that recording is enabled. Default: "" (recording is disabled).
    hidden RecordingConfigurationArn: (String(matches(Regex(#"^$|arn:aws:ivs:[a-z0-9-]+:[0-9]+:recording-configuration/[a-zA-Z0-9-]+$"#)))|Mapping)?

    /// Channel
    hidden Name: (String(matches(Regex(#"^[a-zA-Z0-9-_]*$"#)))|Mapping)?

    /// Whether the channel allows insecure ingest.
    hidden InsecureIngest: (Boolean|Mapping)?

    /// Channel latency mode.
    hidden LatencyMode: (ChannelLatencyMode|Mapping)?

    Properties {
        ["Preset"] = if (Preset == null) null else Preset
        ["Authorized"] = if (Authorized == null) null else Authorized
        ["Type"] = if (TypeProperty == null) null else TypeProperty
        ["Tags"] = if (Tags == null) null else Tags
        ["RecordingConfigurationArn"] = if (RecordingConfigurationArn == null) null else RecordingConfigurationArn
        ["Name"] = if (Name == null) null else Name
        ["InsecureIngest"] = if (InsecureIngest == null) null else InsecureIngest
        ["LatencyMode"] = if (LatencyMode == null) null else LatencyMode
    }

}


typealias ChannelPreset = "HIGHER_BANDWIDTH_DELIVERY"|"CONSTRAINED_BANDWIDTH_DELIVERY"

typealias ChannelType = "STANDARD"|"BASIC"|"ADVANCED_SD"|"ADVANCED_HD"

typealias ChannelLatencyMode = "NORMAL"|"LOW"
