/// AWS::IVS::PlaybackKeyPair
///
/// Generated by rain build --pkl-class AWS::IVS::PlaybackKeyPair
module aws.ivs.playbackkeypair

import "../../cloudformation.pkl"

/// 
open class PlaybackKeyPairTag {
    Key: String|Mapping
    Value: String|Mapping
}

/// Resource Type definition for AWS::IVS::PlaybackKeyPair
open class PlaybackKeyPair extends cloudformation.Resource {

    Type = "AWS::IVS::PlaybackKeyPair"


    /// An arbitrary string (a nickname) assigned to a playback key pair that helps the customer identify that resource. The value does not need to be unique.
    hidden Name: (String(matches(Regex(#"^[a-zA-Z0-9-_]*$"#)))|Mapping)?

    /// The public portion of a customer-generated key pair.
    hidden PublicKeyMaterial: (String|Mapping)?

    /// A list of key-value pairs that contain metadata for the asset model.
    hidden Tags: (Listing<PlaybackKeyPairTag>)?

    Properties {
        ["Name"] = if (Name == null) null else Name
        ["PublicKeyMaterial"] = if (PublicKeyMaterial == null) null else PublicKeyMaterial
        ["Tags"] = if (Tags == null) null else Tags
    }

}
