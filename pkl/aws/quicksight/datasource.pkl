/// AWS::QuickSight::DataSource
///
/// Generated by rain build --pkl-class AWS::QuickSight::DataSource
module aws.quicksight.datasource

import "../../cloudformation.pkl"

/// 
open class DataSourceType {
}

/// <p>The key or keys of the key-value pairs for the resource tag or tags assigned to the
//////             resource.</p>
open class Tag {
    Value: String|Mapping
    Key: String|Mapping
}

/// <p>Amazon Aurora parameters.</p>
open class AuroraParameters {
    Host: String|Mapping
    Port: Number|Mapping
    Database: String|Mapping
}

/// <p>AWS IoT Analytics parameters.</p>
open class AwsIotAnalyticsParameters {
    DataSetName: String|Mapping
}

/// <p>The combination of user name and password that are used as credentials.</p>
open class CredentialPair {
    AlternateDataSourceParameters: (Listing<DataSourceParameters>)?
    Username: String|Mapping
    Password: String|Mapping
}

/// <p>Data source credentials. This is a variant type structure. For this structure to be
//////             valid, only one of the attributes can be non-null.</p>
open class DataSourceCredentials {
    CredentialPair: (CredentialPair)?
    SecretArn: (String(matches(Regex(#"^arn:[-a-z0-9]*:secretsmanager:[-a-z0-9]*:[0-9]{12}:secret:.+"#)))|Mapping)?
    CopySourceArn: (String(matches(Regex(#"^arn:[-a-z0-9]*:quicksight:[-a-z0-9]*:[0-9]{12}:datasource/.+"#)))|Mapping)?
}

/// <p>Amazon RDS parameters.</p>
open class RdsParameters {
    InstanceId: String|Mapping
    Database: String|Mapping
}

/// 
open class StarburstProductType {
}

/// <p>Secure Socket Layer (SSL) properties that apply when QuickSight connects to your
//////             underlying data source.</p>
open class SslProperties {
    DisableSsl: (Boolean|Mapping)?
}

/// <p>Teradata parameters.</p>
open class TeradataParameters {
    Host: String|Mapping
    Port: Number|Mapping
    Database: String|Mapping
}

/// <p>Amazon Elasticsearch Service parameters.</p>
open class AmazonElasticsearchParameters {
    Domain: String|Mapping
}

/// <p>MariaDB parameters.</p>
open class MariaDbParameters {
    Port: Number|Mapping
    Database: String|Mapping
    Host: String|Mapping
}

/// <p>MySQL parameters.</p>
open class MySqlParameters {
    Port: Number|Mapping
    Database: String|Mapping
    Host: String|Mapping
}

/// <p>Starburst parameters.</p>
open class StarburstParameters {
    Port: Number|Mapping
    Catalog: String|Mapping
    ProductType: (StarburstProductType)?
    Host: String|Mapping
}

/// <p>Error information for the data source creation or update.</p>
open class DataSourceErrorInfo {
    Type: (DataSourceErrorInfoType)?
    Message: (String|Mapping)?
}

/// <p>PostgreSQL parameters.</p>
open class PostgreSqlParameters {
    Port: Number|Mapping
    Database: String|Mapping
    Host: String|Mapping
}

/// 
open class ResourceStatus {
}

/// <p>Snowflake parameters.</p>
open class SnowflakeParameters {
    Database: String|Mapping
    Host: String|Mapping
    Warehouse: String|Mapping
}

/// <p>Amazon Aurora with PostgreSQL compatibility parameters.</p>
open class AuroraPostgreSqlParameters {
    Port: Number|Mapping
    Database: String|Mapping
    Host: String|Mapping
}

/// <p>The parameters that Amazon QuickSight uses to connect to your underlying data source.
//////             This is a variant type structure. For this structure to be valid, only one of the
///             attributes can be non-null.</p>
open class DataSourceParameters {
    AuroraPostgreSqlParameters: (AuroraPostgreSqlParameters)?
    MariaDbParameters: (MariaDbParameters)?
    AmazonOpenSearchParameters: (AmazonOpenSearchParameters)?
    S3Parameters: (S3Parameters)?
    TrinoParameters: (TrinoParameters)?
    RdsParameters: (RdsParameters)?
    PrestoParameters: (PrestoParameters)?
    RedshiftParameters: (RedshiftParameters)?
    OracleParameters: (OracleParameters)?
    MySqlParameters: (MySqlParameters)?
    SqlServerParameters: (SqlServerParameters)?
    SnowflakeParameters: (SnowflakeParameters)?
    StarburstParameters: (StarburstParameters)?
    TeradataParameters: (TeradataParameters)?
    AthenaParameters: (AthenaParameters)?
    SparkParameters: (SparkParameters)?
    DatabricksParameters: (DatabricksParameters)?
    AmazonElasticsearchParameters: (AmazonElasticsearchParameters)?
    PostgreSqlParameters: (PostgreSqlParameters)?
    AuroraParameters: (AuroraParameters)?
}

/// <p>Presto parameters.</p>
open class PrestoParameters {
    Port: Number|Mapping
    Host: String|Mapping
    Catalog: String|Mapping
}

/// <p>Permission for the resource.</p>
open class ResourcePermission {
    Actions: Listing<String|Mapping>
    Principal: String|Mapping
}

/// <p>Amazon OpenSearch Service parameters.</p>
open class AmazonOpenSearchParameters {
    Domain: String|Mapping
}

/// <p>Databricks parameters.</p>
open class DatabricksParameters {
    Host: String|Mapping
    Port: Number|Mapping
    SqlEndpointPath: String|Mapping
}

/// <p>S3 parameters.</p>
open class S3Parameters {
    ManifestFileLocation: ManifestFileLocation
    RoleArn: (String|Mapping)?
}

/// <p>Spark parameters.</p>
open class SparkParameters {
    Port: Number|Mapping
    Host: String|Mapping
}

/// <p>VPC connection properties.</p>
open class VpcConnectionProperties {
    VpcConnectionArn: String|Mapping
}

/// 
open class OracleParameters {
    Port: Number|Mapping
    Database: String|Mapping
    Host: String|Mapping
}

/// <p>Amazon Redshift parameters. The <code>ClusterId</code> field can be blank if
//////             <code>Host</code> and <code>Port</code> are both set. The <code>Host</code> and
///             <code>Port</code> fields can be blank if the <code>ClusterId</code> field is set.</p>
open class RedshiftParameters {
    ClusterId: (String|Mapping)?
    Port: (Number|Mapping)?
    Database: String|Mapping
    Host: (String|Mapping)?
}

/// <p>Trino parameters.</p>
open class TrinoParameters {
    Host: String|Mapping
    Port: Number|Mapping
    Catalog: String|Mapping
}

/// <p>SQL Server parameters.</p>
open class SqlServerParameters {
    Database: String|Mapping
    Host: String|Mapping
    Port: Number|Mapping
}

/// <p>Amazon Athena parameters.</p>
open class AthenaParameters {
    WorkGroup: (String|Mapping)?
    RoleArn: (String|Mapping)?
}

/// 
open class DataSourceErrorInfoType {
}

/// <p>Amazon S3 manifest file location.</p>
open class ManifestFileLocation {
    Key: String|Mapping
    Bucket: String|Mapping
}

/// Definition of the AWS::QuickSight::DataSource Resource Type.
open class DataSource extends cloudformation.Resource {

    Type = "AWS::QuickSight::DataSource"


    /// <p>A set of alternate data source parameters that you want to share for the credentials
    ///    ///             stored with this data source. The credentials are applied in tandem with the data source
    ///             parameters when you copy a data source by using a create or update request. The API
    ///             operation compares the <code>DataSourceParameters</code> structure that's in the request
    ///             with the structures in the <code>AlternateDataSourceParameters</code> allow list. If the
    ///             structures are an exact match, the request is allowed to use the credentials from this
    ///             existing data source. If the <code>AlternateDataSourceParameters</code> list is null,
    ///             the <code>Credentials</code> originally used with this <code>DataSourceParameters</code>
    ///             are automatically allowed.</p>
    hidden AlternateDataSourceParameters: (Listing<DataSourceParameters>)?

    /// 
    hidden Credentials: (DataSourceCredentials)?

    /// 
    hidden DataSourceId: (String|Mapping)?

    /// <p>A display name for the data source.</p>
    hidden Name: (String|Mapping)?

    /// 
    hidden TypeProperty: (DataSourceType)?

    /// 
    hidden ErrorInfo: (DataSourceErrorInfo)?

    /// <p>A list of resource permissions on the data source.</p>
    hidden Permissions: (Listing<ResourcePermission>)?

    /// 
    hidden AwsAccountId: (String(matches(Regex(#"^[0-9]{12}$"#)))|Mapping)?

    /// 
    hidden SslProperties: (SslProperties)?

    /// 
    hidden DataSourceParameters: (DataSourceParameters)?

    /// <p>Contains a map of the key-value pairs for the resource tag or tags assigned to the data source.</p>
    hidden Tags: (Listing<Tag>)?

    /// 
    hidden VpcConnectionProperties: (VpcConnectionProperties)?

    Properties {
        ["AlternateDataSourceParameters"] = if (AlternateDataSourceParameters == null) null else AlternateDataSourceParameters
        ["Credentials"] = if (Credentials == null) null else Credentials
        ["DataSourceId"] = if (DataSourceId == null) null else DataSourceId
        ["Name"] = if (Name == null) null else Name
        ["Type"] = if (TypeProperty == null) null else TypeProperty
        ["ErrorInfo"] = if (ErrorInfo == null) null else ErrorInfo
        ["Permissions"] = if (Permissions == null) null else Permissions
        ["AwsAccountId"] = if (AwsAccountId == null) null else AwsAccountId
        ["SslProperties"] = if (SslProperties == null) null else SslProperties
        ["DataSourceParameters"] = if (DataSourceParameters == null) null else DataSourceParameters
        ["Tags"] = if (Tags == null) null else Tags
        ["VpcConnectionProperties"] = if (VpcConnectionProperties == null) null else VpcConnectionProperties
    }

}
