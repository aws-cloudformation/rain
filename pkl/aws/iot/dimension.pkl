/// AWS::IoT::Dimension
///
/// Generated by rain build --pkl-class AWS::IoT::Dimension
module aws.iot.dimension

import "../../cloudformation.pkl"

/// A key-value pair to associate with a resource.
open class DimensionTag {
    Key: String|Mapping
    Value: String|Mapping
}

/// A dimension can be used to limit the scope of a metric used in a security profile for AWS IoT Device Defender.
open class Dimension extends cloudformation.Resource {

    Type = "AWS::IoT::Dimension"


    /// Metadata that can be used to manage the dimension.
    hidden Tags: (Listing<DimensionTag>)?

    /// A unique identifier for the dimension.
    hidden Name: (String(matches(Regex(#"[a-zA-Z0-9:_-]+"#)))|Mapping)?

    /// Specifies the type of the dimension.
    hidden TypeProperty: DimensionType|Mapping

    /// Specifies the value or list of values for the dimension.
    hidden StringValues: Listing<String|Mapping>

    Properties {
        ["Tags"] = if (Tags == null) null else Tags
        ["Name"] = if (Name == null) null else Name
        ["Type"] = if (TypeProperty == null) null else TypeProperty
        ["StringValues"] = if (StringValues == null) null else StringValues
    }

}


typealias DimensionType = "TOPIC_FILTER"
