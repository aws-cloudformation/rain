/// AWS::IoT::Thing
///
/// Generated by rain build --pkl-class AWS::IoT::Thing
module aws.iot.thing

import "../../cloudformation.pkl"

typealias ThingAttributePayloadAttributes = Mapping<String, Any>

/// 
open class ThingAttributePayload {
    Attributes: (ThingAttributePayloadAttributes)?
}

/// Resource Type definition for AWS::IoT::Thing
open class Thing extends cloudformation.Resource {

    Type = "AWS::IoT::Thing"


    /// 
    hidden AttributePayload: (ThingAttributePayload)?

    /// 
    hidden ThingName: (String(matches(Regex(#"[a-zA-Z0-9:_-]+"#)))|Mapping)?

    Properties {
        ["AttributePayload"] = if (AttributePayload == null) null else AttributePayload
        ["ThingName"] = if (ThingName == null) null else ThingName
    }

}
