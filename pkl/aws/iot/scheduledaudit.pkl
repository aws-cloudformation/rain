/// AWS::IoT::ScheduledAudit
///
/// Generated by rain build --pkl-class AWS::IoT::ScheduledAudit
module aws.iot.scheduledaudit

import "../../cloudformation.pkl"

/// A key-value pair to associate with a resource.
open class ScheduledAuditTag {
    Key: String|Mapping
    Value: String|Mapping
}

/// Scheduled audits can be used to specify the checks you want to perform during an audit and how often the audit should be run.
open class ScheduledAudit extends cloudformation.Resource {

    Type = "AWS::IoT::ScheduledAudit"


    /// The day of the month on which the scheduled audit takes place. Can be 1 through 31 or LAST. This field is required if the frequency parameter is set to MONTHLY.
    hidden DayOfMonth: (String(matches(Regex(#"^([1-9]|[12][0-9]|3[01])$|^LAST$|^UNSET_VALUE$"#)))|Mapping)?

    /// The day of the week on which the scheduled audit takes place. Can be one of SUN, MON, TUE,WED, THU, FRI, or SAT. This field is required if the frequency parameter is set to WEEKLY or BIWEEKLY.
    hidden DayOfWeek: (ScheduledAuditDayOfWeek|Mapping)?

    /// How often the scheduled audit takes place. Can be one of DAILY, WEEKLY, BIWEEKLY, or MONTHLY.
    hidden Frequency: ScheduledAuditFrequency|Mapping

    /// The name you want to give to the scheduled audit.
    hidden ScheduledAuditName: (String(matches(Regex(#"[a-zA-Z0-9:_-]+"#)))|Mapping)?

    /// An array of key-value pairs to apply to this resource.
    hidden Tags: (Listing<ScheduledAuditTag>)?

    /// Which checks are performed during the scheduled audit. Checks must be enabled for your account.
    hidden TargetCheckNames: Listing<String|Mapping>

    Properties {
        ["DayOfMonth"] = if (DayOfMonth == null) null else DayOfMonth
        ["DayOfWeek"] = if (DayOfWeek == null) null else DayOfWeek
        ["Frequency"] = if (Frequency == null) null else Frequency
        ["ScheduledAuditName"] = if (ScheduledAuditName == null) null else ScheduledAuditName
        ["Tags"] = if (Tags == null) null else Tags
        ["TargetCheckNames"] = if (TargetCheckNames == null) null else TargetCheckNames
    }

}


typealias ScheduledAuditDayOfWeek = "SUN"|"MON"|"TUE"|"WED"|"THU"|"FRI"|"SAT"|"UNSET_VALUE"

typealias ScheduledAuditFrequency = "DAILY"|"WEEKLY"|"BIWEEKLY"|"MONTHLY"
