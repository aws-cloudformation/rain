/// AWS::IoT::Logging
///
/// Generated by rain build --pkl-class AWS::IoT::Logging
module aws.iot.logging

import "../../cloudformation.pkl"

/// Logging Options enable you to configure your IoT V2 logging role and default logging level so that you can monitor progress events logs as it passes from your devices through Iot core service.
open class Logging extends cloudformation.Resource {

    Type = "AWS::IoT::Logging"


    /// Your 12-digit account ID (used as the primary identifier for the CloudFormation resource).
    hidden AccountId: String(matches(Regex(#"^[0-9]{12}$"#)))|Mapping

    /// The log level to use. Valid values are: ERROR, WARN, INFO, DEBUG, or DISABLED.
    hidden DefaultLogLevel: LoggingDefaultLogLevel|Mapping

    /// The ARN of the role that allows IoT to write to Cloudwatch logs.
    hidden RoleArn: String|Mapping

    Properties {
        ["AccountId"] = if (AccountId == null) null else AccountId
        ["DefaultLogLevel"] = if (DefaultLogLevel == null) null else DefaultLogLevel
        ["RoleArn"] = if (RoleArn == null) null else RoleArn
    }

}


typealias LoggingDefaultLogLevel = "ERROR"|"WARN"|"INFO"|"DEBUG"|"DISABLED"
