/// AWS::IoT::CustomMetric
///
/// Generated by rain build --pkl-class AWS::IoT::CustomMetric
module aws.iot.custommetric

import "../../cloudformation.pkl"

/// A key-value pair to associate with a resource.
open class CustomMetricTag {
    Key: String|Mapping
    Value: String|Mapping
}

/// A custom metric published by your devices to Device Defender.
open class CustomMetric extends cloudformation.Resource {

    Type = "AWS::IoT::CustomMetric"


    /// Field represents a friendly name in the console for the custom metric; it doesn't have to be unique. Don't use this name as the metric identifier in the device metric report. Can be updated once defined.
    hidden DisplayName: (String|Mapping)?

    /// The name of the custom metric. This will be used in the metric report submitted from the device/thing. Shouldn't begin with aws: . Cannot be updated once defined.
    hidden MetricName: (String(matches(Regex(#"[a-zA-Z0-9:_-]+"#)))|Mapping)?

    /// The type of the custom metric. Types include string-list, ip-address-list, number-list, and number.
    hidden MetricType: CustomMetricMetricType|Mapping

    /// An array of key-value pairs to apply to this resource.
    hidden Tags: (Listing<CustomMetricTag>)?

    Properties {
        ["DisplayName"] = if (DisplayName == null) null else DisplayName
        ["MetricName"] = if (MetricName == null) null else MetricName
        ["MetricType"] = if (MetricType == null) null else MetricType
        ["Tags"] = if (Tags == null) null else Tags
    }

}


typealias CustomMetricMetricType = "string-list"|"ip-address-list"|"number-list"|"number"
