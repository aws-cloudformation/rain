/// AWS::IoT::Certificate
///
/// Generated by rain build --pkl-class AWS::IoT::Certificate
module aws.iot.certificate

import "../../cloudformation.pkl"

/// Use the AWS::IoT::Certificate resource to declare an AWS IoT X.509 certificate.
open class Certificate extends cloudformation.Resource {

    Type = "AWS::IoT::Certificate"


    /// 
    hidden CACertificatePem: (String|Mapping)?

    /// 
    hidden CertificateMode: (CertificateCertificateMode|Mapping)?

    /// 
    hidden CertificatePem: (String|Mapping)?

    /// 
    hidden CertificateSigningRequest: (String|Mapping)?

    /// 
    hidden Status: CertificateStatus|Mapping

    Properties {
        ["CACertificatePem"] = if (CACertificatePem == null) null else CACertificatePem
        ["CertificateMode"] = if (CertificateMode == null) null else CertificateMode
        ["CertificatePem"] = if (CertificatePem == null) null else CertificatePem
        ["CertificateSigningRequest"] = if (CertificateSigningRequest == null) null else CertificateSigningRequest
        ["Status"] = if (Status == null) null else Status
    }

}


typealias CertificateCertificateMode = "DEFAULT"|"SNI_ONLY"

typealias CertificateStatus = "ACTIVE"|"INACTIVE"|"REVOKED"|"PENDING_TRANSFER"|"PENDING_ACTIVATION"
