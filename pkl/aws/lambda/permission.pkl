/// AWS::Lambda::Permission
///
/// Generated by rain build --pkl-class AWS::Lambda::Permission
module aws.lambda.permission

import "../../cloudformation.pkl"

/// The ``AWS::Lambda::Permission`` resource grants an AWS service or another account permission to use a function. You can apply the policy at the function level, or specify a qualifier to restrict access to a single version or alias. If you use a qualifier, the invoker must use the full Amazon Resource Name (ARN) of that version or alias to invoke the function.
///
///  To grant permission to another account, specify the account ID as the ``Principal``. To grant permission to an organization defined in AOlong, specify the organization ID as the ``PrincipalOrgID``. For AWS services, the principal is a domain-style identifier defined by the service, like ``s3.amazonaws.com`` or ``sns.amazonaws.com``. For AWS services, you can also specify the ARN of the associated resource as the ``SourceArn``. If you grant permission to a service principal without specifying the source, other accounts could potentially configure resources in their account to invoke your Lambda function.
///  If your function has a fu
open class Permission extends cloudformation.Resource {

    Type = "AWS::Lambda::Permission"


    /// The action that the principal can use on the function. For example, ``lambda:InvokeFunction`` or ``lambda:GetFunction``.
    hidden Action: String(matches(Regex(#"^(lambda:[*]|lambda:[a-zA-Z]+|[*])$"#)))|Mapping

    /// For Alexa Smart Home functions, a token that the invoker must supply.
    hidden EventSourceToken: (String(matches(Regex(#"^[a-zA-Z0-9._\-]+$"#)))|Mapping)?

    /// The name of the Lambda function, version, or alias.
    ///
    ///   **Name formats**
    ///  +   *Function name* ? ``my-function`` (name-only), ``my-function:v1`` (with alias).
    ///   +   *Function ARN* ? ``arn:aws:lambda:us-west-2:123456789012:function:my-function``.
    ///   +   *Partial ARN* ? ``123456789012:function:my-function``.
    ///   
    ///  You can append a version number or alias to any of the formats. The length constraint applies only to the full ARN. If you specify only the function name, it is limited to 64 characters in length.
    hidden FunctionName: String(matches(Regex(#"^(arn:(aws[a-zA-Z-]*)?:lambda:)?([a-z]{2}((-gov)|(-iso([a-z]?)))?-[a-z]+-\d{1}:)?(\d{12}:)?(function:)?([a-zA-Z0-9-_]+)(:(\$LATEST|[a-zA-Z0-9-_]+))?$"#)))|Mapping

    /// The type of authentication that your function URL uses. Set to ``AWS_IAM`` if you want to restrict access to authenticated users only. Set to ``NONE`` if you want to bypass IAM authentication to create a public endpoint. For more information, see [Security and auth model for Lambda function URLs](https://docs.aws.amazon.com/lambda/latest/dg/urls-auth.html).
    hidden FunctionUrlAuthType: (PermissionFunctionUrlAuthType|Mapping)?

    /// The AWS-service or AWS-account that invokes the function. If you specify a service, use ``SourceArn`` or ``SourceAccount`` to limit who can invoke the function through that service.
    hidden Principal: String(matches(Regex(#"^.*$"#)))|Mapping

    /// The identifier for your organization in AOlong. Use this to grant permissions to all the AWS-accounts under this organization.
    hidden PrincipalOrgID: (String(matches(Regex(#"^o-[a-z0-9]{10,32}$"#)))|Mapping)?

    /// For AWS-service, the ID of the AWS-account that owns the resource. Use this together with ``SourceArn`` to ensure that the specified account owns the resource. It is possible for an Amazon S3 bucket to be deleted by its owner and recreated by another account.
    hidden SourceAccount: (String(matches(Regex(#"^\d{12}$"#)))|Mapping)?

    /// For AWS-services, the ARN of the AWS resource that invokes the function. For example, an Amazon S3 bucket or Amazon SNS topic.
    ///
    ///  Note that Lambda configures the comparison using the ``StringLike`` operator.
    hidden SourceArn: (String(matches(Regex(#"^arn:(aws[a-zA-Z0-9-]*):([a-zA-Z0-9\-])+:([a-z]{2}((-gov)|(-iso([a-z]?)))?-[a-z]+-\d{1})?:(\d{12})?:(.*)$"#)))|Mapping)?

    Properties {
        ["Action"] = if (Action == null) null else Action
        ["EventSourceToken"] = if (EventSourceToken == null) null else EventSourceToken
        ["FunctionName"] = if (FunctionName == null) null else FunctionName
        ["FunctionUrlAuthType"] = if (FunctionUrlAuthType == null) null else FunctionUrlAuthType
        ["Principal"] = if (Principal == null) null else Principal
        ["PrincipalOrgID"] = if (PrincipalOrgID == null) null else PrincipalOrgID
        ["SourceAccount"] = if (SourceAccount == null) null else SourceAccount
        ["SourceArn"] = if (SourceArn == null) null else SourceArn
    }

}


typealias PermissionFunctionUrlAuthType = "AWS_IAM"|"NONE"
