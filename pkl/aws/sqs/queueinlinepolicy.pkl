/// AWS::SQS::QueueInlinePolicy
///
/// Generated by rain build --pkl-class AWS::SQS::QueueInlinePolicy
module aws.sqs.queueinlinepolicy

import "../../cloudformation.pkl"

/// Schema for SQS QueueInlinePolicy
open class QueueInlinePolicy extends cloudformation.Resource {

    Type = "AWS::SQS::QueueInlinePolicy"


    /// A policy document that contains permissions to add to the specified SQS queue
    hidden PolicyDocument: Dynamic

    /// The URL of the SQS queue.
    hidden Queue: String|Mapping

    Properties {
        ["PolicyDocument"] = if (PolicyDocument == null) null else PolicyDocument
        ["Queue"] = if (Queue == null) null else Queue
    }

}
