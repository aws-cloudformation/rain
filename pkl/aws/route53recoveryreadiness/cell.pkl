/// AWS::Route53RecoveryReadiness::Cell
///
/// Generated by rain build --pkl-class AWS::Route53RecoveryReadiness::Cell
module aws.route53recoveryreadiness.cell

import "../../cloudformation.pkl"

/// 
open class CellTag {
    Key: String|Mapping
    Value: String|Mapping
}

/// The API Schema for AWS Route53 Recovery Readiness Cells.
open class Cell extends cloudformation.Resource {

    Type = "AWS::Route53RecoveryReadiness::Cell"


    /// The name of the cell to create.
    hidden CellName: (String(matches(Regex(#"[a-zA-Z0-9_]+"#)))|Mapping)?

    /// A list of cell Amazon Resource Names (ARNs) contained within this cell, for use in nested cells. For example, Availability Zones within specific Regions.
    hidden Cells: (Listing<String|Mapping>)?

    /// A collection of tags associated with a resource
    hidden Tags: (Listing<CellTag>)?

    Properties {
        ["CellName"] = if (CellName == null) null else CellName
        ["Cells"] = if (Cells == null) null else Cells
        ["Tags"] = if (Tags == null) null else Tags
    }

}
