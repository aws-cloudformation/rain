/// AWS::S3::AccessPoint
///
/// Generated by rain build --pkl-class AWS::S3::AccessPoint
module aws.s3.accesspoint

import "../../cloudformation.pkl"

typealias AccessPointArn = String|Mapping


/// 
open class AccessPointPublicAccessBlockConfiguration {
    BlockPublicAcls: (Boolean|Mapping)?
    BlockPublicPolicy: (Boolean|Mapping)?
    IgnorePublicAcls: (Boolean|Mapping)?
    RestrictPublicBuckets: (Boolean|Mapping)?
}

/// The Virtual Private Cloud (VPC) configuration for a bucket access point.
open class AccessPointVpcConfiguration {
    VpcId: (String|Mapping)?
}

/// The AWS::S3::AccessPoint resource is an Amazon S3 resource type that you can use to access buckets.
open class AccessPoint extends cloudformation.Resource {

    Type = "AWS::S3::AccessPoint"


    /// The name of the bucket that you want to associate this Access Point with.
    hidden Bucket: String|Mapping

    /// The AWS account ID associated with the S3 bucket associated with this access point.
    hidden BucketAccountId: (String(matches(Regex(#"^\d{12}$"#)))|Mapping)?

    /// The name you want to assign to this Access Point. If you don't specify a name, AWS CloudFormation generates a unique ID and uses that ID for the access point name.
    hidden Name: (String(matches(Regex(#"^[a-z0-9]([a-z0-9\-]*[a-z0-9])?$"#)))|Mapping)?

    /// The Access Point Policy you want to apply to this access point.
    hidden Policy: (Dynamic)?

    /// The PublicAccessBlock configuration that you want to apply to this Access Point. You can enable the configuration options in any combination. For more information about when Amazon S3 considers a bucket or object public, see https://docs.aws.amazon.com/AmazonS3/latest/dev/access-control-block-public-access.html#access-control-block-public-access-policy-status 'The Meaning of Public' in the Amazon Simple Storage Service Developer Guide.
    hidden PublicAccessBlockConfiguration: (AccessPointPublicAccessBlockConfiguration)?

    /// If you include this field, Amazon S3 restricts access to this Access Point to requests from the specified Virtual Private Cloud (VPC).
    hidden VpcConfiguration: (AccessPointVpcConfiguration)?

    Properties {
        ["Bucket"] = if (Bucket == null) null else Bucket
        ["BucketAccountId"] = if (BucketAccountId == null) null else BucketAccountId
        ["Name"] = if (Name == null) null else Name
        ["Policy"] = if (Policy == null) null else Policy
        ["PublicAccessBlockConfiguration"] = if (PublicAccessBlockConfiguration == null) null else PublicAccessBlockConfiguration
        ["VpcConfiguration"] = if (VpcConfiguration == null) null else VpcConfiguration
    }

}
