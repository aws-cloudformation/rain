/// AWS::S3::AccessGrant
///
/// Generated by rain build --pkl-class AWS::S3::AccessGrant
module aws.s3.accessgrant

import "../../cloudformation.pkl"

typealias AccessGrantAccessGrantArn = String|Mapping


/// 
open class AccessGrantAccessGrantsLocationConfiguration {
    S3SubPrefix: String|Mapping
}

typealias GranteeGranteeType = "IAM"|"DIRECTORY_USER"|"DIRECTORY_GROUP"

/// 
open class AccessGrantGrantee {
    GranteeIdentifier: String|Mapping
    GranteeType: GranteeGranteeType|Mapping
}

/// 
open class AccessGrantTag {
    Key: String|Mapping
    Value: String|Mapping
}

/// The AWS::S3::AccessGrant resource is an Amazon S3 resource type representing permissions to a specific S3 bucket or prefix hosted in an S3 Access Grants instance.
open class AccessGrant extends cloudformation.Resource {

    Type = "AWS::S3::AccessGrant"


    /// The configuration options of the grant location, which is the S3 path to the data to which you are granting access.
    hidden AccessGrantsLocationConfiguration: (AccessGrantAccessGrantsLocationConfiguration)?

    /// The custom S3 location to be accessed by the grantee
    hidden AccessGrantsLocationId: String|Mapping

    /// The ARN of the application grantees will use to access the location
    hidden ApplicationArn: (String|Mapping)?

    /// The principal who will be granted permission to access S3.
    hidden Grantee: AccessGrantGrantee

    /// The level of access to be afforded to the grantee
    hidden Permission: AccessGrantPermission|Mapping

    /// The type of S3SubPrefix.
    hidden S3PrefixType: (AccessGrantS3PrefixType|Mapping)?

    /// 
    hidden Tags: (Listing<AccessGrantTag>)?

    Properties {
        ["AccessGrantsLocationConfiguration"] = if (AccessGrantsLocationConfiguration == null) null else AccessGrantsLocationConfiguration
        ["AccessGrantsLocationId"] = if (AccessGrantsLocationId == null) null else AccessGrantsLocationId
        ["ApplicationArn"] = if (ApplicationArn == null) null else ApplicationArn
        ["Grantee"] = if (Grantee == null) null else Grantee
        ["Permission"] = if (Permission == null) null else Permission
        ["S3PrefixType"] = if (S3PrefixType == null) null else S3PrefixType
        ["Tags"] = if (Tags == null) null else Tags
    }

}


typealias AccessGrantPermission = "READ"|"WRITE"|"READWRITE"

typealias AccessGrantS3PrefixType = "Object"
