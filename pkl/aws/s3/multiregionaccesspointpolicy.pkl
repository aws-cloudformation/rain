/// AWS::S3::MultiRegionAccessPointPolicy
///
/// Generated by rain build --pkl-class AWS::S3::MultiRegionAccessPointPolicy
module aws.s3.multiregionaccesspointpolicy

import "../../cloudformation.pkl"

/// The policy to be attached to a Multi Region Access Point
open class MultiRegionAccessPointPolicy extends cloudformation.Resource {

    Type = "AWS::S3::MultiRegionAccessPointPolicy"


    /// The name of the Multi Region Access Point to apply policy
    hidden MrapName: String(matches(Regex(#"^[a-z0-9][-a-z0-9]{1,48}[a-z0-9]$"#)))|Mapping

    /// Policy document to apply to a Multi Region Access Point
    hidden Policy: Dynamic

    Properties {
        ["MrapName"] = if (MrapName == null) null else MrapName
        ["Policy"] = if (Policy == null) null else Policy
    }

}
