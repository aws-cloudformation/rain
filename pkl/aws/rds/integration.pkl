/// AWS::RDS::Integration
///
/// Generated by rain build --pkl-class AWS::RDS::Integration
module aws.rds.integration

import "../../cloudformation.pkl"

typealias IntegrationEncryptionContextMap = Mapping<String, Any>


/// A key-value pair to associate with a resource.
open class IntegrationTag {
    Key: String|Mapping
    Value: (String|Mapping)?
}

typealias IntegrationTags = Listing<(IntegrationTag)?>


/// Creates a zero-ETL integration with Amazon Redshift.
open class Integration extends cloudformation.Resource {

    Type = "AWS::RDS::Integration"


    /// 
    hidden AdditionalEncryptionContext: (IntegrationEncryptionContextMap)?

    /// The data filter for the integration.
    hidden DataFilter: (String(matches(Regex(#"[a-zA-Z0-9_ "\\\-$,*.:?+\/]*"#)))|Mapping)?

    /// The description of the integration.
    hidden Description: (String|Mapping)?

    /// The name of the integration.
    hidden IntegrationName: (String|Mapping)?

    /// An optional AWS Key Management System (AWS KMS) key ARN for the key used to to encrypt the integration. The resource accepts the key ID and the key ARN forms. The key ID form can be used if the KMS key is owned by te same account. If the KMS key belongs to a different account than the calling account, the full key ARN must be specified. Do not use the key alias or the key alias ARN as this will cause a false drift of the resource.
    hidden KMSKeyId: (String|Mapping)?

    /// The Amazon Resource Name (ARN) of the Aurora DB cluster to use as the source for replication.
    hidden SourceArn: String|Mapping

    /// An array of key-value pairs to apply to this resource.
    hidden Tags: (Listing<IntegrationTag>)?

    /// The ARN of the Redshift data warehouse to use as the target for replication.
    hidden TargetArn: String|Mapping

    Properties {
        ["AdditionalEncryptionContext"] = if (AdditionalEncryptionContext == null) null else AdditionalEncryptionContext
        ["DataFilter"] = if (DataFilter == null) null else DataFilter
        ["Description"] = if (Description == null) null else Description
        ["IntegrationName"] = if (IntegrationName == null) null else IntegrationName
        ["KMSKeyId"] = if (KMSKeyId == null) null else KMSKeyId
        ["SourceArn"] = if (SourceArn == null) null else SourceArn
        ["Tags"] = if (Tags == null) null else Tags
        ["TargetArn"] = if (TargetArn == null) null else TargetArn
    }

}
