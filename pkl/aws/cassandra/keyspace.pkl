/// AWS::Cassandra::Keyspace
///
/// Generated by rain build --pkl-class AWS::Cassandra::Keyspace
module aws.cassandra.keyspace

import "../../cloudformation.pkl"

/// 
open class Tag {
    Key: String|Mapping
    Value: String|Mapping
}

typealias ReplicationSpecificationReplicationStrategy = "SINGLE_REGION"|"MULTI_REGION"

/// 
open class ReplicationSpecification {
    ReplicationStrategy: (ReplicationSpecificationReplicationStrategy|Mapping)?
    RegionList: (RegionList)?
}

/// 
open class RegionList {
}

/// Resource schema for AWS::Cassandra::Keyspace
open class Keyspace extends cloudformation.Resource {

    Type = "AWS::Cassandra::Keyspace"


    /// Name for Cassandra keyspace
    hidden KeyspaceName: (String(matches(Regex(#"^[a-zA-Z0-9][a-zA-Z0-9_]{1,47}$"#)))|Mapping)?

    /// 
    hidden Tags: (Listing<Tag>)?

    /// 
    hidden ReplicationSpecification: (ReplicationSpecification)?

    Properties {
        ["KeyspaceName"] = if (KeyspaceName == null) null else KeyspaceName
        ["Tags"] = if (Tags == null) null else Tags
        ["ReplicationSpecification"] = if (ReplicationSpecification == null) null else ReplicationSpecification
    }

}
