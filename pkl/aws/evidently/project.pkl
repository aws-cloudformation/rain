/// AWS::Evidently::Project
///
/// Generated by rain build --pkl-class AWS::Evidently::Project
module aws.evidently.project

import "../../cloudformation.pkl"

/// 
open class ProjectAppConfigResourceObject {
    ApplicationId: String(matches(Regex(#"[a-z0-9]{4,7}"#)))|Mapping
    EnvironmentId: String(matches(Regex(#"[a-z0-9]{4,7}"#)))|Mapping
}

/// Destinations for data.
open class ProjectDataDeliveryObject {
    LogGroup: (String(matches(Regex(#"^[-a-zA-Z0-9._/]+$"#)))|Mapping)?
    S3: (ProjectS3Destination)?
}

/// 
open class ProjectS3Destination {
    BucketName: String(matches(Regex(#"^[a-z0-9][-a-z0-9]*[a-z0-9]$"#)))|Mapping
    Prefix: (String(matches(Regex(#"^[-a-zA-Z0-9!_.*'()/]*$"#)))|Mapping)?
}

/// A key-value pair to associate with a resource.
open class ProjectTag {
    Key: String(matches(Regex(#"^(?!aws:)[a-zA-Z+-=._:/]+$"#)))|Mapping
    Value: String|Mapping
}

/// Resource Type definition for AWS::Evidently::Project
open class Project extends cloudformation.Resource {

    Type = "AWS::Evidently::Project"


    /// 
    hidden AppConfigResource: (ProjectAppConfigResourceObject)?

    /// 
    hidden DataDelivery: (ProjectDataDeliveryObject)?

    /// 
    hidden Description: (String|Mapping)?

    /// 
    hidden Name: String(matches(Regex(#"[-a-zA-Z0-9._]*"#)))|Mapping

    /// An array of key-value pairs to apply to this resource.
    hidden Tags: (Listing<ProjectTag>)?

    Properties {
        ["AppConfigResource"] = if (AppConfigResource == null) null else AppConfigResource
        ["DataDelivery"] = if (DataDelivery == null) null else DataDelivery
        ["Description"] = if (Description == null) null else Description
        ["Name"] = if (Name == null) null else Name
        ["Tags"] = if (Tags == null) null else Tags
    }

}
