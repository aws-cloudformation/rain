/// AWS::Evidently::Segment
///
/// Generated by rain build --pkl-class AWS::Evidently::Segment
module aws.evidently.segment

import "../../cloudformation.pkl"

/// A key-value pair to associate with a resource.
open class SegmentTag {
    Key: String(matches(Regex(#"^(?!aws:)[a-zA-Z+-=._:/]+$"#)))|Mapping
    Value: String|Mapping
}

/// Resource Type definition for AWS::Evidently::Segment
open class Segment extends cloudformation.Resource {

    Type = "AWS::Evidently::Segment"


    /// 
    hidden Description: (String|Mapping)?

    /// 
    hidden Name: String(matches(Regex(#"[-a-zA-Z0-9._]*"#)))|Mapping

    /// 
    hidden Pattern: (String|Mapping)?

    /// An array of key-value pairs to apply to this resource.
    hidden Tags: (Listing<SegmentTag>)?

    Properties {
        ["Description"] = if (Description == null) null else Description
        ["Name"] = if (Name == null) null else Name
        ["Pattern"] = if (Pattern == null) null else Pattern
        ["Tags"] = if (Tags == null) null else Tags
    }

}
